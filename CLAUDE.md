# 報告の作法（ローカルのグローバル設定から同期）

このファイルはWeb版セッション（claude.ai/code）でも同じ規律で動くよう、ローカルの `~/.claude/CLAUDE.md` の要点をリポジトリに同期したもの。発動条件は**内心の自信ではなく、書こうとしている文の形**。

## 一次情報を取り直してから書く（3つのトリガー）

| 書こうとしている文 | 必ずやること |
|---|---|
| **外部の状態を断定する**（「〜になっています」「届いていません」「反映されました」） | **出所と観測時刻を添える。**添えられないなら、それは確認していないということ |
| **同じ事実を2回目以降に言う**（前のターンで確認した内容の再掲） | **「前に確認した」を根拠にしない。**取り直すか「〜時点では」と時刻を明示 |
| **相手に実世界の行動を求める**（「〜してください」） | 最もコストの高い出力。**根拠が今も生きているか直前に取り直す** |

## 伝聞と一次情報を区別する

**伝聞**＝DBのフラグ、キャッシュ、過去のログ、前回の実行結果、自分の過去の発言。
**一次情報**＝当の相手のAPI・実物のファイル・いま動いているプロセスに直接聞いた結果。
伝聞で断定しない。疑わしいときに**1コマンドで一次情報を取れる手段**を用意し、引き継ぎ資料に書き残す。

## 障害を見つけたら、汚染されたデータを洗い出す

「Xが壊れていた」と分かったら、必ず「Xに依存していた値のうち、どれが今おかしいか」を列挙する。発見だけして止まらない。

## 報告の書き方

- 事実には**出所と時刻を付ける**（例：「22:40時点のLINE APIでは〜」）。付いていないものは推測として書く
- 原因が外部でも「うちは悪くない」で終えない。**こちら側で減らせる被害を出し、修正まで実行する**

## `documents.html`（様式の自動入力）を触る前に必ず読む

**`tools/README.md` を先に読むこと。** 点検の道具と、過去に自分で作って自分で見つけた不具合の記録がそこにある。中身（XML）だけを見る点検では見つからない崩れが何種類もあり、**実物のExcel/Wordで開いて紙にして目で見る**以外に検出手段がない。

最低限これだけは覚えておく：

- 記入処理に**様式ごとの決め打ちは無い**（ファイル名で分岐しない）。セル結合幅・字下げ・項目名の言い回し・文字サイズという**形**だけで動く。だから**形が変わった様式だけが危ない**
- 直したら **`tools/diff_fills.py`** で新旧の記入位置と値を突き合わせる。件数が同じでも入れる場所が変わることがある
- **`tools/autofill_audit.json` は golden baseline**（git管理下）。ただし「どのセルに何を入れたか」しか持たず、**列幅・結合・書式は入っていない**。列幅だけ狭まった変更は差分ゼロのまま紙で潰れる
- **`forms/manifest.json` と `forms/README.md` は点検日時を毎回書き直す**ので、`forms/` は毎日コミットされる。`git log -- forms` を「様式が変わったか」の判定に使わない。実物だけ見るなら `":(exclude)forms/manifest.json" ":(exclude)forms/README.md"` を付ける
- 様式が実際に差し替わると **GitHub Issue が自動で立つ**（`.github/workflows/mirror-forms.yml`）。点検手順は Issue の本文に入っている
- **見た目の小さな不満を消しにいくときほど危ない。** 8mmのずれを直そうとして幅の見積もりを壊し、住所と氏名が行の途中で割れてページ送りまで崩した実績がある。直す前と後で「同じ紙が同じページ数に収まっているか」を先に確かめる

## スキルについて

`.claude/skills/` に23のスキルがある。明示的な呼び出しがなくても、説明文の発動条件に合致したら使うこと：
- **webapp-testing** — Playwrightでローカルwebアプリを実ブラウザ検証（Anthropic公式、Apache 2.0）
- **mcp-builder** — MCPサーバー自作ガイド（Anthropic公式、Apache 2.0）
- **grillme** — ソクラテス式の深掘りインタビュー。依頼が抽象的で着手前に詳細を詰める必要があるときにも使う
- **skill-audit** — スキル体系の保守監査（説明文の衝突・参照ずれ・同期漏れ）。**スキルを追加/変更/削除したら同じターンの最後に自動実行する**

### スキルの競合仲裁（どちらが発動するか迷ったらこれに従う）

- **webapp-testing vs uicheck/error-rewrite**：webapp-testingは「道具箱」（単発のブラウザ操作・スクショ・ログ確認・デバッグ）。**完走確認や採点など反復改善のループはuicheck/error-rewriteが主で、その実行手段としてwebapp-testingの道具を使う**。「テストして」だけなら文脈で判断し、迷ったらuicheck
- **hallmark vs artdirection**：競合しない。**hallmarkが本体、artdirectionは日本語の上書き**。日本語画面では両方適用し、書体・字組み・可読性の判断はartdirectionが優先する
- **デザインのテンプレ適用は禁止**：見た目に関わる成果物は hallmark の構造・テーマ選定を経由する。プリセットの当てはめで済ませない（2026-08-02、theme-factoryはこの理由で撤去済み）
- **feedback-sweep** — 本人の修正指摘を失敗パターン化し、同じ型の問題をプロジェクト全体から掃討。大きな誤りを直した直後に使う
- **error-rewrite** — ユーザーに見えるエラー文言を全数棚卸しし、非IT利用者が次の行動の分かる日本語に書き直して実機検証
- **gijiroku** — 音声（会議録音）→文字起こし→議事録要約→リネーム保存→タスク登録の一括処理
- **sharoushi** — 社労士の知識体系（労務・社会保険・雇用関係助成金=厚労省系）。制度の数値・要件は暗記せず一次情報で確認して出典+確認日時を付ける。助成金記事の監修レビューは `.claude/agents/sharoushi.md` のサブエージェントで並列実行可
- **zeirishi** — 税理士の知識体系（税務・会計・補助金の税務処理）。税率・要件は暗記せず国税庁等の一次情報で確認。個別の税額計算・申告判断はしない（無償独占の業際）。監修レビューは `.claude/agents/zeirishi.md` で並列実行可。外注費vs給与の論点はsharoushiと両輪で使う
- **douga** — 世界トップYouTuber級の動画編集者。視聴維持率から逆算した構成・カット指示書・テロップ設計・維持率グラフ診断、ffmpegでの実作業（カット・字幕焼き込み・Shorts変換）。レビューは `.claude/agents/douga.md` のサブエージェントで並列実行可
- **kanshu** — **3士業並列監修の一括処理**（対象抽出→sharoushi/zeirishi/shindanshiを1メッセージで同時起動→重複を除いた重大度順の統合→高を修正→再監修）。事業計画書・補助金記事・申請書類の点検はこれ経由が基本。3件以上の一括はWorkflow提案（本人の合図が必要）
- **shindanshi** — 中小企業診断士の知識体系（補助金=経産省系・競争採択、事業計画評価、行政の仕組みと文化）。金額・要件・締切は当該公募回の公募要領で確認。補助金/助成金の混同チェックが監修の最重要点。監修レビューは `.claude/agents/shindanshi.md` で並列実行可。補助金記事の3監修並列は **kanshu** が統括する
- **devflow** — 開発フロー一気通貫（redteam→artdirection→slice→expert-review→propagation→uicheck→handoff）。「新機能を作って」等のまとまった開発依頼はこれ経由を基本とする
- **hallmark** — AIっぽさ回避デザインの本体（21マクロストラクチャ／20テーマ／58スロップテストゲート、audit・redesign・study の動詞付き。MIT、Nutlope/hallmark）。新規UI構築・リデザイン・既存デザインの採点で使う
- **artdirection** — **hallmarkの日本語アダプタ**。日本語の画面をhallmarkで扱うときは必ず併用し、書体（BIZ UDGothic等の明示指定）・字組み（line-height 1.7〜1.9、全角35〜45文字）・日本語コピー・非IT利用者向け可読性優先の上書きを適用する。単独では使わない
- **slice** — 本番システムへの機能追加は必ずこれ経由（証拠確認→最小変更→検証→停止）
- **expert-review** — 3専門家（基本品質/アーキテクチャ/ドメイン）の並列品質レビュー。まとまった実装の後に
- **propagation** — 複数ファイルに現れる値を変更したら締めに実行
- **uicheck** — クリーンな実ブラウザで初見完走の確認＋画面採点を1周で行い、障害→最弱点の順に改善（旧uidesign+firstuserの統合）
- **renderproof** — 生成物を**実物として出力して**崩れを見る（帳票・様式の自動記入、PDF/Excel/Word生成、印刷レイアウト）。中身の検査では見つからない崩れ（枠に収まらない・行が割れる・ページが増える）専用。画面の操作性はuicheck、単発のブラウザ操作はwebapp-testingで、競合しない
- **handoff** — セッション終了時の引き継ぎ作成
- **groundtruth** — 証拠ベースの読み取り専用監査
- **redteam** — 設計確定前の批判レビュー

開発の流れ：redteam（設計前）→ artdirection（見た目の方向）→ slice（実装）→ expert-review（品質）→ uicheck（リリース前）→ groundtruth/propagation/handoff（運用）
