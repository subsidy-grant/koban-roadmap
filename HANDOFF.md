# 引き継ぎ

## 次の安全なアクション（最新・2026-08-17 第25セッション）

**制度種別（補助金／助成金）の全数監査は、電話照会が必要な2件を除いて完了した。**
128件中126件を一次情報で確定済み。残り2件は横須賀市の制度で、要綱が非公開のため
Webからは確定できない（下の「### 残り2件」参照）。

**次にやること: 特に無い。** 監査の続きをやるなら残り2件の電話照会だが、本人の判断が要る。
以降は週次更新で制度が増えたときに、その分だけ判定すれば維持できる。

**再開時にまず実行するコマンド**（この文書の数字を信用しないこと）:

```sh
cd /d/Claudecode/koban-roadmap
git status --short && git rev-parse --short HEAD origin/main
python -X utf8 _tools/check_program_type.py     # カバレッジ（未確認が何件か）
```

**同期状態**: 本セッションの変更はすべて push 済み。ハッシュは上のコマンドで取り直すこと。

### ⚠ 最後のコミットのデプロイが失敗したまま残っている（対応は次セッションへ）

**サイトの表示には影響しない。慌てて直さなくてよい。**

2026-08-17 18:31 UTC、この HANDOFF.md を更新したコミット（`9b108ea`）のデプロイが
**GitHub Pages 側の 503 で失敗**した。同時刻に GitHub 全体が Partial System Outage で、
インシデントが investigating の状態だった。エラーは以下：

```
Failed to create deployment (status: 503) with build version 9b108ea...
Server error, is githubstatus.com reporting a Pages outage?
```

**自分側のコードの問題ではない。** 同じ障害はこのセッション中に2回起きており、
1回目は空コミットで新しい実行を起こしたら成功した（`gh run rerun --failed` は
同じく503で失敗したので、空コミットのほうが有効）。

**サイト本体への影響が無いことは実物で確認済み**（2026-08-17 18:33:52 UTC に本番を
実ブラウザで観測。栃木・神奈川・国の判定と予算枠の注記がすべて最新で出ている）。
失敗したのは HANDOFF.md だけのコミットで、表示に使うファイルは1つも含まれていない。

再開時の手順：
1. `gh run list --limit 3` で最新の状態を見る（障害が収まっていれば放置でも
   次のコミットのデプロイで一緒に反映される）
2. 単独で直すなら `git commit --allow-empty` で空コミットを作って push する
3. `curl -s https://www.githubstatus.com/api/v2/summary.json` で Pages の状態を
   確認できる。ただしコンポーネント表示が operational でも実際は503が返ることが
   あったので（このセッションで実測）、表示ではなく実際のデプロイ結果で判断すること

---

## 2026-08-17 第25セッション（本人指示2点＋全数監査の完了）

### 本人指示と結果

| 指示 | 結果 |
|---|---|
| ①A（自治体）から着手 | **完了**。神奈川18・埼玉16・東京14・群馬9・千葉7・栃木4 = 68件すべて判定 |
| ②表示ははっきり違いが分かるように | **完了**。ただしレビューを受けて設計から作り直した（下記） |
| ③（追加）専門家のレビューを入れる | **完了**。社労士・診断士のレビューで自分の誤りを3件発見して修正 |

### 監査の結果（2026-08-17時点）

**128件中126件が確定済み**（override 79件＋名称どおり確認済 47件）。

判定の傾向がはっきり出た。**自治体は名称と実質の不一致が多数派、国は逆にほぼ一致**：

| 層 | 傾向 |
|---|---|
| 市区町村 | 大半が josei（名称は「補助金」だが実質は要件充足型）。「不備がなければ交付」型で、超過分は抽選か先着で配分 |
| 県・政令市・公社 | **hojo が集中**。審査の観点表・加点・採択件数を持つ。誤判定もここに集中した |
| 国（経産省系の補助金） | ほぼ全て hojo。外部審査委員会・加点・口頭審査を持つ |
| 国（厚労省の雇用関係助成金） | 全て josei。18件すべてで競争的採択なし |

### 表示の最終形

制度名の下に3行で出る（[program.html](program.html) の `render()` 内）:

```
［相談先: 社会保険労務士か、管轄の労働局］     ← 色付きバッジ。hojo/joseiで中身が変わる
決められた条件に当てはまるかどうかで決まり、事業計画の内容で他社と比べられることはありません
┃申し込む前に必ず: ①発注・契約は「交付決定」の通知が届いてから ②お金は後払いです
```

- **主役は「誰に相談すればいいか」**。本人の判断で、制度の呼び名ではなく相談先を先頭に出した
  （hojo は「商工会・商工会議所か、認定経営革新等支援機関」）
- 3行目の事故防止2点は**種別を問わず全制度で表示**する
- josei でも予算枠で打ち切られる制度には、`programHasBudgetCap()` が
  「ただしこの制度は予算の上限があり、条件を満たしても受けられないことがあります」を足す

### 判定基準（旧い基準は使わないこと）

**過去の引き継ぎに書いてあった考え方は誤りだった。** 使ってはいけない根拠：

| 根拠 | なぜ使えないか |
|---|---|
| 「予算の範囲内において」 | ほぼ全ての交付要綱に入る定型句。競争型の要綱にも必ず入っている |
| 「先着順」「予算に達し次第終了」 | 受付の締め方であって、受付後の審査の性質を語らない。両者は同一文書内に同居する |
| 「審査し、適否を決定」 | 要件審査と競争審査の両方を包含する定型句 |
| 「書類審査」 | 複数の自治体で「書類不備等がある場合」の意味だった。2段階選抜の証拠になるのは面接審査・プレゼン審査と共起するときだけ |

**正しい向き: hojo の積極的証拠を探し、無ければ josei。** 決定的なのは4つで、1つでもあれば hojo：

1. 審査の観点／審査項目の表があり「妥当性」「総合評価」など程度を測る語がある
2. 加点項目がある（相対比較の道具なので単独で決定的）
3. 不備以外の理由で不採択・不交付がありうると書かれている
4. 採択率・採択件数・採択公表への言及がある

なお「予算の範囲内」でも、**「総合的な評価が高いものから順に、予算の範囲内で採択」**の
ように評価順と結合していれば競争型の証拠になる（千葉県成長促進が実例）。

### 判定に使うコマンド

**判定する前に必ずこれを通すこと。今回の誤りはほぼ全てこれで防げた。**

```sh
python -X utf8 _tools/check_program_evidence.py <要綱・募集要領のPDFのURL>
```

このツールが潰している罠（すべて実際に踏んだもの）:

1. **文字化けの偽陰性** — 抽出に失敗するとキーワード検索は必ず0ヒットになり、
   「証拠が無い＝josei」と静かに誤る。日本語比率で検証し、pdftotext → pdfminer と
   取り直す（pypdf が化けても pdftotext なら読めるPDFが実在する）
2. **縦組みPDF** — 縦書きは1文字ずつ改行されて抽出されるためキーワード検索が必ず
   0ヒットになるが、日本語比率は正常に見える。1〜2文字の行の比率で検出して判定を止める
   （正常なPDFは0.02〜0.13、縦組みは0.40〜0.67）
3. **文字間に空白を挟む組版** — 「三 鷹 商 工 会」のような組版で素の検索が取りこぼす。
   空白除去後の版でも検索する
4. **公募要領の取り違え** — 前提制度の申請要領やチラシを読むと証拠0件になる。
   抽出が2万文字未満で0ヒットなら「そもそも公募要領を読んでいるか」を警告する
5. **判別力ゼロの定型句** — 「予算の範囲内」「先着順」を見つけたら根拠にするなと警告
6. **語は同じでも意味が違うケース** — 「（他制度の）採択を受けていないこと」＝重複受給の
   制限、「審査請求料」＝特許の経費名、「取引の妥当性」＝利益相反の例外規定。
   だから出力は「証拠」ではなく「証拠候補」と呼び、文脈確認を促す

### 残り2件（電話照会が要る）

いずれも横須賀市で、**固有の交付要綱が市サイト・例規集とも未掲載**。Webからは確定できない。
現在は名称マッチで hojo と表示されており、推定判定とも一致しているため実害は小さい。

| key | 制度 | 推定 | 照会先 |
|---|---|---|---|
| `kanagawa_yokosuka_ict` | 横須賀市小規模事業者ICT支援補助金 | hojo | 横須賀商工会議所 産業・地域活性課 046-823-0402 |
| `kanagawa_yokosuka_senshinteki` | 横須賀市先進的技術開発促進事業補助金 | hojo | 横須賀市 企業誘致・工業振興課 046-822-8290 |

聞くべき論点は両方とも共通で3点：①審査の観点・評価項目を定めた文書はあるか
②書類不備以外の理由で落ちることがあるか ③採択枠（件数）は決まっているか、
申込多数時は優劣を比べるのか先着か。**両方とも横須賀市内なので1回の照会作業でまとめられる。**

なお `kanagawa_yokosuka_senshinteki` は令和8年度に
**「ものづくり技術開発促進事業補助金」から改称された同一制度**（市ページに明記）。
旧名称なら例規集に要綱が残っている可能性がある。

### 本セッションで見つけた自分の誤り（すべて修正済み）

同じ型を繰り返さないために残す。

1. **本番に事実と異なる表示を出していた（重大）** — 助成金の説明に「予算の上限があり、
   先着順で受付が終わることがあります」と書き、キャリアアップ助成金など雇用関係助成金
   20件に表示していた。神奈川の自治体制度の性質を全josei制度へ一律適用したのが原因。
   **種別バッジに書けるのは審査の性格までで、受付方式は制度ごとに違う**
2. **制度種別の判定を2件間違えた** — `kanagawa_digital` は「先着順」の記述を見た時点で
   探索を打ち切り、同じ要領10ページ後の採択審査・加点を読み落とした。`tokyo_soui` は
   旧基準による誤判定で、実際は書類審査＋面接審査の2段階選抜だった
3. **何も間違えていない自治体を非難する表示を出していた** — 「名称は『助成金』ですが
   運用の実態は補助金です」と書いたが、**補助金適正化法第2条には「助成金」の定義が無く、
   両者を区別する条文も無い**（同法は国の交付分のみが対象で市の制度は適用外）。
   誤っていたのは「名称から性質が決まるはず」というこちらの前提だった
4. **予算枠の検出が17件漏れていた** — 言い回しを完全一致で列挙していたのが原因。
   「枠を表す語（抽選/先着/予算）」と「打ち切りを表す語」の共起で判定する方式に変えた

### あわせて直したリンクの不良

種別監査の副産物として見つけたもの。**リンク切れ検査では検出できない型**が多かった。

- **富士見市23件** — 市がディレクトリを移設し配下の様式が全滅（404）。移設先へ一括置換
- **とちぎ賃上げの記入例① 1件** — 県の現行ページからも消えていたため掲載を取り下げ
- **高崎市** — 市のトップページを指しており制度ページに届いていなかった（404にならないので
  検査をすり抜ける）。実ブラウザで辿って特定。※高崎市は Incapsula が入っていて
  curl では中身が取れない
- **足利市** — 課の補助金一覧表を指しており制度の個別ページに届いていなかった
- **練馬区・府中市** — 200で生きているが中身が「詳細は○○のホームページをご覧ください」
  だけで金額も要件も無い。実質の一次情報がある実施主体側（練馬ビジネスサポートセンター、
  むさし府中商工会議所）へ差し替えた

2026-08-18 02:45 JST の全数チェック（1164件）で**リンク切れ0件**を確認済み。
Issue #2 は実測値を添えてクローズした。

### 二分法に収まらない制度

「助成金だから条件を満たせば受けられる」が成り立たない制度が実在する。

- **抽選** — 相模原市省エネ設備、上尾市設備導入、越谷市設備導入、前橋市DX、太田市DX
- **予算枠での受付終了（雇用関係助成金にもある）** — 業務改善助成金、働き方改革推進支援、
  65歳超雇用推進（継続雇用促進コース）。いずれも「予告なく」と明記
- **減額交付** — とちぎ賃上げは予算超過時に不採択ではなく減額して交付決定される
- **枠で性質が違う** — 前橋市設備投資は生産性向上枠が採点競争、省エネ枠が要件確認型

該当7件は `programHasBudgetCap()` が注記を出す。枠差のあるものは note に明記した。

### 触ってはいけない領域

- **本番 `main` への push は即デプロイ**（GitHub Pages）。検証前に push しない
- `page_data.js` は**自動生成物**。制度データの実体は `index.html` の中にあり、
  `python -X utf8 _tools/build_page_data.py` で再生成する。
  種別判定は `program.html` の `PROGRAM_TYPE_OVERRIDE` 側で持つこと
- **確認したが名称どおりだった制度は `PROGRAM_TYPE_CONFIRMED_AS_IS` に記録する。**
  これが無いとカバレッジ上は未確認と同じ扱いになり、同じ調査を繰り返す
- 配色トークンは13パターンを数値検証して選んだもの。触るときは
  `python -X utf8 _tools/check_palette.py` で **NG 0件**を保つ
- **バッジの文字色は `--on-accent` を使うこと**（昼=白／夜=地の濃色）
- `sw.js` の `CORE_ASSETS` は、**存在しないファイルを1つでも残すと `cache.addAll` が
  まとめて失敗し Service Worker のインストールごとこける**。ページを増減したら必ず更新し、
  `CACHE_NAME` の版も上げる（現在 v20）

### 再開者が仮定してはいけないこと

- **「監査が終わったから以降は不要」と仮定しない。** 週次の自動更新
  （trig_01Vbn8wKa1hdiWyr1DS1ciYo）が `page_data.js` を書き換え、制度が増えれば
  その分が未確認として出てくる。`check_program_type.py` を回せば分かる
- **「override に入っている＝今も正しい」と仮定しない。** 制度は毎年改定される。
  特に第1〜4弾（〜2026-08-13）の31件は今回否定された旧基準で判定されたもので、
  洗い直しはしたが、内容そのものの再確認までは毎年必要
- **「名称に補助/助成と書いてあるから正しい」とも「名称と違うはずだ」とも仮定しない。**
  自治体は不一致が多数派、国はほぼ一致という差がある
- **検査ツールの0ヒットを鵜呑みにしない。** 縦組み・文字化け・文書の取り違えで
  0になる。ツールは警告を出すが、最後は原文を読むこと

---

## 2026-08-17 UI改善6件（**全件完了・push済み・本番反映確認済み。着手不要**）

> **【2026-08-18 追記】この節の6件はすべて実装・検証・本番反映まで完了した。**
> 以下の「未着手」という記述は**着手前に書かれたもので、現在は当てはまらない**。
> 実施結果は下の「### 2026-08-17 実施結果（第23セッション）」にまとめてある。
> 次にやることは、この節ではなく冒頭の「制度種別の全数監査（A=自治体68件）」。

### 現在の目標（※着手前の記述）

初見ユーザー（補助金をよく知らない経営者）視点でサイトを調査し、3コンセプト
（①ポップ/スタイリッシュで見やすい ②ワンストップ ③迷わない動線）への
適合を批判的に評価した。その改善案に対して本人が採否を判断した状態。
**判断は済んでいるが、実装は1件も行っていない。**（←着手前の記述。現在は完了済み）

### 実装する6件（優先順は本人指定なし。上から着手を推奨）

| # | 内容 | 場所 | 仕様 |
|---|---|---|---|
| **C-2** | タブ名「改善計画」を変更 | `app_tabbar.js` ほか | **本人指示「お勧めの案、名前で進めて」＝命名はこちらで決めてよい。** 中身は業種別の使い道プラン集（概算金額・補助率つき）。「改善計画」だと何のページか伝わらないのが理由 |
| **C-3** | 書類準備の手順番号の飛びを解消 | `documents.html` | 初見時に「手順1 →（無番）→ 手順5」に見える。**原因は調査済み**（下記「調査で判明した事実」参照） |
| **C-5** | 専門用語をひらく | 主に `index.html` / `criteria.html` | **本人指示「お勧めで」＝対象語と表現はこちらで決めてよい。** 例:「採択（＝審査に通ること）」。検索例の「持続化、キャリアアップ」も制度略称で初見に通じない |
| **B-3** | 採択後フェーズを追加 | `applications.html` | 申請までは手厚いが採択後（実績報告・返還回避）が機能として無い。`criteria.html` の返還パターンをチェックリスト化して繋ぐ |
| **B-4** | 「申請の進み具合」と「現在の申請状況」を統合 | `applications.html` / `profile_status.html` | 初見では2ページの区別がつかない。**本人指示「その方針で進めて、会社情報から見れるようにして」＝統合し、入口は会社情報(`profile.html`)から** |
| **B-1** | 「はじめての方へ」を追加 | `index.html` | **トップに「03」としてカード開閉式で追加（既定は閉じる）。** 内容は補助金と助成金の違い、**それを扱う士業の違い**なども説明する |
| **C-4** | 「このサイト・アプリの使い方」を追加 | `index.html` | **トップに「04」としてカード開閉式で追加（既定は閉じる）。** 01/02と同じ `.sec-toggle` の意匠に揃える |

※ 03・04 は既存の 01「制度を探す」/ 02「採択基準・返還基準 調査ノート」に続く番号。

### 不採用・保留（**蒸し返さないこと**）

- **A-2「ヒーロー写真を業種連動に」→ 対応不要。** 提案時に「写真が美容室1枚だけ」と書いたが
  **これは調査の誤り**。本人の指摘を受けて本番で取り直したところ、6業種すべて別写真に
  切り替わっていた（`INDUSTRY_HERO_PHOTO`、index.html L2913〜。21:51:36時点の本番で実測）。
  初期表示が美容業なのを見て一般化した誤報だった
- **A-1「ポップに寄せるかコンセプト表記を改めるか」→ 本人回答なし。** 触らない
- **B-2「締切のカレンダー登録(.ics)」→ 保留。** 本人が明示的に保留と判断
- **C-1「診断3問をアコーディオンの外に出す」→ 不採用。** 理由「画面が縦長になる」

### 調査で判明した事実（実装時にそのまま使える）

- **C-3 の原因**: `documents.html` の手順2〜4は**制度を選んだ後にJSで動的生成**される
  （手順2は L4568 付近で `'<p class="step-ttl">手順2　この制度に必要な書類（…件）</p>'` を生成）。
  静的に書かれているのは手順1（L475）と手順5だけ。だから**制度未選択の初見時だけ**
  「1 →（無番の箱）→ 5」に見える。番号を振り直すのではなく、
  **未選択時の見え方**をどうするかの問題として設計すること
- **B-4 の現状**: `applications.html`（申請の進み具合）と `profile_status.html`（現在の申請状況）が
  別ページで併存。`profile.html` のカードから `profile_status.html` へ導線がある
- **C-2 の現状**: タブ定義は `app_tabbar.js` L22。
  `{ href: 'improvement.html', match: /^improvement\.html$/, icon: ICON_BULB, label: '改善計画' }`。
  **`improvement.html` の `<title>` `<h1>` `<meta description>` と、`index.html` の
  meta description にも「改善計画」の語がある**ので、名前を変えるならまとめて直す

### 検証済みの証拠（出所と時刻つき）

- **21:31〜21:45時点**、本番URL `https://subsidy-grant.github.io/koban-roadmap/` を
  クリーンセッション（localStorage・Cookie無し）の実ブラウザで走査。
  375px / 1280px の両方でトップ・書類準備・改善計画・相談・会社情報・採択基準・
  申請進捗のスクリーンショットを取得
- **「制度を見つけて書類準備に着手する」までは中断なしで完走できた**（21:45時点）。
  動線の骨格は壊れていない。上の6件は完走を妨げる障害ではなく、改善項目
- **21:51:36時点の本番**で業種6つを実際に切り替え、ヒーロー写真が6種すべて
  異なることを確認（A-2が不要である根拠）
- **21:52時点**でローカルHEAD・origin/main ともに `1f914f4`、作業ツリーはクリーン

### 一次情報の取り直しコマンド

```sh
cd /d/Claudecode/koban-roadmap
git status --short && git rev-parse --short HEAD origin/main   # 同期状態
python _tools/check_palette.py         # 配色トークン（ライト・ダーク17項目）
python _tools/check_live_palette.py    # 本番のトークンが新配色か
python _tools/check_live_contrast.py   # 本番で実際に描画された色（読めない文字を検出）
python _tools/check_live_toc.py        # 本番の criteria 目次（重なり・背景固定）
```

### 未着手の範囲

- **上の6件はすべて未着手。コードは1行も変更していない**
- 03・04 の**本文（説明する中身そのもの）は未執筆**。士業の違いをどう書くかは
  `sharoushi` / `zeirishi` / `shindanshi` スキルの監修を通すか、本人確認が要るかもしれない
- C-2 の**新しいタブ名は未決定**（本人が「お勧めで」と委任）。決めたら
  `app_tabbar.js` / `improvement.html` / `index.html` の meta を揃えて直す

### 不確実な点・リスク

- **調査レポートの他の指摘も、A-2と同じ誤りを含んでいる可能性がある。**
  実装前に対象箇所を必ず実物で確認すること（A-2は本人の指摘で誤報と判明した）
- 03・04 をトップに足すと**トップが縦に伸びる**。本人は C-1 を
  「画面が縦長になる」という理由で不採用にしている。**既定を閉じる**のはそのため。
  閉じた状態でファーストビューが破綻しないか、実装後に375pxで実測すること
- B-4 の統合は**ページ削除を伴う可能性がある**。どちらを残すかは実装前に本人確認を推奨
- 本番の観測（21:31〜21:52）は時間が経つと腐る。再開時はまず上のコマンドで取り直す

### 触ってはいけない領域

- **本番 `main` ブランチへの push は即デプロイ**（GitHub Pages）。検証前に push しない
- `_tools/verify_live.json` / `_tools/live_contrast.json` は実行のたび変わる中間結果。
  `.gitignore` 済みなので追跡に戻さない
- 週次自動更新ルーチン（trig_01Vbn8wKa1hdiWyr1DS1ciYo）が本番を書き換える。
  同じファイルを同時に触るとコンフリクトしうる
- 配色トークンは13パターンを数値検証して選んだもの。触るときは
  `python _tools/check_palette.py` で **NG 0件**を保つ

### 最後の判断・関門

- **直前の判断**: 本人が改善案6件を採用・3件を不採用/保留と決定（21:52時点、上表のとおり）
- **本人確認待ち**: なし（採否は確定済み）。ただし実装中に
  ①C-2の新しいタブ名 ②B-4でどちらのページを残すか ③03の士業説明の記述内容
  は、こちらで決めて進めてよいが、影響が大きいと判断したら確認する

### 再開者が仮定してはいけないこと

- **「調査レポートに書いてあるから事実」と仮定しないこと。** A-2は実際に誤報だった。
  実装前に必ず実物を見る
- **「6件は独立している」と仮定しないこと。** 03・04 はどちらもトップのカードで、
  番号・意匠・ファーストビューの高さで干渉する。片方だけ実装して測ると判断を誤る
- **「C-3は番号を振り直せば済む」と仮定しないこと。** 手順2〜4は動的生成で、
  未選択時に存在しないことが原因。番号の付け替えでは直らない

### 2026-08-17 実施結果（第23セッション。全件完了・push済み）

コミット（`655fabe` → `7803ac2`、すべてpush済み。デプロイも成功）：

| # | 内容 | commit |
|---|---|---|
| C-2 | タブ「改善計画」→**「使い道の例」**。h1/title/meta も改称 | `655fabe` |
| C-3 | 未選択時だけ出る**破線の予告カード**で手順2〜4を予告し、番号の飛びを解消 | `d66be42` |
| C-5 | 採択・返還・公募要領・認定支援機関を初出でひらく。検索例も平易化 | `dd67eba` |
| B-4 | `profile_status.html` を**削除**し applications.html へ統合 | `27151a6` |
| C-4 | トップに「04 このサイト・アプリの使い方」 | `e7cc96f` |
| B-1 | トップに「03 はじめての方へ」＋ criteria.html の誤記2件を修正 | `ba382f1` |
| B-3 | 「審査に通ったあとにやること」16項目4段階 | `b7deb63` |
| 追加 | 04のヒント1行を削除、「よくある誤解」に開閉アイコン（本人指摘） | `7803ac2` |

**本番で全件の反映を確認済み**（2026-08-17 13:37:53Z と 15:12:08Z の2回、実ブラウザで実測）。

**この過程で criteria.html の既存の誤記2件を一次情報で修正した**（ものづくり補助金
【補助事業の手引き】19次締切版のPDF原文を取得して確認、2026-08-17）：

1. **収益納付**: 「ものづくり補助金等で自己負担超過分を納付」→ 手引きP.36に
   「収益納付は19次以降は不要です」。**ただしP.37に、テスト販売の枠を外れた量産販売等では
   「必要となることがある」とあるため、廃止と言い切っていない**（監修エージェントは
   「19次以降廃止」と報告してきたが、原文を自分で読んで言い過ぎと判明した）
2. **賃上げ未達の返還**: 「1年目未達で全額、2年目2/3、3年目1/3」→ 手引きP.23〜24と別物。
   実際は賃金増加要件が最終年度に1回だけ判定で交付額×(1−達成率)、事業所内最低賃金要件が
   毎年判定で交付額÷年数

**B-3で制度別の数値をハードコードしていない理由**：相見積の基準額は
ものづくりが単価**税抜50万円以上・2社以上**（中古品3社以上）、持続化が発注総額
**税込100万円超・2者以上**と、税抜税込も単価総額も社数も違う（両手引きの原文で確認）。
ハードコードすると腐ったときに気づけないため「その制度の手引きで確認」に寄せた。

**B-4に伴う副作用の対処**：`sw.js` の `CORE_ASSETS` から削除ファイルを外し
`CACHE_NAME` を v19→**v20** に上げた。消えたファイルを残すと `cache.addAll` が
まとめて失敗し Service Worker のインストールごとこけるため。

---

## （以下、第22セッションまでの記録）

## 前セッションの状態（2026-08-17 第22セッション、**すべて完了**）

**指示された作業は終わっている。次にやるべき決まった作業は無い。**
新しい指示を待つか、下の「残っている宿題」から選ぶ。

**20:15時点でローカルHEADとorigin/mainはともに `b4861b7` で一致、作業ツリーはクリーン。**
本番にも反映済み（live URLを取得して `@media (hover: hover)` の存在を確認、
`_tools/check_live_contrast.py` で読みにくい文字0件を確認。いずれも20:15時点）。

### 第22セッション後半の指示と、その結果

本人の指示は5点（①書き出し・読み込みの青いラベル ②採択基準ページの色と
見やすさ ③各制度の詳細の色 ④プロフィール編集 ⑤申請状況）。
**すべて対応済み。** 途中で、指示に含まれていなかった実害のある不具合も
2件見つけて直した（下記「見つかった実害」）。

#### 見つかった実害（指摘の横展開で発見。指示外だが直した）

1. **ダークでボタンの文字がほぼ読めなかった**（cr=2.10、基準4.5）
   `--on-accent` のダーク側が未定義で、明るいコバルト地に白文字が載っていた。
   program.html（3箇所）と **index.html（8箇所）** で発生。index.html は
   `[data-theme=dark]` にはあるのに `@media` 側に無く、**OS設定がダークの人だけ**
   読めない状態だった
2. **薄い色地の上の文字が WCAG AA を割っていた**（cr=4.14、基準4.5）
   `--rust-ink` / `--sage-ink` が criteria/index にしか定義されておらず、
   他8ファイルは `--rust` を wash 地にそのまま載せていた。documents.html の
   「必須」「該当者のみ」など。**トークンの有無を見るだけでは見つからず、
   描画された色を測って初めて出た**

#### 再開者が仮定してはいけないこと（今回の学び）

- **「トークンが新配色ならページも正しい」と仮定しないこと。** 上の2件はどちらも
  トークン展開後に残っていた。`_tools/check_live_contrast.py` で描画を測ること
- **`--rust-wash` / `--sage-wash` / `--accent-wash` を地に直接敷くと必ず沈む。**
  実測でサンド地(#e8e0d2)との差は 1.068〜1.125 しかない（基準1.30）。
  地に置く面は白カード(--paper-raised)にして、色は枠・帯で出す
- **wash 地に文字を載せるときは必ず `-ink` 側を使う。** `--rust`/`--sage` を
  そのまま載せると4.14前後でAAを割る
- **ダークの `--paper-sunken` は1.30に到達できない。** 地(L=0.0038)とカード
  (L=0.0211)の幅が狭く、その中間に置く以上どうやっても届かない。
  罫線で輪郭を出す設計であり、数値だけ見て「直っていない」と判断しないこと
- **「カード」を地に沈めない。** `--paper-sunken` を面に敷くと、ライトでも
  地との差は1.34しかなく、本人には「背景と同系色」に見える（2026-08-17に指摘）。
  カードは白地(`--paper-raised`)＋`--shadow-card`＋左の色帯で浮かせる。
  沈めてよいのは**カードではないもの**だけ（`index` の `.hero-industry` のような
  ページ幅いっぱいの帯、表のヘッダー行、カードの中の見出し帯）
- **面が分離しているかの判定に「枠があるからOK」を使わないこと。**
  本人が指摘した `.editing-note` は枠があっても同系色に見えていた。
  基準は「面cr>=1.30 **または** 影がある」。影は面を地から浮かせる
- **`:hover` で背景・枠色を変えるときは `@media (hover: hover)` で囲うこと。**
  スマホでは指を離しても貼り付き、押していないものが選択中に見える。
  **「背景色」だけ見て済ませないこと** — border-color だけ変わるものも同じように
  貼り付く（最初この観点が漏れて index.html の `.sec-toggle` などを取りこぼした）。
  囲うのは「地・枠に色が付くもの」だけでよい。`filter: brightness`・下線・
  打ち消し(`transparent`)・`[disabled]` は貼り付いても選択中には見えないので対象外
  （2026-08-17時点で囲いの外に28件あるが、すべてこの無害な側）。
  `:focus-visible` はキーボード操作に必要なので**囲いの外に出す**
- **タッチの検証で「1回タップして色が変わったら不具合」と判定しないこと。**
  開閉ボタン(`.sec-toggle`)や選択肢(`.ent-btn`)は、タップで実際に状態が変わって
  色が付くのが正しい。**もう一度押して元に戻るかどうか**で切り分ける
  （この判定を誤って、正常なものを不具合と読み違えた）
- **criteria の目次を触るときは `body.toc-open` の存在を忘れないこと。**
  目次を開いている間は body を `position: fixed` にして背景を止めている
  （止めないと目次だけ固定され後ろが流れて重なる。本人指摘 2026-08-17）。
  これに伴う副作用が2つあるので、目次まわりを直すときは必ず確認する:
  * body が fixed になると `position: sticky` が基準を失う。追従バーが
    ページ上部の元位置に取り残されて目次に食い込むため、開いている間は
    `body.toc-open .toc-bar` で fixed に切り替えている
  * 閉じるときの `window.scrollTo(元の位置)` と、目次項目のアンカー移動は
    ぶつかる。**項目クリック時は「元へ戻す」処理を通さない**別経路にしてある

#### 1コマンドで取り直せる確認手段

```sh
cd /d/Claudecode/koban-roadmap
python _tools/check_palette.py        # トークンの値（ライト・ダーク17項目）
python _tools/check_live_palette.py   # 本番のトークンが新配色か
python _tools/check_live_contrast.py  # 本番で実際に描画された色（読めない文字を検出）
```

### 配色刷新は全10ページで完了・本番反映済み（第22セッション）

前セッションで index.html だけ新配色にして中断していたぶんを、残り9ページへ展開した。
**混在は解消している。**

本番（live URL）を実ブラウザで直接開いた結果 — **2026-08-17 19:01:37 時点**:

| 確認項目 | 結果 |
|---|---|
| 10ページすべて `--paper: #e8e0d2`（新配色） | OK |
| `--paper-sunken` / `--line-strong` / `--shadow-card` | 全ページ入っている |
| カード/地のコントラスト | 1.311（基準 1.30） |
| 旧配色の残存 | 0 |
| JSエラー | 0 |

ローカルでは **OSライト/ダーク × 手動テーマ(未設定/light/dark) × 10ページ = 52通り**で
NG 0件（375px横あふれ0も含む）。

**再確認したくなったらこの1コマンドで取り直せる**（伝聞ではなく一次情報が取れる）:

```sh
cd /d/Claudecode/koban-roadmap
python _tools/check_live_palette.py
```
Playwright が無い環境なら、最低限の確認だけなら:
```sh
curl -s https://subsidy-grant.github.io/koban-roadmap/criteria.html | grep -o -- "--paper: #[0-9a-f]*" | head -1
# → --paper: #e8e0d2 なら新配色が本番に出ている
```

### 第22セッションで分かった、前の引き継ぎが間違っていたこと

- **「各ファイルに :root が4ブロックある」は誤り。** `program.html` と `consult.html` は
  **2ブロックしかない**。この2ページは `data-theme` 属性に非対応で、OSの
  `prefers-color-scheme` にだけ従う（配色刷新以前からの既存挙動で、バグではない）。
  ダークモード切替ボタンを作るときはこの2ページに手当てが要る
- **「`--accent-wash` を地に使っている箇所を直す」を全部やるのは誤り。**
  白カードの上に載る `--accent-wash`（バッジ・ホバー・選択状態）は問題ない。
  実ブラウザで「祖先をたどった実効背景」との比を測ったところ、**本当に沈んでいたのは
  3箇所だけ**だった（criteria `.disclaimer` / profile_edit `.editing-note` /
  improvement `.ind-picker`、いずれも 1.12）。推測で広く塗り替えないこと
- **grep だけでは旧色を掃討しきれない。** select の矢印がインラインSVGの中に
  `stroke="%234d6a8a"` と **URLエンコードで**旧色を直書きしていた。
  `grep -o "%23[0-9a-fA-F]\{6\}"` で洗い出すこと（`%23` = `#`）

### 再開者が仮定してはいけないこと（配色まわり）

- **`--ink` のダーク値 `#eaf1fb` は旧ライトの `--paper` と同じ値**。単純な文字列置換だと
  取り違える。apply_palette.py は `--ink:` を狙って先に処理してから他をやる（順序が重要）
- **「配色の判断は好みの問題」と仮定しないこと。** 採用値は13パターンを数値検証して
  選んでいる。触るときは `python _tools/check_palette.py` を流して **NG 0件**を保つ
- **`_tools/apply_palette.py` はもう流す必要がない**（全ページ適用済み。再実行しても
  旧値が無いので0件になるだけで無害）。新しいページを作ったときの参考として残してある

### この配色にした理由（数値）

指摘は「単調」「カードが背景と同系色」「強弱が認識しづらい」。実測すると
**カード(#ffffff) vs 背景(#eaf1fb) が 1.14:1** しかなく、面が分離していなかった。
本文の可読性は 16.46:1 で足りていたので、**問題は文字ではなく面の区別**だった。

| 指標 | 変更前 | 変更後 |
|---|---|---|
| カード/背景 | 1.14:1 | **1.31:1** |
| 対象/締切の色覚差 | 56.4 | **66.4** |
| 本文/カード | 16.46:1 | 17.40:1 |

ライト・ダークとも17項目すべて基準通過（NG 0件）。

強弱は本人指示で**影と色帯の両方**でつけた:
- `--shadow-card` / `--shadow-card-strong` の2段を新設し、重要なカードほど強く浮かす
- `.sec-toggle` の左の色帯を 5px → 8px に太く

### 第22セッションの変更内容（`77cbed9`、push・デプロイ済み）

- 9ファイルの全 `:root` ブロックを新配色へ（325箇所）
- `--shadow-card` / `--shadow-card-strong` / `--paper-sunken` / `--line-strong` を
  全32ブロックへ追加
- 旧配色の青ベースの影 `rgba(12,32,54,0.12)` → `rgba(60,46,26,0.16)`（16箇所）
- インラインSVG（selectの矢印）の旧色を `%23887d63` に統一
  （program.html の `%23767d8a`、index.html の `%235a6478`）
- 地に沈んでいた3箇所を `--paper-sunken` + `--line-strong` + 左帯へ
  （criteria `.disclaimer` / profile_edit `.editing-note` / improvement `.ind-picker`）
- `final_palette_result.json`（採用値の控え）をリポジトリに追加

### 第21セッションで直した他の不具合（すべてpush・デプロイ済み）

配色の前に、自動化と印刷まわりの不具合を5件直している。**同種の見落としを防ぐため、
「動いているように見えて動いていない」type の罠を以下に残す。**

1. **週次更新ルーチンが5週間ぶん空回りしていた**（`25b7a83`）
   トリガーは7/19以降きちんと毎週起動していたが、**push権限が無く403で弾かれ、
   成果は一度も本番に届いていなかった**。`git log --grep="週次自動更新"` が0件で確定。
   原因は org `subsidy-grant` に **Claude の GitHub App が入っていなかった**こと
   （権限不足ではなく未インストール）。本人がGitHubで追加して解消、実地でpush成功を確認。
   確認は `gh api orgs/subsidy-grant/installations`（**PowerShellで実行**。Git Bashだと
   パス変換で失敗する）。この間、本番には誤った採択率が載り続けていた
2. **ルーチンのプロンプトを全面改訂**（trig_01Vbn8wKa1hdiWyr1DS1ciYo）
   5つの欠陥を修正。とくに **go.jp が WebFetch でブロックされるため、ルーチンが
   業者ブログだけを根拠に本番を書き換えていた**。curl に User-Agent を付ければ
   取得できることを実測し、その手順をプロンプトに明記した
3. **リンク確認が様式1017件を素通りしていた**（`a01c06f`, `8737b24`）
   `PROGRAM_DOCS` が `program_docs_data.js` へ分離されたのにチェッカーが index.html を
   見続けており、`ValueError` で落ちて**前回の結果を貼り直していた**。直したら本物の
   404が3件出てきたので現行URLへ差し替え済み
4. **事業計画書の印刷が全ページ不揃いだった**（`af986e5`）
   印刷CSSが `height:auto` で、高さが261〜309mmとばらつき（実測47.5mm）。
   左の青い棒が高さに追従するため長さも変わっていた。`height:280mm` に固定し、
   はみ出しは印刷時のみ余白を詰めて吸収（**文章は一切削っていない**）。全60本で
   ばらつき0.00mm・はみ出し0件
5. **制度詳細の印刷が白紙だった**（`1290d5c`）
   `body > *:not(#programArticle)` と書いていたが `#programArticle` は `<main>` の中に
   あり body 直下ではないため、**`<main>` ごと消えていた**。8制度で0文字→449〜825文字に回復

---

## 2026-08-17 第20セッション、専門家相談機能の新規実装（すべてpush・デプロイ済み）

**次の安全なアクションは、本人からの次の指示を待つこと。** 積み残しの技術タスクはない。
本セッションでは専門家相談機能をゼロから実装し、9コミットをpush・デプロイ済み
（`7ba3309`〜`b9f91e7`）。**05:11時点のローカルHEADとorigin/mainはともに `b9f91e7` で一致、
作業ツリーはクリーン。**

本人が明示的に「まだ作らない」と判断したものが2つある（下記「未着手の範囲」参照）。
これらを気を利かせて実装しないこと。

### 再開者が仮定してはいけないこと（第20セッション分を先頭に追加）

- **「相談機能に載っている専門家2人は実在する」と仮定しないこと（最重要）。**
  `experts_data.js` の `experts` 配列にある社労士・診断士の2件は**動作確認用のサンプルで
  実在しない**。連絡先も届かないダミー（`sample-sr@example.com` / `03-0000-0000` 等）。
  提携先が決まったらこの2件を実データで置き換える。ファイル冒頭に注意書きあり。
  **提携先はまだ1人も決まっていない**
- **「相談ページの送信ボタンは実際に送信する」と仮定しないこと。** 「LINEで送信」
  「メールで送信」は**動きだけのダミー**。押すと送信先選択→確認→「送信しました
  （画面上の動作確認のみ）」まで進むが、何も送られない。本物の連絡手段は専門家カード内の
  「LINEで相談する / メールで相談する / 電話する」（mailto: と LINE ディープリンク）。
  本番化するときは `consult.js` の `doSend()` を差し替える
- **「consult.js は company_data.js の API を使っている」と仮定しないこと。**
  `KOBAN_COMPANY.getCurrent()` は未登録端末に空の「会社1」を書き込む副作用がある
  （旧形式からの移行処理を兼ねているため）。相談パネルを開いただけで書き込みたくないので、
  **consult.js は localStorage を直接読んでいる**（キー `koban_companies` /
  `koban_currentCompanyId`）。**company_data.js のキー名を変えたら consult.js も直すこと**
- **「申請進捗ページは消した」と仮定しないこと。** 2026-08-17に**タブバーから外しただけ**で、
  `applications.html` は存在し機能する。`documents.html`（様式準備の完了時）と
  `profile_status.html` から個別にリンクしている。タブバーは5枠固定で、外した枠に
  「相談」を入れた

- **「ものづくり補助金は現役の制度」と仮定しないこと（重要）。** 第23次で新規公募が終了して
  いる。旧サイト（portal.monodukuri-hojo.jp）に終了告知が出ていないため見落としやすいが、
  後継制度の公募要領の対象外事業者リストに「ものづくり・商業・サービス生産性向上促進補助金
  **（1〜23次締切）**」と範囲を閉じて記載されている。**私は本セッション中に一度
  「終了と断定する根拠はない」と誤った報告をして訂正している**（旧サイトだけを見て
  判断したため）。同じ轍を踏まないこと
- **「中小企業新事業進出補助金は現役の制度」と仮定しないこと。** 公式サイトに
  「公募は終了いたしました」と明記されている（2026-08-17時点）。後継は
  「新事業進出・ものづくり商業サービス補助金」で、これは**旧2制度とは別制度**
  （公式が「とは異なる補助金です」と明記）
- **「後継制度は旧ものづくり補助金と同じように使える」と仮定しないこと。** 革新的新製品・
  サービス枠は公募要領に「既存の製品・サービスの生産等のプロセスについて改善・向上を図る
  事業は補助対象外」「単に機械装置・システム等を導入するにとどまり、新製品・新サービスの
  開発を伴わないものは該当しません」と明記。**美容室が設備を入れて生産性を上げる従来型の
  使い方はこの枠ではできない**
- **「criteria.html の表は横スクロールする」と仮定しないこと。** 640px以下では
  `table.stackable` により1行＝1カードの縦積みに組み替わる。`table` の
  `min-width: 680px` はPC用で、モバイルでは `min-width: 0` に上書きされる。
  各 `<td>` の `data-label` が `::before` で見出しとして表示される仕組みなので、
  **表に列を足すときは `data-label` も必ず付けること**
- **「viewport-fit=cover は全ページにある」と仮定しないこと（新規ページ作成時）。**
  タブバーを表示するページでこれが無いと、iOS実機でタブバーがホームバー領域に食い込み
  「スクロールすると沈んで戻らない」症状が出る。**Chromiumでは再現しない**（safe-area
  insetが常に0のため）ので、PCブラウザの確認だけでは気づけない
- **「加点は多いほど採択率が上がる」と仮定しないこと。** 公式データでは3個までが急伸し
  4個以降は頭打ち。かつ公表は第16次で更新停止しており、直近回や後継制度には当てはまらない

---

## 2026-08-17（第20セッション）専門家相談機能の新規実装

### 1. 現在の目標

「制度を知る」で止まっていたサイトに「活用する」への橋を架けること。制度詳細に
「専門家に相談」ボタンを置き、提携専門家（社労士・診断士等）へ1タップで相談依頼を
送れるようにする、という本人の要望から開始。セッション途中で本人から追加指示が
7回入り、そのつど実装・検証・pushを繰り返した。

### 2. 変更点（すべてコミット・push・デプロイ確認済み）

**新規ファイル4つ**
- `experts_data.js` — 専門家データ。`window.KOBAN_EXPERTS`（status / experts / demo）と、
  相談テーマ定義 `KOBAN_CONSULT_TOPICS`（6テーマ、各テーマに対応資格と準備物）、
  段階定義 `KOBAN_CONSULT_STAGES`（4段階）
- `consult.js` — 相談パネルの自己完結モジュール。`app_tabbar.js` と同じくCSS・マークアップとも
  JSが注入する。`window.KOBAN_CONSULT.mount(container, {programKey, programName, kind, topic})`
- `consult.html` — 相談専用ページ（タブバーの「相談」の遷移先）
- `LEGAL_NOTES.md` — 法的方針の恒久メモ（後述）

**変更ファイル4つ**
- `program.html` — `.pt-actions` に「専門家に相談する」ボタン、`#consultPanel`、script2本、
  render()末尾で `currentProgramKey`/`currentProgram` を保持、印刷除外に `#consultPanel` 追加
- `criteria.html` — 297行目付近「社会保険労務士にご相談ください」をリンク化し、
  `kind:'josei'` でパネルを開く
- `app_tabbar.js` — 「申請進捗」を外し「相談」を右から2番目に追加（5枠維持）。
  吹き出しアイコン `ICON_CHAT` を新設
- `sw.js` — v8→v19。CORE_ASSETS に `consult.html`/`consult.js`/`experts_data.js` を追加。
  `experts_data.js` は FRESH_FIRST に入れた（提携決定後の差し替えを push だけで反映するため）

**実装した機能**
- 相談導線3か所：制度詳細（program.html）／採択基準（criteria.html）／タブの「相談」
- 3問アンケートを1問ずつ表示（Q1相談テーマ＝複数選択・必須、Q2段階＝単一・任意、
  Q3連絡方法＝LINE/メール/電話の複数選択・任意）。答えた問いは畳んで上に残り「変更」で戻れる
- 回答の使い道3つ：(a)専門家の絞り込み (b)メッセージ本文の生成 (c)準備物の案内
- 会社情報（profile.html登録分）から連絡先・事業概要を本文に自動挿入
- メッセージ本文は固定表示（全文が一目で見える）＋「内容を編集する」で textarea に切替
- 送信ボタン（ダミー）：送信先選択→確認→完了表示。破線枠＋「準備中」バッジで仮と分かる見た目

**外部状態の変更**
- なし（DB・API・課金なし。localStorage への書き込みもしていない）

### 3. 検証済みの証拠

すべて Playwright（実ブラウザ Chromium）で確認。**05:11時点で本番URLに反映済みを確認**。

| 確認したこと | 方法・結果 | 時刻 |
|---|---|---|
| 本番のキャッシュ名 | `curl https://subsidy-grant.github.io/koban-roadmap/sw.js` → `koban-roadmap-v19` | 05:11 |
| 本番のconsult.html | curl → HTTP 200 | 05:11 |
| 最新デプロイ | `gh run list` → conclusion:success, headSha:`b9f91e7`（19:58Z作成） | 05:11 |
| ローカル/リモート同期 | `git rev-parse HEAD` と `origin/main` がともに `b9f91e7` | 05:11 |
| 作業ツリー | `git status --short` が空 | 05:11 |
| 専門家の絞り込み | career(josei)×wage→社労士のみ／ai(hojo)×plan→診断士のみ／助成金×診断士向けテーマ→社労士（種別を維持したまま広げる） | 実装時 |
| ひな形の生成 | 3起動元（相談ページ／制度詳細／採択基準）で本文を出力しファイルに保存して目視 | 実装時 |
| 会社情報の自動挿入 | localStorageに投入→再読込→本文に反映を確認。未登録時は空欄書式＋登録リンク | 実装時 |
| localStorageへの書き込み | 未登録端末で相談パネルを開いた後 `koban_companies` が null のままであることを確認 | 実装時 |
| 送信フロー | LINE/メール両方で 選択→確認→完了 を通し、候補が1人なら自動選択・2人なら未選択を確認 | 実装時 |
| 全ページ回帰 | 8ページ（index/documents/improvement/consult/profile/criteria/program/applications）でJSエラー0、タブ5枚 | 実装時 |
| モバイル | 375px幅で横あふれ0、タップ領域44px以上、本文の見切れなし（scrollHeight==clientHeight） | 実装時 |
| 印刷 | `emulate_media(print)` で `#consultPanel` と `.pt-actions` が display:none | 実装時 |

### 4. 一次情報の取り直しコマンド

```sh
# 本番に何が出ているか（キャッシュ名・機能の有無）
curl -s https://subsidy-grant.github.io/koban-roadmap/sw.js | grep -o "koban-roadmap-v[0-9]*"
curl -s https://subsidy-grant.github.io/koban-roadmap/experts_data.js | grep -c "sample-sharoushi"

# デプロイ状況（ローカルのコミットと headSha が一致しているかを見る）
gh run list --repo subsidy-grant/koban-roadmap --limit 3 --json status,conclusion,headSha,createdAt

# ローカルとリモートのズレ
git -C /d/Claudecode/koban-roadmap rev-parse HEAD origin/main

# 相談機能を実ブラウザで触る（サンプル専門家は expertdemo なしでも出る）
cd /d/Claudecode/koban-roadmap && python -m http.server 8123
# → http://localhost:8123/consult.html
```

### 5. 未着手の範囲（本人が明示的に「やらない」と判断したもの）

- **メール送信基盤（1ボタンで実送信）** — 調査だけ実施。**本人が「送信基盤はまだ入れません」と
  明言**。推奨案は Google Apps Script を中継役にし、宛先アドレスではなく専門家ID
  （`sr-001` 等）を送る方式（GAS側の対応表に無いIDは拒否＝踏み台化しない）。
  Netlify Forms はサイト移転が必要、Vercel Hobby は非商用限定で規約違反、Resend は
  `github.io` でドメイン認証不可、MailChannels/SendGrid は無料枠終了、というのが脱落理由
- **専門家の登録・管理画面** — **本人が「まだ作らない」と明言**。当面は
  `experts_data.js` を直接編集する運用。静的サイトなので管理画面から本番に保存する仕組みは
  作れず、作るなら「入力補助ツール（コードを生成してコピー）」が現実的、という整理まで
- **サイト内メッセージ機能** — 設計案の資料のみ作成（実装なし）。
  https://claude.ai/code/artifact/0e9ce86a-fe40-441f-8654-568015ec786a
- **index.html への相談導線** — タブバーから行けるので今回は触っていない
- **joseikin-navi への横展開** — そもそも joseikin-navi 自体を削除したので該当なし

### 6. 不確実な点・リスク

- **サンプル専門家2件が本番に出ている。** 「（サンプル・実在しません）」と明記し連絡先も
  ダミーだが、**一般利用者が見て混乱する可能性はある**。本人の指示で入れたもの。
  提携先が決まるまでの暫定状態であることを忘れないこと
- **送信ボタンがダミーのまま本番にある。** 「準備中」バッジと破線枠、完了画面の
  「実際にはまだ送信されません」で示しているが、押した人が送れたと誤解するリスクはゼロではない
- **GASのCORS挙動は未検証。** 調査エージェントの実測（`script.google.com` は
  `Access-Control-Allow-Origin` を返さないので `Content-Type: text/plain` にして
  単純リクエストにする）はGoogle公式ドキュメントに記載がない。実装時に実ブラウザで要検証
- **送信基盤の費用・無料枠は着手時に再確認が必要。** 調査は 2026-08-17 時点のもので、
  各社の料金・無料枠は変わる
- **`--sage-ink` は program.html と consult.html に未定義。** consult.js は
  `var(--sage-ink, var(--ink))` とフォールバック付きで参照しているので動くが、
  criteria.html と index.html にしか定義がないことは把握しておくこと

### 7. 触ってはいけない領域

- **`page_data.js` / `sim_data.js` は生成物。** 手編集しない。制度データを直すときは
  `index.html` の PROGRAMS を直して `_tools/build_page_data.py` を流す
- **`trig_01Vbn8wKa1hdiWyr1DS1ciYo`（koban-roadmap 週次自動更新）は enabled:true で稼働中。**
  05:11時点で `last_fired_at` 2026-08-16T13:01Z、`next_run_at` 2026-08-23T13:00Z。
  今回のセッションでは触っていない。止めないこと
- **`applications.html` を削除しないこと。** タブから外れただけで現役
- **提携が決まっていない専門家を `experts` に実名で書かないこと。** `LEGAL_NOTES.md` の方針

### 8. 最後の判断・関門

- 直前の判断：本人の「また相談します」でセッションを閉じる流れだったが、`/handoff` の
  指示を受けて引き継ぎを作成した
- 本人確認待ちの事項：**なし**。未着手3件はいずれも本人が明示的に「やらない」と判断済みで、
  待ちの状態ではない
- 本セッションで削除したもの（第19セッションからの変更）：
  **`D:\Claudecode\joseikin-navi` と `D:\Claudecode\hojosei-concierge` をフォルダごと削除**
  （本人指示）。両方とも未コミット変更なしを確認してから削除。hojosei-concierge の
  `.dev.vars` に実際のAnthropic APIキーがあったが `.gitignore` 対象でgit履歴にも含まれず、
  フォルダ削除でローカルからは消滅。**GitHub側にリポジトリが残っている場合の secrets は未確認**。
  joseikin-navi の週次トリガー `trig_01NR6Zudgzk6v1wzC5wPDZza` は削除前から enabled:false
  だったものを再度 disable 済み（05:11時点で enabled:false を確認）

---

## 2026-08-17（第19セッション）

### 1. 現在の目標

セッション開始時の目標は第18セッションからの持ち越し「⑦ 美容業への `ai`/`jizoku` 割り当て」
の組み直し。これを完了後、本人から採択基準ページ（criteria.html）に関する指示が4回に分かれて
連続した：(a) 戻るボタン・カード枠線・トップページの02削除、(b) 採択基準ページの導線改善と
スマホでの表の横スクロール解消、(c) 内容の具体化（採択率などの参考値追加）、(d) タブバーの
不具合修正・目次追加・数字への見出し追加。最後に前セッションからの申し送り3点を消化した。

### 2. 変更点（すべてコミット・push・デプロイ確認済み）

**コミット `e743144`**（improvement/beauty/ 13ファイル）：**⑦の組み直し**
- 第18セッションで診断士に却下された「補助金の上限に投資額を合わせる」逆算をやめ、
  経営上必要な投資規模を先に決めてから合う制度を当てる順序で全10案を再設計
- 制度分布：省力化4件・業務改善6件 → **持続化3件・AI導入3件・業務改善2件・自己資金2件**
- `schemeInfo()` に「自己資金」パターンを新設（補助金0円・上限なし）。no2(AI電話予約)と
  no10(リピート予測AI)はどの制度にも無理なく当てはまらないため自己資金推奨とした（本人選択）
- 持続化補助金は上限50万円のため、投資額が上限を超える案（no1/4/5）は**実質補助率**
  （45.5%等）を表示する分岐を追加。制度上正しい頭打ちであってこじつけではない
- v1/v2両方のP6(資金計画)・P10(要件チェック・転用メモ)を5制度対応に拡張

**コミット `ee3d90f`**（86ファイル）：**戻るボタン・枠線・トップページ02削除**
- 事業計画書とプロトタイプの「← 探す に戻る」を「← 戻る」に変更し、戻り先を
  `index.html` から `improvement.html` へ。生成元3本（build_plans.py / build_protos.py /
  _build_plans_v2.js）と手書きの美容業プロトタイプ10本を直し、全業種を再生成（計83ファイル）
- improvement.html のカード内リンクに1.5pxの枠線を追加（青＝事業計画書 / 緑＝プロトタイプ）
- index.html の「02 業種別・改善計画 厳選10選」を削除し「採択基準〜」を02へ繰り上げ。
  空いた縦を埋めるためヒーロー（見出し37px→46.75px、写真、高さ）とセクションカード
  （min-height 56px→74.8px）を拡大。iPhone SE相当（max-height:700px）は従来寸法へ戻す上書き
- **併せて `build_protos.py` の既存バグを修正**。引数なし実行で `proto` 定義の無い業種
  （education等）が `KeyError` になり、中途半端なファイルを1つ残して落ちていた。
  `proto` を持つプランだけを対象にする安全弁を入れた

**コミット `726c208`**（criteria.html, index.html）：**導線改善と表の縦積み化**
- index.html の02セクションで、リンクを説明文より先に出すよう順番を入替
- そのリンクを大きなボタン（`a.sec-cta`、青ベタ塗り＋白文字）に。カード地が
  `--accent-wash`（薄い青）のため青文字リンクだと埋もれる問題への対応
- criteria.html の「← ロードマップ本体へ戻る」を削除、「このページの性質について」を
  冒頭から出典一覧の後ろへ移動
- **3つの表をスマホで横スクロール不要に**。各 `<td>` に `data-label` を付与（75セル）し、
  640px以下で1行＝1カードの縦積みに変換。`thead` は視覚的に隠しつつDOMには残す

**コミット `082da6f`**（criteria.html）：**採択率の追加**
- 競争審査型8制度の直近回の採択率を公式の採択結果発表から取得（省力化61.5% /
  デジタル化AI 51.5% / 持続化通常47.2% / 持続化創業41.2% / ものづくり35.1% /
  新事業進出34.9% / 空き家対策30.8% / 東京都創業17.1%）
- 事務局が採択率そのものを公表している値と、件数から当方算出した値（†）を画面上で区別
- **デジタル化・AI導入補助金の分類を「要件充足型」→「競争審査型」に訂正**
  （採択率が公表されている以上、誤分類だった）
- 観光庁の制度は公表311施設が「承諾を得た先のみ」で実数の下限のため「算出不可」と明記

**コミット `62dff42`**（criteria.html）：**誤り2件の訂正と返還事例の実数追加**
- **【誤り1・実害大】飲食業の制度の対象者を取り違えていた。** 公募要領に「補助事業者は、
  飲食事業者**に対する**専門家による伴走支援を行う」とあり、飲食店が設備投資の補助を
  受ける制度ではなく、**飲食店を支援する側の団体を募集する事業**だった。正式名称も
  「飲食業労働生産性向上支援補助金」ではなく**「飲食業労働生産性向上推進事業」**。
  審査基準も非公表ではなく3分類7項目が要領に明記されており「推測される」自体が誤りだった
- **【誤り2】加点の記述が事実と逆だった。** 「4個以上で採択率が大きく上がる」→ 公式データ
  では3個までが急伸し4個以降は頭打ち。ただし個数別の数値はグラフ画像内にあり読み取れない
  ため**数値は載せず**、確認できた事実（16次で更新停止・直近回に当てはまらない・
  単純集計であり因果ではない）のみ記載
- 東京都デジタル導入促進補助事業の「予算先着的」を訂正。要領に「到着した順に内容を確認し、
  不備・不足が無いことを確認できたものから審査します（申請書類の到着順ではありません）」
  「予算額に達した時点で募集を締め切る場合があります」とあり、**早く出すこと自体は有利に
  ならないが不備があると後回しになる**という構造
- 返還基準に「実際に指摘された件数・金額」の表を新設（雇用調整助成金4,640件・約1,153億円、
  人材開発支援助成金191事業所・約20億円ほか計7制度）

**コミット `4fa4421`**（criteria.html, program.html）：**タブバー不具合・目次・数字の見出し**
- **タブバーがスクロールで沈んで戻らない不具合を修正。** 原因は criteria.html に
  `viewport-fit=cover` が無かったこと。`env(safe-area-inset-bottom)` はこの指定が無いと
  常に0扱いになる。**2026-08-16のコミット `244599c` が同じ不具合を6ページで修正済み
  だったが criteria.html が漏れていた同型の取りこぼし。**影響範囲を棚卸ししたところ
  `program.html` も同じ状態だったため併せて修正し、タブバーを表示する9ページすべてが
  対処済みになったことを確認
- criteria.html に目次（`nav.toc`）を追加。h2の6項目＋h3を1つ、PC/タブレットで2列・
  スマホで1列。各見出しに id を付与
- stat-grid の数字に見出し（`.k`）を追加（採択率の幅 / 最も通りやすい制度 / 最も狭き門 /
  最大の不正受給事案）。区画にも `h2#numbers`「数字で見る採択と返還」を付けた

**コミット `7081e9f`**（criteria.html）：**申し送り3点の消化**
- **ものづくり補助金と中小企業新事業進出補助金を「公募終了」表示に変更**（`.pill.closed`）。
  説明文の冒頭に「**この制度に新規申請はできません**」を太字で明記。採択率は実績データ
  として残した
- **後継制度「新事業進出・ものづくり商業サービス補助金」の行を新設。** 第1回は採択発表前
  （2026年12月〜2027年1月予定）のため採択率は空欄。革新的新製品・サービス枠が
  「既存プロセスの改善は対象外」である点を太字で明記
- **賃上げ加点の落とし穴を `.caution` ボックスで警告。** 加点を受けて未達だと未達報告から
  **18か月間、中小企業庁所管の他制度（IT導入・持続化・省力化投資等9制度）でも大幅減点**。
  さらに賃上げは加点とは別に基本要件でもあり、未達だと**返還義務**が生じる
- **補助金の課税について `.caution` ボックスを追加。** 事務局の公式文書に「技術導入費、
  専門家経費等の『経費を補填するための補助金』については…圧縮記帳等の適用は認められません」
  とあり、設備以外に充てた分は課税される
- **併せて既存のコントラスト不足を修正。** `.pill.competitive` / `.requirement` が
  ライトで3.62:1 / 3.76:1とAAを割っていたため、`--rust-ink` / `--sage-ink` を4系統に
  追加して差し替え、**5.29:1 / 5.66:1** に改善（今回の変更が原因ではない既存の問題）

### 3. 検証済みの証拠

すべてPlaywright（headless Chromium、`service_workers="block"`）による実ブラウザ検証。
**2026-08-16〜17のセッション中に取得。**

- **⑦の組み直し**：全10案でコンソールエラー0件、投資内訳合計＝投資総額の一致、自己資金案の
  補助金行非表示、持続化案の実質補助率表示（no1=45.5%、no5=41.7%）を確認。
  採択率8件は件数から検算し記載値と一致
- **戻るボタン等**：viewport metaの実DOM確認、目次リンク7本×3幅＝21回のクリック遷移が
  すべて見出しに着地、iPhone SE(375x667)で縦横とも溢れ0px、枠線80本の色分け、
  戻るリンク7ファイルのhrefとクリック遷移（HTTP 200）
- **表の縦積み**：375x667・390x844で4表とも横スクロールなし（`.twrap` の+7pxは縦
  スクロールバー分で、`scrollLeft`を動かしても実際のスクロール量0pxを追試で確認）。
  PC(1280x800)では差分0pxで従来の表形式を維持
- **コントラスト**：`.caution-ttl` light 6.48:1 / dark 6.44:1、`.pill.closed` 9.00:1 / 6.75:1、
  修正後の `.pill.competitive` 5.29:1、`.requirement` 5.66:1（**2026-08-17 01:5x時点に
  実ブラウザで再実測**）、目次の第1階層 16.46:1 / 13.15:1。すべてWCAG AA達成
- **外部リンク**：criteria.html の全42本が到達可能。うち6本（gov-online.go.jp、
  chusho.meti.go.jp 2本、hojyokin-portal.jp 3本）はAPIリクエストでは403だが、
  実ブラウザ（UA付き）では200で正しい本文が返ることを確認。**WAFによる自動化
  クライアント遮断であってリンク切れではない**
- **一次情報の原文確認**：農水省の公募要領PDF（補助事業者の定義・審査基準3分類7項目）、
  東京都の募集要項PDF（到着順ではない旨・予算枠・審査の視点3点）、後継制度の公募要領
  1.2版 全61ページ（「1〜23次締切」の閉じた記載・革新的新製品枠の対象外規定・18か月の
  減点条項・締切10/30）、厚労省PDF（191事業所・約20億円・返還済149/未返還42）を
  いずれも**pypdfで本文抽出して原文を確認**
- **2026-08-17 01:55時点で一次情報を再取得**：`git status` で作業ツリークリーン、
  未push 0件・未pull 0件、`gh run list` で `7081e9f` のデプロイが `success`
  （2026-08-16T16:45:15Z完了）、`python3 _tools/check_sim_data.py` が
  「誤り0件・注意0件・ALL OK」（制度121件）
- **同時刻の現況確認**：「推測される」の残存0件、公募終了バッジ2件、`.caution` 2件、
  タブバー表示ページでの `viewport-fit=cover` 欠落0件
- ローカルサーバーは各検証後に停止し、`netstat` で残存なしを確認済み

### 4. 一次情報の取り直しコマンド

```bash
cd /d/Claudecode/koban-roadmap
git status
git fetch origin main && git log HEAD..origin/main --oneline   # 空なら同期済み
git log origin/main..HEAD --oneline                             # 空ならpush不要
gh run list --limit 3 --json status,conclusion,createdAt,headSha
python3 _tools/check_sim_data.py                                # sim_data.jsの機械チェック

# 本セッションの変更点の現況確認
grep -c "推測される" criteria.html                    # 0 のはず
grep -c "pill closed" criteria.html                   # 2 のはず（公募終了の2制度）
grep -c 'class="caution"' criteria.html               # 2 のはず（賃上げ加点・税務）
grep -o 'name="viewport" content="[^"]*"' criteria.html program.html   # viewport-fit=cover があること

# タブバーを表示する全ページで viewport-fit の欠落がないか棚卸し
for f in $(grep -rl '<script src="app_tabbar.js"' --include="*.html" .); do
  grep -q "viewport-fit=cover" "$f" || echo "欠落: $f"
done

# 美容業の計画書を作り直す（順番を守ること）
cd improvement/beauty && node _build_plans.js && node _build_plans_v2.js
#   ↑ v1だけ実行すると旧版に退行する。必ずv2まで通すこと

# 他業種の生成物（build_protos.py は引数なしでも安全になった）
python3 improvement/_build/build_hub.py      # 改善計画ページ
python3 improvement/_build/build_plans.py    # 他5業種の計画書
python3 improvement/_build/build_protos.py   # プロトタイプ（proto定義のある業種のみ）
```

制度が現役かどうかを疑うときは、**旧サイトの告知だけを見ないこと**。後継制度の公募要領の
「補助対象外となる事業者」欄に旧制度が過去形で列挙されていないかを確認するのが確実。

### 5. 未着手の範囲

- **他ページの制度掲載の現況確認**。本セッションで criteria.html の16制度は全て現況を
  確認したが、`index.html` / `program.html` / `sim_data.js` に載っている**128制度**に
  同種の「実は公募終了していた」制度が混じっていないかは**未確認**。ものづくり補助金と
  新事業進出補助金がサイト全体でどう扱われているかも見ていない（criteria.html しか直していない）
- **既存の `.pill` 以外のコントラスト監査**。今回 criteria.html のバッジは直したが、
  他ページの同種要素（index.html の `pill` / `elig-badge`、program.html の `pill-row` 等）は
  未確認
- 第16セッションから持ち越しのiPhone SE（375×667）で documents.html +12px・
  profile.html +32px 溢れる件（本人が「ここで止める」と判断済みの既知事項）
- 制度データそのものに関する積み残し（gunma_takasaki_lease、都道府県横展開、
  国の新設制度網羅調査等）

### 6. 不確実な点・リスク

- **ものづくり補助金の「終了」を明言した公式文書は発見できていない。** 判断は
  ①後継制度公募要領の「1〜23次締切」という閉じた記載、②令和8年度予算での再編、
  ③24次公募の不存在、の3点からの推認。旧サイトには終了告知が出ていない（2026-08-17時点）。
  **もし24次が公募された場合、現在の「公募終了」表示は誤りになる**
- **後継制度の締切に公式サイト内で食い違いがある。** スケジュール欄に9月30日、
  公募要領1.2版の表紙に10月30日。**要領を正**として10/30を記載し、その旨を注記したが、
  事務局への確認が望ましい
- **後継制度の加点項目の申請数上限は「規定が無い」ことしか確認できていない。** 61ページ
  全文を検索して上限の記述が無いことを確認したが、電子申請システム側で制限される可能性は
  否定できない。サイトには「公募要領に規定なし」と書いてある
- **加点個数別の採択率の具体的な数値は未取得。** 事務局データポータルにグラフはあるが
  画像内の数値で読み取れず、**数値は一切載せていない**。診断士エージェントは
  「16次：0個33.4%／1個43.3%…」と報告してきたが、**私は一次情報で確認できていないため
  採用しなかった**。載せる場合は画像を実際に読む必要がある
- **タブバー修正はiOS実機でしか検証できない。** 本人が2026-08-17に「直っていました」と
  確認済みだが、Chromiumでは再現しないため自動検証では担保できない
- **採択率は公募回ごとに変わる。** 記載値はすべて2026-08-16〜17時点で公表されていた
  直近回のもの。新しい回の結果が出れば陳腐化する
- **令和8年度の東京都最低賃金1,280円は答申段階**（前セッションからの継続事項。
  官報公示前・発効日未定）

### 7. 触ってはいけない領域

第6セッション「7. 触ってはいけない領域」および第17〜18セッション追記分に加えて、**本セッションで追加**：

- **`criteria.html` の表に列を追加するときは、必ず各 `<td>` に `data-label` を付けること。**
  640px以下では `thead` を視覚的に隠し `td::before { content: attr(data-label) }` で
  見出しを出す設計のため、`data-label` が無い列はスマホで見出しなしのセルになる
- **`.pill.competitive` / `.requirement` の文字色に `--rust` / `--sage` を直接使わないこと。**
  同系のwash地の上ではライトでAAを割る（3.62:1 / 3.76:1）。wash地に載せる用の
  `--rust-ink` / `--sage-ink` を使うこと（本セッションで criteria.html の4系統に追加済み）
- **タブバーを表示する新規ページを作るときは `viewport-fit=cover` を必ず入れること。**
  無いとiOS実機でタブバーが沈む。Chromiumでは再現しないため気づけない
- **`improvement/_build/build_protos.py` を引数なしで実行しても安全になったが、
  プロトタイプがあるのは beauty と food のみ**（beauty は手書き、food は生成物）。
  他4業種の JSON には `proto` キーが無い

### 8. 最後の判断・関門

- ⑦の再設計で、no2（AI電話予約）とno10（リピート予測AI）を「自己資金推奨」とする方針を
  本人が選択（無理な制度当てはめを避けるため）
- 持続化補助金で上限に張り付く案の表示を「実質補助率を表示」で本人が選択
  （投資額を上限に合わせて削る案は却下）
- 内容具体化の範囲を「主要制度の採択率を優先」で本人が選択
- 目次がPCで3列になっていたのを、7項目では読みにくいと判断して2列に変更（自己判断）
- 既存バッジのコントラスト不足は指示範囲外だったが、`--rust-ink` を追加した直後で
  軽微に直せるため併せて対応（自己判断）
- **本セッションで私が1回、誤った報告をして訂正している**：「ものづくり補助金が終了と
  断定する根拠はない」と報告したが、旧サイトの告知だけを見ての判断で、後継制度の公募要領を
  確認していなかった。実際は第23次で終了していた
- **本人確認待ちの事項：なし**（タブバーの修正は本人が実機で確認済み）

---

## 次の安全なアクション（2026-08-16 第18セッション時点の記録、アイコン案9適用＋試算の誤り訂正。すべてpush・デプロイ済み）

**次の安全なアクションは、本人からの次の指示を待つこと。** 積み残しの技術タスクはないが、
⑦（美容業への `ai`/`jizoku` 割り当て）だけは**診断士の判定で却下されたまま**残っている。
再開するなら「5. 未着手の範囲」の手順から始めること（調査は済んでいるので調査からやり直す必要はない）。

### 再開者が仮定してはいけないこと

- **「省力化投資補助金の試算は一般型しか計算できない」と仮定しないこと。** 本セッションで
  カタログ注文型を選ぶ質問（`ruikei`）を新設した。`sim_data.js` の `calc.cap.values` は
  `general|e5|no` / `catalog|e5|no` というキー体系になっている（従来は `e5|no`）
- **「`_build_plans.js` を実行すれば美容業の計画書が再現できる」と仮定しないこと（重要）。**
  第17セッション以前は生成物に手修正が入っていて、実行すると消える状態だった。本セッションで
  手修正をスクリプトへ取り込み、**いまは v1→v2 の順に実行すれば再現できる**。ただし
  **必ず `node _build_plans.js` の後に `node _build_plans_v2.js` を実行すること**。v1だけ実行すると
  旧版のHTMLに退行する（v1は `_plan_data.json` の生成元で、現行HTMLはv2が作る）
- **「美容業の事業場内最低賃金は1,180円」と仮定しないこと。** 1,230円に変更し `WAGE_BEFORE` に
  定数化した。1,180円は東京都の現行最賃1,226円を下回るうえ、70円上げても令和8年度の
  見込額1,280円に届かない値だった
- **「業務改善助成金は60円コース」と仮定しないこと。** 60円コースは存在しない（50/70/90円の3つ）。
  70円コースに統一済み
- **「改善計画カードのリンクは絵文字」と仮定しないこと。** 34pxの角丸アイコン＋インラインSVGの
  縦並び（本人選定の案9・説明文なし）に変更済み
- **「`capManByTier` が生きている」と仮定しないこと。** `calc` から引けるようになったので
  `capKeyByTier` に置き換えた。`index.html` の `simTrackFromTop()` 系は2026-08-13の
  シミュレーター廃止（`7acdd26`）以降デッドコードのまま（本セッションでは削除していない）

---

## 2026-08-16（第18セッション）

### 1. 現在の目標

第17セッションからの持ち越し2件（⑤アイコン見本の選定と適用、⑦美容業の対象制度調査）に着手した。
⑤は完了。⑦は調査の過程で**別の実害のある誤りが見つかり、そちらの修正が本題になった**。

### 2. 変更点（すべてコミット・push・デプロイ確認済み）

**コミット `9fdb9cd`**（improvement.html, improvement/_build/build_hub.py）：
- 改善計画カードのリンクを絵文字（📄🖥）から案9「大きめアイコン」に変更。34pxの角丸アイコン＋
  インラインSVG、縦2本並べて行全体を押せる形。説明文は本人指示により無し
- 青＝事業計画書 / 緑＝プロトタイプで役割を色分け
- **同じリンク生成コードが external(beauty)用と plans(他5業種)用の2箇所に重複していたので
  `plan_links_html()` に集約した**。この2箇所の食い違いが第17セッションの「写真60枚が
  一度も表示されない」バグの原因だったため、再発を構造的に防ぐ
- プロトタイプ側のアイコン地は、`--sage` のままだとライトで4.31:1しか出ず青側(6.68:1)と
  比べて弱かったので、ライトのみ `#0b7a61` に落として5.29:1にした（`--sage` は他ページ共用の
  ため変数自体は触らず、ダークは元の値に戻している）

**コミット `7d020f6`**（sim_data.js, index.html, page_data.js, improvement/_build/build_plan_data.py,
improvement/_build/data/plans/{lodging,realestate}.json）：**本セッションで最も実害が大きかった修正**
- **省力化投資補助金の試算に「カタログ注文型」を選ぶ手段が無く、常に一般型の上限額で
  計算していた。** 従業員5人以下だと画面に750万円が出るが、カタログ注文型の正しい上限は
  200万円で**3.75倍の過大表示**だった
- 原因は2つ重なっていた：
  - `top.tracks` には両類型の定義があったが、それを読む `index.html` の `simTrackFromTop()` が
    2026-08-13の「02 概算シミュレーター」廃止（`7acdd26`）で到達不能になっていた。
    `program.html` 側には最初から類型の実装が無い
  - `capManByTier` の値自体も**2026年3月19日の制度改定前のまま**だった（5人以下 500万→200万、
    6〜20人 750万→500万）。2026-08-05に「現行版と一致」と確認した記録が残っているが、
    公式ページが改定前後の額を併記しているため**改定前の表を見た誤り**だった
- 対応：`questions` の先頭に `ruikei`（申請する類型）を追加し、`calc.cap.axes` の先頭に据えて
  `values` を `general|...` / `catalog|...` に分けた
- カタログ注文型は補助率が一律1/2なので、`rateq` の2/3の選択肢に `when`/`else` を付けて
  `chusho` へ倒す。**`rate` を `optionsOf` から `axes` 方式に変えないと `when` が効かない**
  （`simResolveRate` の `optionsOf` 分岐は `when` を見ない。解釈するのは `simAxisPart` だけ）
- `top` の `capManByTier` をやめ `capKeyByTier` にして、表示額と計算値を `calc.cap.values` の
  1か所から出すようにした
- 賃上げ特例の達成基準が類型で違う（一般型=最賃+50円、カタログ=+3.0%）ことを help に追記
- `index.html` のFAQと `build_plan_data.py` の `cap` も改定後の値に訂正（`build_plan_data.py` の
  `cap` はモデル事業者=5人以下に適用される額なので200）

**コミット `4971887`**（improvement/beauty/ 13ファイル）：
- **`plan-*.html` に生成スクリプトへ未反映の手修正が入っており、`_build_plans.js` を実行すると
  消える状態だった**（本セッションで実際に踏み、`git checkout` で復元した）。手修正を
  スクリプトに取り込み、同時に制度上の誤りを訂正した
- 訂正した誤り6件：
  1. 事業場内最低賃金 1,180円 → **1,230円**（定数 `WAGE_BEFORE` に集約）
  2. 引上げ額 60円 → **70円**（60円コースは存在しない。生成物側は既に70円だった）
  3. 省力化の引上げ後賃金 1,220円 → **1,270円**（現行1,230円より低い逆転。1180+40の名残）
  4. 省力化の補助率。文言は「小規模事業者 2/3」なのに金額は0.5で計算していた → 2/3で計算
  5. 上の修正で「補助金(1/2)」ラベルのまま2/3の額が出る不整合 → `rate` の先頭を実際の率に
  6. 業務改善の上限「最大600万円」→ **180万円**（600万は特例事業者10人以上の額で、
     従業員5名のモデルでは届かない。30人未満・70円コース・4〜6人の区分が正しい）
- 賃上げ額の分岐が3箇所にコピーされていたのを `wageUpOf()` に、散らばっていた `1180` を
  `WAGE_BEFORE` に集約（制度を増やすと直し漏れる形だったため）
- ツールバーの「← 探す に戻る」もスクリプト側に取り込んだ
- `schemeInfo()` に `ai`・`jizoku` の分岐を追加した。**ただしまだどの案にも割り当てていない**

### 3. 検証済みの証拠

すべてPlaywright（headless Chromium、`service_workers="block"`）による実ブラウザ検証。
**2026-08-16のセッション中に取得。**

- **アイコン案9**：6業種×10件のリンク構成、タップ領域51px（下限44px超）、アイコン34px・SVG 19px、
  縦並び（x差0.00px）、写真60枚が `naturalWidth=640/naturalHeight=360`、リンク遷移HTTP 200、
  横スクロールなし・コンソールエラー0件（390x844・1280x800 × light/dark）
- **コントラスト**：修正後のプロトタイプ・アイコンはライト5.29（修正前4.31）、ダーク10.23で
  AA充足。事業計画書側6.68、リンク文字13.91/14.38は変化なし
- **カタログ注文型の上限8通り**：5人 200/300万、20人 500/750万、50人 1,000/1,500万、
  100人・150人とも 1,000/1,500万。すべて期待値と一致
- **一般型10通り**：750/1,000・1,500/2,000・3,000/4,000・5,000/6,500・8,000/10,000万円で
  従来どおり（退行なし）
- **補助率の類型別動作**：カタログ注文型で小規模事業者・再生事業者・最低賃金特例を選んでも
  1/2のまま（計算行 `2,000万円 × 1/2 = 1,000万円` で数値確認。2/3なら1,333万円になるはず）。
  elseNote も画面表示を確認。一般型では2/3が効くことも確認
- **他制度の退行なし**：ai（149.9万/450万）、jizoku（50/100/200/250万）、kaizen（110/230/450万）、
  monodukuri（2,500/3,000/4,000万）で枠の切替が動作、コンソールエラー0件
- **美容業の計画書**：全10案で補助率ラベルと実額の比率が一致（3/4=0.750、2/3=0.666）。
  生成物の差分が意図した変更のみになっていることを確認
- 各コミット前に `python3 _tools/check_sim_data.py` を実行し「誤り0件・注意0件・ALL OK」
  （制度121件）を確認
- **2026-08-16 18:58時点で一次情報を再取得**：`git status` で作業ツリークリーン、
  未push 0件・未pull 0件、`gh run list` で `4971887` のデプロイが `success`
  （2026-08-16T09:56:21Z完了）
- ローカルサーバー（ポート8911〜8951）は各検証後に `taskkill` で停止し、`netstat` で
  残存なしを確認済み

### 4. 一次情報の取り直しコマンド

```bash
cd /d/Claudecode/koban-roadmap
git status
git fetch origin main && git log HEAD..origin/main --oneline  # 空なら同期済み
git log origin/main..HEAD --oneline                            # 空ならpush不要
gh run list --limit 3 --json status,conclusion,createdAt,headSha  # デプロイ確認
python3 _tools/check_sim_data.py                                # sim_data.jsの機械チェック

# 本セッションの変更点の現況確認
grep -c "ruikei" sim_data.js                                    # 類型の質問（5箇所のはず）
grep -n "catalog|e5|no" sim_data.js                             # カタログ注文型の上限キー
grep -n "WAGE_BEFORE = " improvement/beauty/_build_plans.js     # 1230 のはず
grep -c "wageUp:70" improvement/beauty/_build_plans.js          # 70円コース（5案のはず）
grep -c "<svg" improvement.html                                 # カードのアイコン（61のはず）

# 生成物の作り直し（順番を守ること）
python3 improvement/_build/build_hub.py                         # 改善計画ページ
python3 _tools/build_page_data.py                               # documents.html用データ
cd improvement/beauty && node _build_plans.js && node _build_plans_v2.js   # 美容業の計画書
#   ↑ v1だけ実行すると旧版に退行する。必ずv2まで通すこと
```

### 5. 未着手の範囲

**⑦ 美容業への `ai`/`jizoku` 割り当て（調査完了・実装は却下されたまま）**

調査で確定した事実（すべて一次情報、2026-08-16）：
- **美容業は `ai`（デジタル化・AI導入補助金2026）・`jizoku`（小規模事業者持続化補助金 第20回）
  とも対象業種**。除外業種に該当せず、`ai` の別紙2には「生活関連サービス業（生Ｐ-０６）」区分に
  「カルテ管理」が機能例として明記されている
- **サイト本体は最初から美容業を対象扱いしていた**。`ai`・`jizoku` は `index.html:6032` の
  `UNIVERSAL_PROGRAM_KEYS` に入っており、美容業の10カテゴリにも `ai`×5・`jizoku`×6 が
  割り当て済み
- **2制度しかない原因は制度の制約ではなく、外部プロジェクト（beauty-ai-factory）の設計**。
  `improvement/beauty/ideas.js` の段階で1000案すべてに「省力化」「業務改善」のどちらかが
  固定で振られており、`_build_plans.js` の `schemeInfo()` も2値しか受け付けなかった
- **ITツールの実在（実ブラウザで実測）**：予約・カルテ/CRM・在庫・シフト勤怠・会計POSは
  登録ツールが潤沢。**「多言語」「翻訳」「ホームページ」は0件、「動画編集」は1件（教育用途）のみ**
- `ai` は150万円以上の申請で**4プロセス以上が必須**。実測の結論は「単機能AIの寄せ集めで
  4プロセスを組む発想は登録ツールの実態と合わない」（一体型パッケージが軸になる）
- `jizoku` は**上限50万円**、販路開拓が主軸で業務効率化だけの計画は対象外。広報費・
  ウェブサイト関連費は各上限30万円かつ単独申請不可

**診断士による判定（却下）**：投資額を制度の上限に合わせて削る設計案（no1を180万→45万等）を
提示したところ、**「根本的に考え直すべき」と判定された**。理由：
- 補助金の上限に合わせて投資額を削るのは**因果の逆流**。no1を45万にして効果額129万を据え置くと
  ROI 0.35年になり、「補助金なしで即やるべき案件」になって計画全体の数字が信用されなくなる
- no2（AI電話予約）の140万円は「縮小」ではなく**実施不能**（連携開発78＋シナリオ46＋研修28で、
  本体168万を除いても152万円）
- no1・no4・no5を `jizoku` に付け替えるべきでない（広報費・ウェブサイト関連費の各30万円の
  内枠を見落とした設計になっている）
- **サイト掲載としても反対**。投資額が制度上限に張り付いた案が並ぶと、読者が
  「上限に合わせて計画を作るもの」と学習する（審査で最も嫌われる作り方）

**次に再開するときの手順（診断士の推奨）**：制度の付け替えをやめ、**逆順で組み直す**。
1. 各案の事業内容と投資額を、経営上必要な規模で確定する（補助金を見ない）
2. その規模に**合う**制度を当てる。合う制度がなければ「自己資金推奨」と書く
   （これも正直なモデルケースとして価値がある、との評価）
3. 制度が合う案だけ、当該公募回の公募要領で費目・上限・内枠を突合する
4. 効果額は投資内訳から積み上げ直す（按分しない）

なお `schemeInfo()` の `ai`・`jizoku` 分岐は実装済みなので、`PLANS` の `scheme` を
変えるだけで使える状態にはなっている。

**その他、本セッションで触れていないもの**：
- 第16セッションから持ち越しのiPhone SE（375×667）でdocuments.html +12px・profile.html +32px
  溢れる件（本人が「ここで止める」と判断済みの既知事項）
- index.html・applications.htmlのスマホ表示で画面下部に余白が残る件（同上）
- 制度データそのものに関する積み残し（gunma_takasaki_lease、都道府県横展開、
  国の新設制度網羅調査等）

### 6. 不確実な点・リスク

- **カタログ注文型の併用制限は未確認**。一般型では「最低賃金引上げの特例を使う事業者・
  再生事業者は大幅賃上げ特例と併用不可」（公募要領P.9）だが、カタログ注文型に同じ除外があるかは
  公式ページ（`/catalog/about/`）に記載が無い。現在の実装は `chinage` の `when` が `rateq` の
  **生の選択値**を見るため、カタログ注文型で再生事業者を選ぶと補助率は1/2に倒れる（区分が
  効かない）のに賃上げ特例だけは止まる、という挙動になる。**上限が通常額に留まる＝少なめに
  出る側なので過大表示にはならない**。その旨を画面の注記にも明示済み。カタログ注文型の
  要領で除外の有無が確認できたら見直すこと
- **`index.html` の `simTrackFromTop()` 系はデッドコードのまま**。2026-08-13の
  シミュレーター廃止で到達不能になった `renderTrackSelect` / `updateTrackDesc` /
  `getSelectedTrack` / `onSimTrackChange` / `PROGRAM_TRACKS` が残っている。本セッションでは
  削除していない。削除する場合、`SHORYOKUKA_CAP_BY_TIER` だけは `capManYen()`（6871行付近）から
  生きているので残す必要がある
- **`ai`（デジタル化・AI導入補助金）にも同種の枠未対応がある（軽微）**。インボイス枠・
  セキュリティ対策推進枠が質問に無く通常枠固定。ただし `notesMore` に明記済みで、
  無関係な枠の額を誤表示するわけではないため優先度は低い。枠を質問化するなら、
  インボイス枠は金額帯で補助率が2段階になるため計算式の拡張が要る
- **`ai` の第2次以降の締切は未公表**（第1次は2026年8月25日）。推測で書かないこと
- **令和8年度の東京都最低賃金1,280円は答申段階**（2026-08-05答申、官報公示前・発効日未定）。
  「10月頃発効」は報道ベースで一次情報ではない。9月の公示後に再確認が必要
- **美容業の計画書は投資額と効果額の妥当性が未検証**。本セッションで直したのは制度の
  数値（補助率・上限・賃金）だけで、`inv`・`saveY`・`roi` の妥当性は診断士のレビューを
  通していない

### 7. 触ってはいけない領域

第6セッション「7. 触ってはいけない領域」および第17セッション追記分に加えて、**本セッションで追加**：

- **`improvement/beauty/plan-*.html` と `_plan_data.json` を直接編集しないこと**。
  `_build_plans.js` → `_build_plans_v2.js` の生成物。本セッションで手修正をスクリプトへ
  取り込んだので、いまは再生成しても壊れないが、**また生成物側だけを直すと同じ問題が再発する**
- **`node _build_plans.js` を単独で実行しないこと**。v1は `_plan_data.json` の生成元で、
  現行HTMLはv2が作る。v1だけ実行すると旧版に退行する。必ずv2まで通すこと
- **`sim_data.js` の `calc.rate` に `optionsOf` を使っている制度で、選択肢に `when` を
  書かないこと**。`simResolveRate` の `optionsOf` 分岐は `when` を見ないので黙って無視される。
  `when`/`else` を効かせたいなら `rate` を `axes` 方式に変えること（shoryokuka がその形）
- **`choice` 型の質問を指す軸に `brackets` を書かないこと**。`brackets` は数値質問用で、
  `check_sim_data.py` が「max の無い区分がある」として弾く。`choice` の軸は
  `{ q: 'xxx' }` だけ書けば `simAxisPart` が options を直接読む

### 8. 最後の判断・関門

- アイコンは本人が「9の大きめアイコンで説明なし」を選択。案9の見本から説明文（`.tx` の
  サブテキスト）を落とし、ラベルは省略前の「事業計画書（A4×10p）」に戻して実装
- ⑦の作業範囲を「A. 一部だけ差し替え」で本人が選択 → 調査の結果、制度側の数値に疑いが出たため
  「先に数値の裏取り」を選択 → 裏取りの結果サイトは正しく、**私が診断士に渡した前提が誤り**だった
- ITツールの実在確認を「実ブラウザで確認する」で本人が選択
- 実装範囲を「整合を取って最後まで」→「投資額も含めて作り直す」で本人が選択したが、
  **診断士に設計案を却下されたため実装に至らなかった**
- 却下後の進め方を「修正を検証してコミット」で本人が選択し、誤り訂正のみをコミット
- **本セッションで私が3回、誤った報告をして訂正している**（いずれも推測を確認せずに
  報告したもの）：
  1. 「サイトの数値が誤りの可能性」→ サイトは正しく、私が診断士に渡した前提が誤りだった
  2. 「実額の2.5倍の誤りを修正した」→ 修正は画面に届いておらず、実際は3.75倍だった
  3. 「tracks を持つ14制度すべてで枠を選べない」→ 13制度は別経路で選べており、実害は1制度のみ
- **本セッションで作業ツリーを一度壊している**：生成手順の確認のつもりで
  `node _build_plans.js` を実行し、コミット済みの `plan-*.html` 10本と `_plan_data.json` を
  退行させた。`git checkout -- improvement/beauty/` で復元済み（コミット・pushはしていないので
  被害なし）。この事故が「生成物に手修正が入っている」という発見のきっかけになった

---

## 次の安全なアクション（2026-08-16 第17セッション時点の記録、配色強化＋改善計画ページ独立化。すべてpush・デプロイ済み。⑤⑦が持ち越し）

**次の安全なアクションは、本人にアイコン見本10案（下記URL）から採用案の番号を聞き、improvement.htmlのカード内リンクに適用すること。**

見本URL: https://claude.ai/code/artifact/791e7325-7c93-4cd8-81f8-834a3d185ebc
（2026-08-16 15時台に公開。本人はまだ番号を選んでいない）

適用先は `improvement/_build/build_hub.py` の2箇所（external用=beauty、plans用=他5業種）にある
`<div class="links">` の生成部分。**improvement.html を直接編集しないこと**（build_hub.py の生成物で、
次回の再生成で消える）。

### 再開者が仮定してはいけないこと

- **「業種別・改善計画10選のカードは絵文字アイコンで表示されている」と仮定しないこと。** 2026-08-16の
  コミット`2e234b1`で写真表示バグを修正済み。**現在は60枚すべて実写真**（6業種×10件、
  `improvement/{業種}/img10/plan-NN.jpg`）。本セッションで本人から「アイコンではなく写真を入れて」と
  指示があったが、現物を確認したところ既に写真表示になっていたため、本人確認のうえ作業不要と判断した
  経緯がある。同じ指摘が再度来たら、まず現物（本番URL）を見ること
- **「improvement/index.html が改善計画ページの実体」と仮定しないこと。** 2026-08-16に
  **ルート直下の `improvement.html` へ移動**した。`improvement/index.html` は新URLへの転送ページに
  置き換え済み。生成元 `build_hub.py` の出力先も変更済み
- **「embed_hub_cards.py を実行すればindex.htmlに改善計画カードが埋まる」と仮定しないこと。** 廃止済み。
  index.html側のマーカーを撤去したため、実行すると`SystemExit`で異常終了する（意図した安全弁）
- **「index.htmlの業種選択はページ内だけで完結している」と仮定しないこと。** 2026-08-16に
  localStorage（キー`koban_industry`）連動を追加した。improvement.htmlと双方向で同期する
- **「下部タブバーは4タブ」と仮定しないこと。** 5タブになった。並びは
  探す／書類準備／申請進捗／**改善計画**／会社情報（改善計画は右から2番目、本人指示）
- **「サイトの配色は淡くて色がほとんど使われていない」と仮定しないこと。** 2026-08-16に
  着色を大幅に増やした（実測でapplications 0→7要素、profile 0→12、documents 2→32、index 0→16）

### この配色作業で判明した重要な知見（次に配色をいじる人へ）

**ページ背景`--paper`(#eaf1fb)とカード地`--accent-wash`(#e2edfb)はRGB差が8/4/0しかなく、ほぼ同色。**
カードの地に`--accent-wash`を使うと「色を付けたのに背景に溶ける」現象が起きる（本人から2回指摘された）。
色を面で示したいときは、**地は白(`--paper-raised`)のまま、上辺/左辺の帯（4〜5px）と影で示す**こと。

また、`--sage`/`--rust`を同系のwash地の上に文字色として載せると、実測で3.6〜3.8:1しか出ず
WCAG AA(4.5:1)を割る。この用途向けに`--sage-ink`/`--rust-ink`をindex.htmlの4系統すべてに追加済み
（ライトは濃い側、ダークは明るい側の値）。

---

## 2026-08-16（第17セッション）

### 1. 現在の目標

本セッションは本人からの指示が4回に分かれて連続した：
1. profile.htmlの「会社情報の書き出し・読み込み」の見出しがカード枠ギリギリ→内側に寄せる
2. 「以前配色を変更したはずだが色が全然変わっていない」→原因調査と、サイト全体への配色適用
3. スクリーンショット2枚で「カードの色が背景と同系色」「見出しがまだ左寄り」の再指摘→修正
4. 「業種別・改善計画10選を別ページにして固定タブに追加」→見積もり提示→実装
5. 前工程で発見した既存バグ（写真が表示されない）の修正指示→修正
6. 改善計画ページの追加修正7項目→スライスと見積もり提示→S1〜S4実装＋S5見本提示

### 2. 変更点（すべてコミット・push・デプロイ確認済み）

**コミット `2456160`**（profile.html, index.html）：
- `.fold > summary`に`padding-right: 1rem`を追加。負のマージン(`margin: 0 -1rem`)を
  `padding-left`だけで戻していたため右側の余白が実質ゼロだった

**コミット `0f8262a`**（app_tabbar.js, applications.html, criteria.html, documents.html, index.html, profile.html, profile_status.html）：
- **配色トークンは定義済みだったが使う場所がほとんど無かった**という真因への対応。役割（コバルト=案内／
  オレンジ=注意／ミント=状態）は変えず、使用箇所を大幅に増やした
- タブバー：選択中タブのアイコンをコバルトのベタ塗り＋白抜きに
- index.html：セクション見出しの開閉状態を面の色で表現、章番号をチップ化、
  受付状態（通年/次回予定/受付終了/受付前）を状態別の色バッジに
- applications.html / profile_status.html：申請一覧に進捗連動の左帯（未着手=灰／進行中=コバルト／完了=ミント）
- applications.html：フェーズ見出しを帯に、進捗バーを12px・ミント地に、済んだ行を左帯で表示
- profile.html：会社切替パネル、選択中チップのベタ塗り、アクションカード上辺の役割色
- documents.html：記入の成功/失敗が同じ見た目だったのを橙/ミントで分離、手順見出しを帯に
- `--sage-ink`/`--rust-ink`を新設（wash地の上に載せる文字用、コントラスト確保）
- criteria.htmlに欠けていた`--on-accent`を4系統すべてに補った

**コミット `d7d3947`**（profile.html, index.html）：
- 本人のスクリーンショット指摘への対応。`.company-switcher`と`.sec-toggle`の地を
  `--accent-wash`から白に戻し、上辺/左辺の帯＋影で色を示す方式に変更（背景とのRGB差 8/4/0 → 21/14/4）
- `.fold > summary`の負のマージンを撤去し、見出しを素直にカード内側へ（カード内側から17.0px、
  他カード17.6pxと差0.6px）。index.htmlのフッターにも同じ修正を適用

**コミット `b99ef0b`**（app_tabbar.js, improvement.html[新規], index.html, sw.js, improvement/_build/{build_hub,embed_hub_cards,verify}.py, improvement/index.html）：
- 「業種別・改善計画 厳選10選」をルート直下の`improvement.html`として独立ページ化
- 配置の判断：`app_tabbar.js`は`location.pathname`のファイル名だけを見る実装のため、
  サブディレクトリに置くと「探す」タブが自分自身を指して誤点灯する。ルート直下にした
- `build_hub.py`の出力先自体を変更（手作業コピーだと次回の再生成で分岐するため）。
  カード内リンクもルート基準（`improvement/{業種}/...`）に組み替え
- `embed_hub_cards.py`は廃止（index.html側のマーカー撤去により実行すると異常終了）
- `verify.py`のリンクチェック対象・基準ディレクトリも新配置に追従
- 配色を旧茶系(`--accent:#845818`)から本体のコバルト系に統一、フォント・PWA meta・タブバーも本体標準に
- 業種連動：localStorage(`koban_industry`)＋URLパラメータ(`?industry=`)。URL優先、無ければ前回の選択
- タブバーに5個目「改善計画」を追加
- index.htmlの02区画（520行の埋め込みカード）をリンク2本の案内に置換
- sw.jsのCORE_ASSETSに追加、キャッシュv7→v8

**コミット `2e234b1`**（improvement.html, improvement/_build/build_hub.py）：
- 写真が一度も表示されないバグを修正。原因は`build_hub.py`が旧`embed_hub_cards.py`と2箇所食い違っていたこと
  - external(beauty、10件)：`icon`を先にチェックしており、`image`が設定されていても使われなかった
  - plans(他5業種、50件)：**image分岐自体が存在しなかった**
- `width="640" height="360"`も付与（レイアウトシフト防止）

**コミット `e4e849d`**（app_tabbar.js, improvement.html, improvement/_build/build_hub.py）：
- 本人指示7項目のうち軽微なUI変更4件（S1〜S4）
  - 「← ロードマップ本体へ」削除
  - タイトル直下の長い説明文をページ最下部へ移動（`.lede-foot`）
  - タブバーの「改善計画」を2番目→右から2番目へ
  - 業種選択を6個の横並びボタン→プルダウン(`select#indSelect`)に。index.htmlの`.hero-industry`と同じ見た目

### 3. 検証済みの証拠

すべてPlaywright(headless Chromium, `service_workers="block"`)による実ブラウザ検証（サブエージェント経由）。
**2026-08-16のセッション中に取得。**

- **配色の実測（変更前）**：本番URLを直接計測し、applications/profileは着色背景**0個**、
  documentsは2個、画面が「淡青＋白＋濃紺のほぼモノトーン」であることを確認。
  `--sage-wash`は5ページどこにも1度も出現していなかった。**この計測が「配色が変わっていない」という
  本人の指摘の真因特定につながった**
- **配色の実測（変更後）**：applications 0→7、profile 0→12、documents 2→32、index 0→16に増加
- **コントラスト**：全状態でWCAG AA達成を数値で確認。`.tt-state`はライトで
  is-open 5.66 / is-next 5.65 / is-closed 5.29、ダークで 8.21 / 5.49 / 6.68。
  修正前はis-open 3.76、is-closed 3.62でAAを割っていた
- **カードの背景分離**：`.company-switcher`と`.sec-toggle`の背景がbody背景とRGB差
  21/14/4（ライト）、14/20/27（ダーク）あることを実測。border-top/leftの帯も実測で確認
- **見出しの位置**：「会社情報の書き出し・読み込み」がカード内側から17.0px、
  他カードの17.6pxと差0.6pxで揃っていることを座標で実測
- **improvement.html新設**：A〜E全項目（回帰／タブバー／業種連動／カードリンク／旧URL転送）で問題なし。
  業種連動は双方向（index→improvement、improvement→index）とURL優先の3パターンすべて実測確認
- **写真表示バグ修正後**：6業種×2ビューポート×2テーマで、全60枚が`naturalWidth=640, naturalHeight=360`、
  HTTP 200、絵文字フォールバック0件を実測
- **S1〜S4**：5ページ×2ビューポート×2テーマでコンソールエラー0件・横スクロールなし。
  タブ並びが`[探す, 書類準備, 申請進捗, 改善計画, 会社情報]`であること、
  説明文の位置がカード末尾<`.lede-foot`<免責文言の順（390pxでy=3659<3699<3940）であることを実測
- 各コミット前に`python3 _tools/check_sim_data.py`を実行し「誤り0件・注意0件・ALL OK」（制度121件）を確認
- **2026-08-16 16:02時点で一次情報を再取得**：`git status`で作業ツリークリーン、
  `git log HEAD..origin/main`・`git log origin/main..HEAD`とも空（同期済み、HEADは`e4e849d`）、
  `gh run list`で`e4e849d`のデプロイが`success`（2026-08-16T06:36:38Z完了）
- **同時刻に本番URLを直接curlで確認**：タブ並びが5タブで「改善計画」が4番目、
  improvement.htmlの戻るリンク0件・プルダウン1件・img 60枚を確認
- ローカルサーバー（ポート8901〜8906）は各検証後に`taskkill`で停止し、
  `netstat`で残存なしを確認済み（最終確認も16時台に実施、残存0）

### 4. 一次情報の取り直しコマンド

```bash
cd /d/Claudecode/koban-roadmap
git status
git fetch origin main && git log HEAD..origin/main --oneline  # 空なら同期済み
git log origin/main..HEAD --oneline                            # 空ならpush不要
gh run list --limit 3 --json databaseId,status,conclusion,createdAt,headSha  # デプロイ確認
python3 _tools/check_sim_data.py                                # sim_data.jsの機械チェック

# 本セッションの変更点の現況確認
grep -n "label: '" app_tabbar.js                                 # タブ並び（5タブのはず）
grep -c "<img" improvement.html                                  # 写真の枚数（60のはず）
grep -n "id=\"indSelect\"" improvement.html                      # 業種プルダウン（1件のはず）
grep -n "sage-ink\|rust-ink" index.html                          # 配色トークン（4系統+使用2箇所）
grep -n "koban_industry" index.html improvement.html             # 業種連動

# 改善計画ページを再生成する（improvement.htmlを直接編集しないこと）
python3 improvement/_build/build_hub.py

# 美容業の対象制度を数える（⑦の調査対象）
python3 -c "
import json,sys
sys.stdout.reconfigure(encoding='utf-8')
D='improvement/_build/data'
ext=json.load(open(D+'/external/beauty.json',encoding='utf-8'))
print('beauty:', sorted({p['schemeKey'] for p in ext['plans']}))
for ik in ['food','lodging','manufacturing','realestate','education']:
    d=json.load(open(f'{D}/plans/{ik}.json',encoding='utf-8'))
    print(ik+':', sorted({p['subsidy']['key'] for p in d}))
"
```

### 5. 未着手の範囲

本人指示7項目のうち、**2項目が持ち越し**。

**⑤ アイコン見本の選定と適用（見本提示まで完了、選定待ち）**
- 見本10案は作成・公開済み（URL上記）。本人はまだ番号を選んでいない
- 案の方向性：案2(線画)/3(丸ベタ塗り)/4(角丸淡色)/5(塗りピル)/6(枠ピル)/7(グラデ丸)/8(横長行)/9(説明つき)/10(絵文字＋丸地)
- 適用先は`build_hub.py`の2箇所（external用とplans用）の`<div class="links">`生成部分
- 見積もり30〜45分

**⑦ 美容業の対象制度調査（未着手）**
- 一次情報で確認済みの事実（2026-08-16）：**美容業のみ2制度**（`kaizen`=業務改善助成金、
  `shoryokuka`=省力化投資補助金）。他5業種は4〜5制度
  - food: ai, food_labor, jizoku, kaizen, shoryokuka
  - lodging: ai, jizoku, kaizen, kanko_shoryokuka, shoryokuka
  - manufacturing: ai, jizoku, kaizen, monodukuri, shoryokuka
  - realestate: ai, jinzai, kaizen, shoryokuka
  - education: ai, jinzai, jizoku, kaizen, shoryokuka
- 他業種が使っている`ai`（デジタル化・AI導入補助金）・`jizoku`（小規模事業者持続化補助金）は
  美容業でも使える可能性が高く、調査の主対象になる見込み（**これは未検証の推測**）
- `shindanshi`（中小企業診断士）・`sharoushi`（社労士）スキルで一次情報にあたる想定
- 見積もり2〜4時間
- **注意**：制度を追加する場合、データ側（`improvement/_build/data/external/beauty.json`）の
  `schemeKey`を変えるのか、プラン自体を追加するのかは未検討

**その他、本セッションで触れていないもの**：
- 第16セッションから持ち越しのiPhone SE（375×667）でdocuments.html +12px・profile.html +32px溢れる件
  （本人が「ここで止める」と判断済みの既知事項）
- index.html・applications.htmlのスマホ表示で画面下部に大きめの余白が残る件（同上）
- 制度データそのものに関する積み残し（gunma_takasaki_lease、都道府県横展開、国の新設制度網羅調査等）

### 6. 不確実な点・リスク

- **`.cs-rename`と`.cs-status-pct`はデッドセレクタ**：profile.htmlのCSSに定義があるが、
  対応するDOM要素を生成するコードが無い（`.cs-rename`はJS側の`querySelector`も残っているが、
  生成側が第14セッションで削除済み）。本セッションで発見したが、スコープ外として触っていない
- **`build_hub.py`の`LINK_PREFIX`は「生成物がルート直下にある」前提**：将来この生成物を
  別の階層に移す場合、`LINK_PREFIX`と`verify.py`の基準ディレクトリを両方直す必要がある
- **`improvement/index.html`（転送ページ）は手書き**：`build_hub.py`はもうこのファイルを書かないので
  上書きされる心配はないが、逆に言えば「生成物ではない手書きファイルが1つある」状態。
  将来`improvement/`配下を整理する際に、これが生成物だと誤認して消さないこと
- **配色の「やりすぎ/足りない」は主観評価**：検証エージェントは「うるさすぎる箇所は無い」と評価したが、
  本人がスマホ実機で見てどう感じるかは未確認。特に`documents.html`は着色要素が32個と最も多い
- **アイコン見本のURLはClaude Artifactsのもの**：本人のアカウントに紐づく。
  次セッションで参照する際、URLが有効かは確認が必要
- **`--sage-ink`/`--rust-ink`はindex.htmlにしか定義していない**：現状の使用箇所が
  index.htmlの`.tt-state`のみのため。他ページで同じ「wash地に同系色の文字」を使う場合は
  そのページにもトークンを追加する必要がある

### 7. 触ってはいけない領域

第6セッション「7. 触ってはいけない領域」に加えて、**本セッションで新たに追加**：

- **`improvement.html`を直接編集しないこと**。`improvement/_build/build_hub.py`の生成物で、
  再生成すると手編集が消える。変更は必ず`build_hub.py`側に入れて`python3 improvement/_build/build_hub.py`を実行する
- **`improvement/index.html`は転送ページ**（手書き）。改善計画ページの実体ではない
- **`improvement/_build/embed_hub_cards.py`は廃止済み**。実行しないこと（異常終了する設計だが、
  もし将来index.htmlに「改善計画」の埋め込みを復活させたくなった場合は、このスクリプトを直すのではなく
  現在の独立ページ方式を維持したまま検討すること）
- `index.html`のモバイル用CSSは`<style>`ブロック末尾に足すこと（途中に足すと同詳細度の後勝ちで無効化される。
  第16セッションで実際に踏んだ罠）

### 8. 最後の判断・関門

- 「配色が変わっていない」の原因について、最初「サーバーは更新済みなのでブラウザキャッシュ」と回答したが、
  本人の「シークレットでも変わらない」で棄却。**実測し直して「トークンは定義済みだが使う場所が無い」が
  真因と判明した**。この誤りは本人に明示的に謝罪・訂正済み
- 配色の強度を「しっかり色を使う」、役割を「現行3役割を強化」で本人が選択
- 改善計画ページの配置を「ルートにimprovement.htmlを新設」、index.html側を「削除」で本人が選択
- 途中で`improvement/index.html`が自動生成物と判明したため、方式を「build_hub.pyの出力先を変える」で
  本人が選択（当初の見積もりを訂正して提示し直した）
- 業種連動の方式を「localStorage＋URL両方」で本人が選択
- タブのラベルを「改善計画」、タブ位置を「右から2番目」で本人が指定
- ④「アイコンではなく写真を入れて」は、現物確認の結果すでに写真表示だったため、
  本人に確認して「写真が出ていないと思っていた」との回答を得てスキップと確定
- 本セッションの区切りを「S1〜S4＋S5見本提示まで」で本人が選択
- **本人確認待ちの事項：アイコン見本10案のうちどれを採用するか（次の安全なアクション）**

---

## 次の安全なアクション（2026-08-16第16セッション時点の記録、モバイル改善4件・すべてpush済み。iPhone SE 2ページに既知の未解消分あり）

**次の安全なアクションは、本人からの次の指示を待つこと。** ただし以下2点は明示的に「これ以上は詰めない」と本人判断で確定した既知の未解消事項であり、次回このページに触れる際に「まだ直っていない」と誤解しないよう申し送る。

- **iPhone SE（375×667）幅で、documents.html（書類準備）が+12px、profile.html（会社情報）が+32px、画面から溢れている。** これは実測（Playwright、2026-08-16 20時台）で確認済みの残課題で、本人に「ここで止めるか／SEでも完全に収めるか／先に大画面の余白を直すか」を確認し、「ここで止める（推奨）」を選択したことによる意図した現状。バグではない。
- **index.html・applications.htmlは、390×844や430×932など主要スマホサイズでは1画面に収まっているが、画面下部に大きめの余白が残っている。** 特にapplications.html（申請進捗、初期状態＝申請0件）は390×844で約504px、430×932で約592pxが無地の水色余白になっている（Playwright実測、2026-08-16）。これは「1画面に収める」という制約を優先して他要素を圧縮した結果、余白が相対的に目立つようになったもので、本人からは「先に大画面の間延びを直す」という選択肢を提示したが「ここで止める（推奨）」が選ばれ、着手していない。次回「スマホの見た目が間延びしている」という指摘が来たら、この申し送りを前提に対応すること（原因は既に特定済みなので調査からやり直す必要はない）。

再開者が仮定してはいけないこと：
- 「スマホの1画面収め対応はすべてのサイズ・ページで完全に達成された」と仮定しないこと。iPhone SEの2ページ（documents.html/profile.html）のみ未達で、本人の意図的な判断により保留中。
- 「index.htmlのモバイル用CSSはヒーローの直後（199行目付近）にある」と仮定しないこと。本セッション中に、モバイル用メディアクエリを基底ルールより前に書いたことで同詳細度の後勝ちにより指定が無効化される不具合が実際に発生し、`<style>`ブロック**末尾**（1310行目以降）に移動して解決した。今後index.htmlのモバイル調整を追加する際は、必ず末尾に足すこと（途中に足すと同じ不具合を再現する）。
- 「`.has-accordion main`と`main`は同じ詳細度」と仮定しないこと。前者の方が詳細度が高く、モバイル用メディアクエリでmainのpadding-topを上書きする際は`main, .has-accordion main`のように両方セレクタを書く必要がある（本セッションで実際にこの罠を踏んで気づいた）。

### 一次情報の取り直しコマンド

```bash
cd /d/Claudecode/koban-roadmap
git status
git fetch origin main && git log HEAD..origin/main --oneline  # 空なら同期済み
python3 _tools/check_sim_data.py                                # sim_data.jsの機械チェック
gh run list --limit 3 --json databaseId,status,conclusion,createdAt,headSha  # デプロイ確認
grep -n "max-height: 700px" index.html documents.html profile.html applications.html  # SE対応の現況確認
```

iPhone SE等の小さい画面での高さを再実測する場合は、Playwrightで375×667・390×844・430×932の3サイズ×4ページ（index/documents/applications/profile、初期表示・全カード閉じた状態）で`document.documentElement.scrollHeight`と`window.innerHeight`を比較するのが最速。本セッション中に複数回この形式で検証しており、再現性は確認済み。

---

## 2026-08-16（第15セッション、配色案8反映＋タブバー/バッジ/会社情報カードをSVGアイコン化、すべてpush・デプロイ確認済み）

**次の安全なアクションは、本人からの次の指示を待つこと。** 本セッションで着手した3つの見た目変更（アイコン、配色、会社情報カード）はすべて完了・コミット・push・デプロイ確認済みで、積み残しの技術タスクはない。ただし、profile.html以外のページ（documents.html・applications.html・index.html等）のアイコンは今回のUIアイコン置き換えの対象に入っていない絵文字がまだ残っている可能性があり、本人が「他のページも同様に」と言えばそこから着手する。

### 1. 現在の目標

前セッション（第14セッション）で提示した配色見本10案・UIアイコン見本10案への回答を受け、(a) タブバー・書類バッジのアイコンをSVG化、(b) サイト全体の配色を案8「コバルト×オレンジ×ミント」に変更、(c) 会社情報ページのアクションカードアイコンをポップなグラデーション丸背景に変更、の3つを順に実施した。

### 2. 変更点

**コミット `debffff`**（`app_tabbar.js`, `documents.html`）：
- `app_tabbar.js`：下部タブバー4項目（探す／書類準備／申請進捗／会社情報）の絵文字（🔍📄✅🏢）を、案7スタイル（太めアウトライン＋角丸スクエア、2pxストローク）のインラインSVGに置き換え。アイコンサイズを`font-size:1.85rem`相当から`width/height:30px`に拡大
- `documents.html`：書類チェックリストの`gt-badge`（自動入力可／取り寄せ要／対象外）に、案2スタイル（丸ぽちゃ線画、2.4pxストローク）のSVGアイコン（チェック丸／注意三角／×丸）を追加

**コミット `68c5628`**（`index.html`, `documents.html`, `applications.html`, `profile.html`, `profile_edit.html`, `profile_status.html`, `criteria.html`, `program.html`, `manifest.json`）：
- サイト全体8ページのCSSカラートークンを、旧配色「藍×橙×花青」から案8「コバルト×オレンジ×ミント」へ全面差し替え。3色のロール構造（案内色／注意色／状態色）は維持し、色相のみ変更：案内=コバルト`#0057c2`、注意=オレンジ`#d84315`、状態=ミント`#0d8a6e`（ダーク版・`accent-soft`/`-wash`等の派生値もすべて再設計）
- ライト、`prefers-color-scheme:dark`、`data-theme="dark"`/`"light"`手動切替の3系統すべてを更新
- `manifest.json`の`theme_color`/`background_color`、全8ページの`<meta name="theme-color">`も新配色に統一
- index.htmlは手作業で書き換え、残り7ページはhex値の機械的な文字列置換スクリプト（`recolor.py`、スクラッチパッドのみに保存、リポジトリには含めていない）で一括反映

**コミット `10b5f1c`**（`profile.html`）：
- 本人からprofile.html（会社情報ページ）のスクリーンショット付きで「このページに使うアイコンもポップな感じ、色にしたい」との指示を受け、プロフィール編集／現在の申請状況の2カードを題材に見本10案を提示。「案4（グラデーション丸背景、白抜きアイコン）」を選定
- profile.htmlの5箇所（プロフィール編集＝鉛筆、現在の申請状況＝チェックリスト、メモ＝メモ用紙、書き出し＝下矢印、読み込み＝上矢印）の絵文字（✏️📋📝⬇️⬆️）を、案4スタイル（`border-radius:50%`＋`linear-gradient(135deg, 色1, 色2)`背景＋白抜き線画SVG）に統一。グラデーション色は役割ごとにコバルト/ミント/オレンジを2色ずつ組み合わせ

### 3. 検証済みの証拠

- **2026-08-15、Playwright実ブラウザ検証（ローカル`python3 -m http.server`、headless Chromium）**：
  - `debffff`分：index.html/documents.htmlをスマホ幅(390px)でスクリーンショット確認。documents.htmlは制度選択後のバッジ表示（オレンジ/ミントのアイコン）も確認。applications.html/profile.htmlのタブバー表示も確認。全ページコンソールエラー0件
  - `68c5628`分：8ページ×ライト/ダーク＝16パターンをスクリーンショット確認。全パターンで`scrollWidth<=clientWidth`（横スクロールなし）・コンソールエラー0件を機械的に確認。documents.htmlのバッジ色（ミント=OK、オレンジ=注意）も目視確認
  - `10b5f1c`分：profile.htmlをライトモード・ダークモード（`color_scheme='dark'`）・書き出し/読み込みパネル展開状態の3パターンでスクリーンショット確認。コンソールエラー0件、グラデーションアイコンの視認性を目視確認
- 各コミット前に`python3 _tools/check_sim_data.py`を実行し「誤り0件・注意0件・ALL OK」（制度121件）を確認
- **2026-08-16、本handoff作成時点で一次情報を再取得**：`git status`で作業ツリークリーン、`git fetch origin main`後`git log HEAD..origin/main`・`git log origin/main..HEAD`とも空（同期済み）、`gh run list --limit 3`で最新3コミット（`10b5f1c`/`68c5628`/`debffff`）すべて`conclusion:"success"`・`status:"completed"`を確認（`10b5f1c`のデプロイは2026-08-15T16:10:57Zに完了）
- ローカルサーバーは各検証後に`netstat`でPIDを確認の上、都度`taskkill`で停止済み

### 4. 一次情報の取り直しコマンド

```bash
cd /d/Claudecode/koban-roadmap
git status
git fetch origin main && git log HEAD..origin/main --oneline  # 空なら同期済み
git log origin/main..HEAD --oneline                            # 空ならpush不要
gh run list --limit 3 --json databaseId,status,conclusion,createdAt,headSha  # デプロイ確認
python3 _tools/check_sim_data.py                                # sim_data.jsの機械チェック
grep -n "^  :root {" -A 15 index.html                            # 現在の配色トークン確認
grep -n "ICON_SEARCH\|ICON_DOCS\|ICON_CHECK\|ICON_BUILDING" app_tabbar.js  # タブバーアイコン確認
grep -n "cs-action-icon" profile.html                             # 会社情報カードアイコン確認
```

### 5. 未着手の範囲

- **index.htmlの改善計画10選カード内の絵文字**（`📄 事業計画書`／`🖥 プロトタイプ`、約50箇所）は今回のアイコン置き換え対象に入れていない。UIアイコン見本を提示した際の対象は「タブバー」「バッジ」「会社情報カード」の3種のみで、それ以外の絵文字使用箇所は本人から指示を受けていない
- **index.html本体の`pill`/`elig-badge`、program.htmlの`pill-row`、criteria.htmlの`pill competitive`等**（前セッションの調査で判明した他のchip/badge要素）も、色は配色反映で新トークンに追従しているはずだが、アイコン付与や形状変更は行っていない
- PWAホーム画面アイコン（icon-192/512/512-maskable/apple-touch-icon）は第14セッションで差し替え済みのまま。今回の配色変更に合わせた再デザインの指示は受けていない（本人がダウンロードフォルダに置いた既存デザインを使い続けている状態）

### 6. 不確実な点・リスク

- **`recolor.py`はリポジトリに含まれていない**：スクラッチパッド（`C:\Users\hiros\AppData\Local\Temp\claude\...\scratchpad\recolor.py`）にのみ存在するセッション限りのスクリプトで、次回配色を再調整する際は同じ仕組みを再現するかindex.htmlの`:root`ブロックを起点に手作業で行う必要がある
- **criteria.html・program.htmlのトークンブロックは他ページと変数の過不足がある**（`--on-accent`や`--cat-*`系を持たない簡略版）。今回はhex値の文字列置換のみで対応したため、変数構造自体の統一は行っていない。将来的にこの2ページで`--cat-*`系を使う変更をする場合は、先に変数追加が必要
- **グラデーションアイコンの配色組み合わせ（コバルト×ミント×オレンジの2色ペア）は本セッションで新規に設計した値**で、既存のdesign.mdやトークン設計ドキュメントには反映していない。次回another色を足す場合はprofile.html内のCSS（`.ci-accent`/`.ci-sage`/`.ci-rust`）を直接参照すること

### 7. 触ってはいけない領域

前セッションから変更なし。第6セッション「7. 触ってはいけない領域」、第14セッション追記分（`profile_edit.html`・`profile_status.html`の依存関係、`documents.html`の`registerToApplications()`とlocalStorageキーの整合性）を参照。

### 8. 最後の判断・関門

- 配色見本10案（案1〜10）を提示し、本人が「案8」を選定（コバルト&ミント）
- UIアイコン見本10案を提示し、「アイコンは案7、バッジは案2」の組み合わせで確定
- 会社情報カード用のアイコン見本を別途10案提示し、「案4（グラデーション丸背景）」を選定
- 配色反映の是非は本人に「はい」の明示的な承認を得てから着手した。会社情報カードのアイコン変更は、本人が案4を選んだ回答自体を実装開始の合図として着手し、コミット・push後に完了報告のみ行った（各コミット前に都度確認は取っていない）

---

## 2026-08-15（第14セッション：documents/applications/profile改善＋スケジュール機能廃止＋PWAアイコン差し替え、すべてpush済み）

**本セッションで行った作業はすべてコミット・push済み（最終コミット`6d2dd59`、2026-08-15時点でorigin/mainと同期確認済み、GitHub Pagesへのデプロイも`gh run list`のheadShaで`6d2dd59`が`success`で反映済みを確認済み）。**

次の安全なアクションは、**本人が予告している「UI/UXの大きめの変更」の依頼を待つこと**（本セッション終了時点で本人から「次のセッションではUI/UXの大きめの変更を依頼する予定」との申し送りあり。具体的な内容は未提示）。積み残しの技術タスクはない。

再開者が仮定してはいけないこと：
- 「schedule.html・gantt_data.js・gantt_ui.jsがまだ存在する」と仮定しないこと。第14セッションで完全削除済み（`git rm`）。下部タブバーは4タブ構成（探す／書類準備／申請進捗／会社情報）になっている。**HANDOFF.md 141行目・192行目に残る「5タブ構成」の記述は第12セッション時点の過去の事実描写であり、現状を表していない**（削除前の記録として意図的に残してある。現状は`app_tabbar.js`を直接見ること）
- 「profile.htmlの中でプロフィール編集・現在の申請状況が開閉する」と仮定しないこと。第14セッションで別ページ化した。プロフィール編集は`profile_edit.html`、現在の申請状況は`profile_status.html`に分離済み。profile.html内で開閉するのは「メモ」のみ
- 「documents.htmlの『申請進捗・スケジュールに登録する』ボタンは単なるリンク」と仮定しないこと。第14セッションで`<button>`化し、クリック時にlocalStorageへ直接申請データを書き込んで自動作成する仕様に変更済み（同一制度の申請が既にあれば重複作成しない）
- 「PWAのホーム画面アイコンは第12セッション作成のPython生成アイコン（藍色の折れ線グラフ）のまま」と仮定しないこと。第14セッションで本人がダウンロードフォルダに置いた画像（チェックリスト＋成長グラフのデザイン）に差し替え済み（icon-192/512/512-maskable/apple-touch-icon の4ファイル、コミット`6d2dd59`）

第13セッションまでの引き継ぎ本文は下に残すが、現状と矛盾する記述は本セッション（第14セッション）の内容を優先すること。

---

## 2026-08-15（第14セッション：documents/applications/profileのUI改善、スケジュール機能の全廃止、PWAアイコン差し替え）

### 1. 現在の目標

本セッションは会話の流れで2つの依頼が連続した：
1. documents.html（書類準備ページ）と会社情報タブ（profile.html）のデザイン改善7点＋追加機能アイデアの提示（複数選択式で本人が「会社情報のエクスポート／インポート」「会社ごとのメモ欄」を選択）
2. 実装後に本人から新たに7点の追加改善指示。うち「スケジュールに登録できる制度は限られてるの？」という質問への回答をきっかけに、スケジュール機能（gantt）の全廃止という大きな決定に至った
3. さらにセッション終盤で「スマホのホーム画面に追加したときのアイコンをダウンロードフォルダにある画像にして」という別件の依頼があり、PWAアイコンを差し替えた

### 2. 変更点（すべてコミット・push済み、origin/mainと同期確認済み）

**コミット `4ead1c1`**（documents.html/applications.html/profile.html/company_data.js、push済み）：
- 全6ページ共通：ページ上部の「← 戻る」リンクと「画面を暗くする」ボタン（テーマ手動切替）を全廃止。ダークモードはOSの`prefers-color-scheme`設定への自動追従のみに統一（index.htmlは元々この方式で変更なし）。program.html本文下部にも同種の「← 制度の一覧に戻る」ボタンが残っていたため、本人に範囲確認の上これも削除
- documents.html：自動入力バッジ「取り寄せて記入を試みます（失敗することがあります）」がスマホ375px幅で横スクロールを起こす問題を、CSSで`display:block`＋折り返し可能にして解消。文言も短縮
- documents.html：手順5にあった「提出の締切日を入れると…」の日付入力欄（`#dueBox`/`#deadlineInput`）と「全員/法人/個人事業主/該当者のみ」の凡例説明文を削除（本人の「画像2削除」指示、締切逆算機能自体を削除する判断は本人に確認済み）
- documents.html：手順4のボタン文言を「記入済みを保存する」→「まとめてダウンロード」に変更（実態がダウンロード動作のため）
- documents.html：自動入力対象外の書類（PDF等）の一括ダウンロードは、ミラー登録が0件で全件が配布元への直接リンクのみのためCORS制約で技術的に不可能と確認し、実装を見送り（本人に説明の上、了承を得た）
- documents.html：制度を選ぶと書類一覧の下に「申請進捗・スケジュールに登録する」ボタンを新設（後述の第14セッション後半で自動登録機能に強化）
- applications.html：申請詳細に「スケジュールで確認する」リンクを追加（後述の第14セッション後半で削除）
- profile.html：会社選択チップの下に「プロフィール編集」「現在の申請状況」「メモ」の3カードを新設する階層構造に変更（開くと会社選択のみ表示、各カードを押すとパネルが展開する方式。後述の第14セッション後半で編集・状況は別ページ化）
- profile.html：追加機能として「会社ごとのメモ欄」（`company.memo`をcompany_data.js経由で保存）と「会社情報のエクスポート／インポート」（JSON書き出し・読み込み）を実装。company_data.jsに一覧全体を置き換える`replaceAll()`APIを新設

**コミット `55d2041`**（app_tabbar.js/applications.html/documents.html/index.html/profile.html/program.html/sw.js、新規profile_edit.html/profile_status.html、削除gantt_data.js/gantt_ui.js/schedule.html、push済み）：
- **スケジュール機能を全廃止**。本人から「スケジュールに登録できる制度は限られてるの？」と問われ一次情報を確認した結果、`gantt_data.js`の`schemes`が128制度中7制度（career/jinzai/kaizen/hatarakikata/jizoku/ai/shoryokuka）のみに対応（各制度の公募要領を人手で拾って作った手作業データのため機械的な全制度拡充は不可能）と判明。この事実を報告した上で「今回は現状のまま」「別途拡充作業」「機能自体を削除」の3択を提示し、本人が「削除。この機能をなくす」を選択
- 削除範囲を「schedule.html本体だけ」か「全ページのリンク・言及も含む全体」か確認し、「はい、すべて削除します」の指示で全体削除を実施
- `schedule.html`・`gantt_data.js`・`gantt_ui.js`の3ファイルを`git rm`
- `app_tabbar.js`：TABSから「スケジュール」タブを削除、4タブ構成（探す／書類準備／申請進捗／会社情報）に
- `program.html`：「申請とスケジュール」概要セクション（`#ptSchedule`、`renderScheduleOverview()`関数）を削除。この過程で誤って汎用の`esc()`関数まで削除してしまうミスが発生したため、直後に復元（同一コミット内で修正済み、実害なし）
- `applications.html`：申請詳細の「スケジュールで確認する」リンク（`#appDetailScheduleLink`）を削除
- `index.html`：呼び出し元が既に存在しないデッドCSS（`.ov-*`/`.gt-*`、旧「06 申請の流れの概要」「07 手順とスケジュール」セクションの残骸、168行分）を削除
- `sw.js`：CORE_ASSETSから`schedule.html`/`gantt_data.js`/`gantt_ui.js`を除去、キャッシュバージョンを`v4`→`v5`に更新
- **documents.htmlの「申請進捗・スケジュールに登録する」リンクを「申請進捗に登録する」ボタンに変更し、自動登録機能を実装**。`registerToApplications(key)`関数がlocalStorageの`koban_applications`を直接読み書きし、同一制度の申請が無ければ自動作成、あれば既存のものを開く（重複作成しない）
- **applications.html**：進捗表示「0 / 21」に「取り寄せ書類」という説明ラベルを追加。一覧の見た目をbox-shadow付きのカード風に変更
- **profile.htmlを3ページに分割**：
  - `profile.html`：会社の選択・追加・削除・切替・メモ・書き出し／読み込みのみに縮小。「プロフィール編集」「現在の申請状況」ボタンは`<a>`リンク化し、それぞれ新規ページへ遷移する方式に変更。会社選択チップの「名前を変更」ボタンを削除（プロフィール編集フォームの入力欄で直接書き換える想定に統一、本人指示）
  - `profile_edit.html`（新規）：プロフィール編集フォーム（質問形式ウィザード一式）を移植。ページ上部に「編集中の会社：〇〇」表示と「別の会社に切り替える」リンクを追加
  - `profile_status.html`（新規）：申請状況一覧を移植。パーセンテージ表示の下に「取り寄せ書類 n/m件」という説明ラベルを追加（本人の「パーセンテージが何の割合か分からない」指摘への対応）
- `app_tabbar.js`：matchパターンを`/^profile(_edit|_status)?\.html$/`に拡張し、新設2ページでも「会社情報」タブがハイライトされるように

**コミット `6d2dd59`**（icons/apple-touch-icon.png, icons/icon-192.png, icons/icon-512-maskable.png, icons/icon-512.png, apple-touch-icon.png、push済み）：
- 本人がダウンロードフォルダに保存した画像（`C:\Users\hiros\Downloads\ChatGPT Image 2026年8月15日 07_03_11.png`、チェックリスト＋成長グラフ＋円マークのデザイン、1254x1254、既に丸角スクエアのアイコン向けデザイン）から、Pillowで4種類のアイコンを生成して差し替え：
  - icon-192.png（192x192、単純リサイズ）
  - icon-512.png（512x512、単純リサイズ）
  - icon-512-maskable.png（512x512キャンバスに絵柄を80%サイズで中央配置、白背景でパディングし安全マージンを確保）
  - apple-touch-icon.png（180x180、icons/配下とルート直下の両方に同一ファイルを配置）
- manifest.jsonおよび各HTMLのlink要素は変更していない（ファイルの中身のみ差し替え）

### 3. 検証済みの証拠

**すべて2026-08-15、Playwright実ブラウザ検証（サブエージェント経由、ローカル`http.server`、headless Chromium）**：

- 第1回検証（コミット`4ead1c1`分、program.html修正含め2回実施）：全6ページで「戻る」「テーマ切替」の不在確認、`prefers-color-scheme`のdark/lightでbody背景色が切り替わること確認、documents.htmlの375px幅で横スクロールなし確認（`scrollWidth<=clientWidth`）、`#dueBox`/`#deadlineInput`の不在確認、手順4ボタン文言確認、「申請進捗・スケジュールに登録する」→`applications.html?p=<key>`遷移確認、applications.htmlの`?p=`遷移・「スケジュールで確認する」リンク確認、profile.htmlの3カード階層UI・排他制御・会社切替時の再描画・書き出し/読み込み動作確認。コンソールエラー全ページ0件。1回目の検証でprogram.htmlに削除漏れの「← 制度の一覧に戻る」ボタンを発見し、本人確認の上削除・再検証しPASS
- 第2回検証（コミット`55d2041`分）：schedule.html直接アクセスで404確認、下部タブバー4タブ構成確認、program.html（`?key=ai`）でコンソールエラー0件・「申請とスケジュール」セクション不在確認、applications.htmlに「スケジュールで確認する」リンク不在確認、documents.htmlの自動登録ボタンでlocalStorageに1件だけ申請が作成されること（2回押しても重複しないこと）確認、applications.htmlの「取り寄せ書類」ラベル・カード風見た目確認、profile.html/profile_edit.html/profile_status.htmlの3ページ分割全9項目確認（「名前を変更」ボタン不在、`<a>`リンクでの遷移、フォーム入力の自動保存、会社切替時に前の会社データが残らないこと、下部タブバーのハイライト、書き出し/読み込み動作等）、全ページコンソールエラー0件、ライト/ダーク両モードでスクリーンショット目視確認。**全55項目PASS**（うち1件はテストスクリプト側の`json.dumps`アサーション不備による誤FAILで、実際のlocalStorageデータは正しく保存されていることを生データで確認済み、プロダクトコードの不具合ではない）
- 第3回検証（コミット`6d2dd59`分、PWAアイコン）：manifest.jsonのicons 3エントリすべて200確認、`/icons/apple-touch-icon.png`と`/apple-touch-icon.png`両方200・image/png・21,651 bytes・内容一致確認、index.htmlの`<link rel="apple-touch-icon">`実リンク先200確認、sw.jsのCORE_ASSETSにicon-192/512含有確認、Pillowでの実測ピクセルサイズが全ファイル期待通り（192x192/512x512/512x512/180x180）確認、index.htmlコンソールエラー0件確認
- 各コミット前に`python3 _tools/check_sim_data.py`を実行し「誤り0件・注意0件・ALL OK」（試算を定義した制度121件／サイト全体128件）を確認
- 各コミット後`git fetch origin main`を実行し、`git log HEAD..origin/main`・`git log origin/main..HEAD`とも空であることを確認（push成功・同期済み）
- 本引き継ぎ作成時点（2026-08-15）で改めて`git status`・`git fetch`・`gh run list`を再実行し、作業ツリークリーン・HEAD `6d2dd59`・origin/mainと同期・GitHub Pagesデプロイ`success`（headSha一致）を確認済み
- ローカルサーバー（複数ポート使用）は各検証後にすべて停止・`netstat`で残存プロセスなしを確認済み（各サブエージェントの報告による）

### 4. 一次情報の取り直しコマンド

```bash
cd /d/Claudecode/koban-roadmap
git status
git fetch origin main && git log HEAD..origin/main --oneline  # 空なら同期済み
git log origin/main..HEAD --oneline                            # 空ならpush不要
gh run list --limit 3 --json databaseId,status,conclusion,createdAt,headSha  # GitHub Pagesデプロイの反映確認
python3 _tools/check_sim_data.py                                # sim_data.jsの機械チェック
ls schedule.html gantt_data.js gantt_ui.js 2>&1                 # 削除済みか確認（"No such file"が出れば正常）
grep -n "TABS = \[" -A 5 app_tabbar.js                           # 現在のタブ構成確認（4タブのはず）
grep -n "registerToApplications" documents.html                  # 自動申請登録機能の現状確認
ls profile.html profile_edit.html profile_status.html           # 3ページ分割の現状確認
python3 -c "from PIL import Image; [print(f, Image.open(f).size) for f in ['icons/icon-192.png','icons/icon-512.png','icons/icon-512-maskable.png','icons/apple-touch-icon.png','apple-touch-icon.png']]"  # アイコンサイズ確認
```

### 5. 未着手の範囲

**本人が次回セッションで予告した最優先事項**：
- **「UI/UXの大きめの変更」**。本セッション終了時点（`/handoff`呼び出し時）で、本人から「次のセッションではUI/UXの大きめの変更を依頼する予定」との申し送りのみあり、具体的な内容・対象ページ・変更の方向性は一切提示されていない。次回セッション開始時に本人へ詳細をヒアリングすること

**その他、本セッションでは触れていないもの**：
- 前回までの引き継ぎ（第6〜13セッション）に記載されていた制度種別の全数監査・都道府県横展開・国の新設制度網羅調査等、サイトの制度データそのものに関する積み残しタスクは今回も未着手（本セッションはUI/UX改修とスケジュール機能廃止に終始）
- profile_edit.html・profile_status.htmlの新規UI自体の使い勝手（初回訪問時の分かりやすさ、質問ウィザードの操作性等）は、移植後の動作確認のみで新規のUX改善検討は行っていない
- documents.htmlの「申請進捗に登録する」ボタンで自動作成された申請の名前は、制度名がそのまま使われる仕様（`(D.programs[key] || {}).name || key`）。ユーザーが後から名前を変更したい場合はapplications.html側の編集機能に依存するが、この導線が分かりやすいかは未検証

### 6. 不確実な点・リスク

- **HANDOFF.md自体に残る過去の「5タブ構成」の記述**：141行目・192行目（第12セッションの記録）は、当時は事実だったが現在は4タブに変更されている。意図的に過去の記録として残しているが、次回セッションでこのファイルを検索・参照する際に「今も5タブ」と誤読しないよう注意が必要（本引き継ぎの「再開者が仮定してはいけないこと」に明記済み）
- **program.htmlの`esc()`関数削除→復元の経緯**：スケジュール機能削除の過程で、`renderScheduleOverview()`関数と一緒に汎用ユーティリティの`esc()`まで誤って削除してしまい、直後に気づいて同一コミット内で復元した。最終的なコミット内容は正しいが、同種の「大きなブロック削除で汎用関数を巻き込む」ミスは今後も起こりうるパターンとして注意
- **profile.htmlの`csStatusSub`（「申請の件数と進み具合を見ます」）が静的文言のまま**：以前は動的に「N件の申請があります」と表示する仕組みがあったが、profile_status.htmlへの分離に伴い、profile.html側のこのサブテキストは固定文言に戻っている（動的化は見送り、今回のタスク範囲外と判断）。将来的に動的表示に戻す場合は、profile.html側で`koban_applications`を読む処理を追加する必要がある
- **PWAアイコンの著作権・出所**：ダウンロードフォルダの画像はファイル名から「ChatGPT Image」と推測され、本人がAI生成した画像と思われるが、生成経緯・利用許諾については確認していない（本人が自分のサイトに使う目的で用意したものと判断し、そのまま使用した）
- **sw.jsのCORE_ASSETSにmaskable版・apple-touch-icon.pngが含まれていない**：Service Workerの事前キャッシュ対象はicon-192.png/icon-512.pngのみで、maskable版とapple-touch-icon.pngは含まれていない（第14セッション以前からの既存構成で、今回変更していない）。オフライン時にこれらのアイコンが表示されない可能性があるが、意図した設計かどうかは未確認

### 7. 触ってはいけない領域

前回までのセッションから変更なし。第6セッション「7. 触ってはいけない領域」を参照。**新たに追加**：
- `profile_edit.html`・`profile_status.html`は`profile.html`から分離した新規ファイルで、3ファイルとも`company_data.js`（会社データ）・`page_data.js`（制度データ）・`common_docs.js`（共通書類データ）に依存する。3ファイルのいずれかを変更する際は、他の2ファイルとの整合性（特に`window.KOBAN_COMPANY`のAPI呼び出し方）を確認すること
- `documents.html`の`registerToApplications()`関数は`localStorage`の`koban_applications`・`koban_applications_seq`キーに直接読み書きする。`applications.html`側の同名キーの読み書きロジック（`load()`/`save()`/`nextId()`）と一致した形式を維持すること。片方だけ変更するとデータ形式の不整合が起きる

### 8. 最後の判断・関門

- documents.htmlの証明書絞り込み説明文＋締切入力ブロックの削除範囲（凡例文のみ／両方）を本人に確認し、「凡例説明文+締切入力ボックス両方削除」を選択
- 自動入力対象外書類の一括ダウンロード機能について、CORS制約で技術的に不可能である事実を説明した上で対応方針を確認し、「今回は見送り」を選択
- 会社情報タブの追加機能候補を複数選択式で提示し、「会社情報のエクスポート／インポート」「会社ごとのメモ欄」を選択
- 「スケジュールに登録できる制度は限られてるの？」という質問に対し、一次情報（128制度中7制度のみ対応）を確認・報告した上で対応方針を確認し、「スケジュールを削除。この機能をなくす」を選択
- 削除範囲（schedule.htmlのみ／全ページのリンク・言及も含む全体）を確認し、「はい、すべて削除します。書類準備にあるボタンの表記なども確認して変更して」を選択
- 「プロフィール編集」「現在の申請状況」を別ページ化する際、メモ機能も同様に別ページ化するか確認し、「メモは現状維持（ページ内開閉のまま）」を選択
- 別ページ化の実装方式（新規HTMLファイル2本／クエリパラメータ切替）を確認し、「新規HTMLファイル2本を作る」を選択
- 各コミット・push実行は都度本人に確認し、すべて「推奨」を選択して実行
- 本人からPWAアイコンの差し替え依頼を受け、ダウンロードフォルダの画像を確認・適用し、実ブラウザ検証後にコミット可否を確認して「はい」で実行

---

## 2026-08-15（第13セッション：複数会社の自動入力バグチェック、レースコンディション1件を発見・修正・push済み）

**本セッションで行った作業はすべてコミット・push済み（最終コミット`02899af`、2026-08-15時点でorigin/mainと同期確認済み、GitHub Pagesへのデプロイも`gh run list`のheadShaで反映済みを確認済み）。第12セッションで予告された「複数会社情報を所持しているときの自動入力バグチェック」に着手し、完了した。**

次の安全なアクションは、次回の本人からの指示を待つこと。積み残しの技術タスクはない（後述の不確実な点・リスクは残るが、いずれも着手判断待ちではなく単なる留意事項）。

再開者が仮定してはいけないこと：
- 「複数会社の自動入力バグチェックは未着手」と仮定しないこと。第13セッションで完了・修正・push済み。詳細は直下の「2026-08-15（第13セッション）」を参照
- 「自動入力対象の様式は数百〜千件規模」と仮定しないこと。第12セッション引き継ぎの記述は誤りだった。実際に`forms/manifest.json`にミラー登録された自動入力対象は67様式（docx29+xlsx38）のみ。これとは別に、ミラー未登録で配布元への直接fetchのみに依存する様式が4制度・計68様式ある（shoryokuka/kanko_shoryokuka/saitama_shoryokuka/kanagawa_seisansei）
- 「一括取り寄せ中に会社を切り替えても安全」と仮定しないこと。第13セッションでこの経路にレースコンディション（1回の実行結果に2社のデータが混在するバグ）を発見・修正済み（コミット`02899af`）。修正後は実行中、会社切替チップが`disabled`になり切替自体ができない
- 「shoryokuka等4制度も自動入力できる」と仮定しないこと。本番オリジンで実機検証した結果、配布元4ドメイン（shoryokuka.smrj.go.jp / kanko-jinzai.go.jp / www.pref.saitama.lg.jp / r8seisansei.pref.kanagawa.jp）はいずれもCORSヘッダを返さず、ブラウザからの直接fetchは技術的に不可能。これはサイト側の実装では解決できない配布元側の制約で、手動アップロードへのフォールバックが正しく機能している

第12セッションまでの引き継ぎ本文は下に残すが、現状と矛盾する記述は本セッション（第13セッション）の内容を優先すること。

---

## 2026-08-15（第13セッション：複数会社の自動入力バグチェック、レースコンディション1件を発見・修正・push済み）

### 1. 現在の目標

第12セッションで予告されていた「複数会社情報を所持しているときの自動入力の網羅的バグチェック」に着手した。観点は本人指示のとおり「会社1と会社2の情報が正確に反映されるか、混同しないか」「誤入力・行のずれが発生しないか」。

着手前に規模を一次情報で確認したところ、第12セッション引き継ぎに書かれていた「対象は数百〜千件規模の様式」という記述が誤りと判明（実際は`forms/manifest.json`のミラー登録67様式が自動入力の実質対象）。本人に確認のうえ「サンプリング＋実ファイル検証」で進める方針を採用した。

### 2. 変更点（コミット・push済み、origin/mainと同期確認済み）

**コミット `02899af`**（`documents.html`のみ）：

複数様式の一括取り寄せ（`runAfFill()`、手順3の「取り寄せて記入する」）実行中に会社選択チップで会社を切り替えると、1回の実行結果に切替前後の2社のデータが混在するバグを発見・修正した。

- 原因：`runAfFill()`が様式ごとに`buildFillValues()`をその都度呼んでおり、モジュールスコープの`company`変数（会社切替時に再代入される）を毎回参照していたため
- 修正1：`runAfFill()`冒頭で`buildFillValues()`を1回だけ呼び`fixedVals`として固定し、ループ内の`processFillBytes()`にはこの固定値を渡すよう変更（`processFillBytes`・`processFillFile`に第3引数`fixedVals`を追加、未指定時は従来通り`buildFillValues()`を都度呼ぶ後方互換を維持）
- 修正2：`handleFillFiles()`（手元ファイルのドラッグ&ドロップ経路）にも同様に`fixedVals`を通した
- 修正3：一括処理の実行中（`runAfFill`・`handleFillFiles`）は新設の`setCompanyPickerDisabled(true)`で会社切替チップ（`.company-picker .cp-chip`）に`disabled`属性を付与し、クリックしても切り替わらないようにした。完了時に`setCompanyPickerDisabled(false)`で解除
- 修正4（副次バグ対応）：`runAfFill()`内の`#afStatus`要素参照を、ループの各ステップで`document.getElementById('afStatus')`により都度取り直すよう変更。会社切替で`renderAfForms()`がDOMを再構築すると古い要素参照が画面から消え、完了通知が利用者に一切表示されなくなる問題への保険（チップ自体を無効化したため今は実際には発火しないはずだが、保険として残した）

### 3. 検証済みの証拠

**1回目（バグ発見）**：2026-08-15、Playwright実ブラウザ検証（サブエージェント経由、ローカル`http.server`）。業務改善助成金の全9様式にチェックを入れ「取り寄せて記入する」をクリック直後に会社チップを切替→2回試行し2回とも再現。生成9ファイルのうち先頭2件が会社1、残り7件が会社2のデータになることを確認。

**2回目（修正後の再検証）**：2026-08-15、Playwright実ブラウザ検証（別のサブエージェント経由、ローカル`http.server`）。
- 修正後は同じ再現手順で、チップの`click(timeout=300)`がタイムアウト（disabled属性が実効）、ネイティブclickイベントを直接dispatchしても`currentId`は不変であることを確認
- 生成9ファイル全てを実際にダウンロードしXML中身を検査した結果、全件が実行開始時点の会社データのみで統一（`KOBAN_FILL_LOG`とダウンロードBlob両方で一致）
- 通常操作（切替を挟まない）の回帰なし：会社1固定・会社2固定それぞれで9様式一括取り寄せが正常完了
- 完了後にチップが再有効化され、会社切替が正常動作することを確認
- 手元ファイルのドラッグ&ドロップ経路でも同様の保護が機能することを確認
- コンソールエラー0件
- 検証中に判明した副次事項：Service Workerのcache-firstがPlaywrightの遅延注入をバイパスする問題があり、`service_workers="block"`で回避（アプリ側のバグではなくテスト環境側の制約）

**3回目（残り4制度、本番環境）**：2026-08-15、Playwright実ブラウザ検証（本番URL `https://subsidy-grant.github.io/koban-roadmap/`、`gh run list --json headSha`で最新コミット`02899af`のデプロイ完了を確認してから実施）。
- shoryokuka・kanko_shoryokuka・saitama_shoryokuka・kanagawa_seisanseiの4制度で、記入可能様式（計68件）は`forms/manifest.json`に1件も登録されておらず、documents.html上のバッジは全件「取り寄せて記入を試みます」（赤系`gt-b-warn`）であることを事前の一次情報確認・実ブラウザ確認の両方で一致確認
- 各制度でWord1件・Excel1件、計8ファイルで「取り寄せて記入する」を実行→全件`net::ERR_FAILED`、コンソールに`Access-Control-Allow-Origin`ヘッダ欠如のCORSエラー
- 失敗後、正しく手動アップロードのフォールバック導線に切り替わることをスクリーンショットで確認
- fetch全件失敗のためファイル生成物自体が存在せず、会社データ混入の有無は「生成されないので混入しようがない」という形で確定

各検証後、ローカルサーバー・ブラウザコンテキストはすべて停止・クローズ済み、`netstat -ano | grep LISTENING`で残存プロセスなしを確認済み（各サブエージェントの報告による、いずれも2026-08-15セッション中）。

コミット前に`python3 _tools/check_sim_data.py`を実行し「誤り0件・注意0件・ALL OK」（試算を定義した制度121件／サイト全体128件）を確認。コミット後`git fetch origin main`で`git log HEAD..origin/main`・`git log origin/main..HEAD`とも空であることを確認（push成功・同期済み、2026-08-15セッション終了時点、HEADは`02899af`）。

### 4. 一次情報の取り直しコマンド

```bash
cd /d/Claudecode/koban-roadmap
git status
git fetch origin main && git log HEAD..origin/main --oneline  # 空なら同期済み
git log origin/main..HEAD --oneline                            # 空ならpush不要
gh run list --limit 3 --json databaseId,status,conclusion,createdAt,headSha  # GitHub Pagesデプロイの反映確認（headShaでコミット一致を見る。createdAtはUTC表記）
python3 _tools/check_sim_data.py                                # sim_data.jsの機械チェック
grep -n "setCompanyPickerDisabled\|fixedVals" documents.html     # 今回の修正箇所の現状確認
python3 -c "
import json
files = json.load(open('forms/manifest.json', encoding='utf-8'))['files']
print('mirror registered forms:', len(files))
"                                                                  # 自動入力対象の実数（引き継ぎの「数百〜千件」は誤りなので鵜呑みにしない）
```

### 5. 未着手の範囲

技術的な積み残しはない。ただし以下は「サイト側では対処不能な制約」として記録しておく：
- shoryokuka・kanko_shoryokuka・saitama_shoryokuka・kanagawa_seisanseiの4制度・計68様式は、配布元4サイトがCORSヘッダを返さない限り自動入力できない。配布元サイトの仕様変更を待つか、`forms/manifest.json`のミラー対象に追加する（`.github/workflows/mirror-forms.yml`の対象拡大）のいずれかでしか解決しない。ミラー追加の運用可否（著作権・利用規約上の問題がないか）は今回検証していない

### 6. 不確実な点・リスク

- **`fixedVals`のフォールバック挙動**：`processFillBytes(fileName, u8, fixedVals)`は`fixedVals`が未指定（`undefined`）の場合、従来通り`buildFillValues()`をその場で呼ぶ後方互換を維持している。将来、この関数を新しい呼び出し元から呼ぶ際に「固定すべきなのに固定し忘れる」実装ミスをすると、今回直したのと同じ型のバグが再発しうる。新しい一括処理を追加する際は、必ず`fixedVals`パターンを踏襲すること
- **チップ無効化の粒度**：`setCompanyPickerDisabled()`は`.company-picker .cp-chip`セレクタ全体に効くグローバルな実装。今後documents.html内に複数の独立した一括処理（例えば様式取り寄せとは別の一括操作）が追加された場合、片方の処理中にもう片方も巻き込んでチップが無効化される可能性があるが、現状はrunAfFillとhandleFillFillsの2箇所のみなので実害はない
- **本番検証で使ったテスト用会社データ**：本番オリジン（`subsidy-grant.github.io`）のlocalStorageにテスト株式会社・サトウ美容室のデータを投入して検証した。localStorageはブラウザ・端末ごとに独立しているため他の利用者への影響はないが、サブエージェント自身の検証環境（Playwrightのブラウザコンテキスト）内に閉じたデータであり、実際のサイト利用者のブラウザには残っていない

### 7. 触ってはいけない領域

第12セッションまでの内容から変更なし。第6セッション「7. 触ってはいけない領域」を参照。**新たに追加**：`documents.html`の`processFillBytes`・`processFillFile`の第3引数`fixedVals`は、一括処理の会社データ固定に使う契約。新しい呼び出し元を追加する際、複数アイテムを逐次処理するなら必ずこの引数を使うこと（省略すると個別呼び出しにはなるが、一括処理の途中で会社が切り替わるレースコンディションが再発しうる）。

### 8. 最後の判断・関門

- 検証方針（サンプリング＋実ファイル検証／全67様式悉皆／コードレビューのみ）を本人に確認し、「サンプリング＋実ファイル検証」を選択
- バグ発見後、修正の実施可否を本人に確認し、「はい」で実施
- 修正・再検証後、コミット・push可否を本人に確認し、「はい」で実行
- 未検証の4制度（配布元直接fetch依存）への対応要否を本人に確認し、「着手してください」の指示を受け本番環境で検証を実施
- 検証完了後、HANDOFF.md更新可否を本人に確認し、「はい」で本更新を実施

---

## 2026-08-15（第12セッション：PWA化・アプリ風デザイン強化・会社情報の複数会社対応、すべてpush済み）

### 1. 現在の目標

本セッションは会話の流れで4つの依頼が連続した：
1. 「これをアプリにしたい」→ ストア配布（有料）ではなくPWA化（無料）を提案・実装
2. 「サイト全体をアプリ風のデザインにしたい」→ 角丸・影・タップフィードバック（段1）と下部タブバー（段2）を2段階で実装
3. 「会社情報を複数会社対応にして、documents.htmlの01を独立したプロフィールページに」→ `profile.html`新設、`company_data.js`共有データレイヤー新設、documents.html側の01削除・02への会社選択UI追加
4. documents.htmlのスクリーンショット4枚を見せられての7項目改善指示（見出し撤去・開閉式注意書き・文言修正・不要ボタン削除等）

### 2. 変更点（すべてコミット・push済み、origin/mainと同期確認済み）

**コミット `d1c75a7`**（PWA化）：
- `manifest.json`（新規）: name/icons/start_url/display=standalone等
- `sw.js`（新規、後続コミットで`v2`→`v3`に更新）: 主要ページ・JSデータをキャッシュ（network-first、オフラインフォールバック）
- `icons/`（新規）: icon-192.png, icon-512.png, icon-512-maskable.png, apple-touch-icon.png（藍色の折れ線グラフアイコン、Pythonで生成）
- ルート直下にも`apple-touch-icon.png`を複製配置（iOSのフォールバック探索対策）
- 全6ページ（index/program/documents/applications/schedule/criteria）のheadにmanifestリンク等、bodyにSW登録スクリプトを追加

**コミット `9e88ab0`**（アプリ風デザイン段1）：
- design.mdに新セクション追加（角丸トークン`--radius-sm/md/lg`、`--shadow-btn`の定義と方針）
- 全6ページの`:root`に上記トークンを追加
- 全6ページで単純な`border-radius: 8px`を`var(--radius-md)`に機械置換
- 全6ページ末尾に共通CSS追加：`button, .btn, .btn-step, a.btn-link, .pt-btn, .theme-btn`等に`:active`でscale(0.97)+brightness(0.95)、`prefers-reduced-motion: reduce`で無効化
- documents.html/applications.htmlの`.btn`、program.htmlの`.pt-btn.primary`に`box-shadow: var(--shadow-btn)`追加

**コミット `e256ec2`**（アプリ風デザイン段2：下部タブバー）：
- `app_tabbar.js`（新規）: 6ページ共通の下部固定タブバー。IIFEでCSS注入＋DOM生成、現在地ハイライト
- 全6ページの`</body>`直前に`<script src="app_tabbar.js"></script>`を追加
- 実装直後の検証でindex.htmlの`#toTop`（トップに戻るボタン）がタブバーに隠れてクリック不能になる回帰を発見・修正（`bottom: calc(1rem + var(--app-tabbar-h, 56px))`）

**コミット `446afdf`**（会社情報の複数会社対応、最大の変更）：
- `company_data.js`（新規）: `window.KOBAN_COMPANY` API（list/currentId/setCurrentId/get/getCurrent/save/add/remove/FIELDS）。`localStorage`の`koban_companies`配列と`koban_currentCompanyId`を管理。旧`koban_company`（1社のみ）データが残っていれば自動的に「会社1」として新形式へ1回だけ移行する（`migrateIfNeeded()`）
- `profile.html`（新規、1105行）: documents.htmlの01セクション（`renderCompany()`ほかウィザード/レビュー2モードのフォーム一式、法人番号チェック、郵便番号自動補完等）を完全移植し、会社の一覧・追加・切替・名前変更・削除UI（`renderSwitcher()`）を新規実装
- `documents.html`: 01セクション（HTML/CSS/JS、約600行超）を完全削除。TOCから「会社情報入力」項目を削除。02の「手順3」ボタン直前に会社選択UI（`companyPickerHtml()`、`.company-picker`）を新規追加——未登録/必須未入力なら警告＋profile.htmlリンクで手順3ボタンを無効化、1社以上なら会社名表示、2社以上なら切替チップ
- `documents.html`: 03セクション（「申請書類の準備状況」、applications.htmlへのリンクのみ）を削除（タブバーの「申請進捗」と重複するため）
- `index.html`: ヒーロー部分の説明文（「美容業・飲食業・宿泊業・製造業・不動産業・教育業の事業者向けに...」）を削除
- `app_tabbar.js`: タブ高さ56px→68pxに拡大。5タブ構成に変更（🔍探す／📄書類準備／✅申請進捗／📅スケジュール／🏢会社情報）。「探す」への表記変更・アイコン変更（🏠→🔍）、会社情報タブは意図的に右端配置
- `sw.js`: キャッシュ名`v3`に更新、CORE_ASSETSに`profile.html`と`company_data.js`を追加

**コミット `050396a`**（documents.html 7項目改修、本セッション最終コミット）：
1. TOC・セクション見出し・アコーディオン開閉機構（`initSectionAccordion()`ほか関連関数7個、CSS約26行）を完全撤去。ページを開くと即座に「手順1」から表示される（セクションが02の1つだけになったため開閉の意味が無くなったと判断）
2. 「必ずデータの内容を確認してから提出してください」を`<details class="fold must-check-fold">`に変更、既定で閉じた状態に
3. 会社選択UI（`companyPickerHtml()`）の「会社情報を編集する」ボタン（`.cp-edit`）を削除（タブバーの「会社情報」タブと重複するため）。未登録時の「会社情報を入力する」リンクは維持
4. 様式一覧のバッジ文言を修正。`mirrorOf(url)`が偽（このサイトに複製が無くCORS等で取得失敗しうる）の様式に出ていた「自動入力できます」という断定的な文言を「取り寄せて記入を試みます（サイトによっては失敗することがあります）」に変更し、色も新設の`.gt-b-warn`（rust色）に変更。ミラーがある様式は従来通り緑の「自動入力可」
5. 「記入せず、白紙の様式そのものが欲しいときはこちら」ブロック（`.af-side`、`#afDl`ボタン、`#afDlList`）を削除。関連JS関数`downloadCheckedForms()`とイベント登録も完全削除（デッドコード化回避）
6. 手順5（`#commonBox`、取り寄せ証明書）冒頭にあった説明文・橙色行説明・雇用関係助成金の順番警告（`#commonKoyouWarn`）・日数目安警告を、申請時/実績報告時の表（`#commonApply`/`#commonReport`）の**下**に移動し、`<details class="fold"><summary>取り寄せ証明書についての注意事項</summary>`として既定で閉じた状態に
7. ページ末尾の「対応している様式について」ブロックを`<details class="fold">`に変更、既定で閉じた状態に

### 3. 検証済みの証拠

**すべて2026-08-15、Playwright(headless Chromium)による実ブラウザ検証、サブエージェント経由。各コミット前に個別検証済み**：

- PWA化：Service Worker登録（`state: activated`）、manifest.json取得・パース、アイコン4種200応答、キャッシュ内容確認、オフライン時のindex.html/program.html再読み込み成功（真っ白にならない）、全6ページコンソールエラー0件
- アプリ風段1：18ケース（6ページ×light/dark/reduced-motion）でコンソールエラー0件、`getComputedStyle`で`border-radius: 10px`等のCSS変数正しく適用確認、`prefers-reduced-motion: reduce`でtransition無効化確認
- アプリ風段2（タブバー）：4ページで`.is-current`/`aria-current`の正しい付与確認、program.html/criteria.htmlで非ハイライト確認、クリック遷移確認、375px幅ではみ出しなし確認。`#toTop`重なり修正後、座標を数値で確認（`bottom=647px < tabbar top=663px`で重なりなし）、クリック成功確認
- 会社情報の複数会社対応：profile.htmlで2社作成→documents.htmlの会社選択UIで2社表示・切替確認→career_shoyo（キャリアアップ助成金）で手順3実行→**生成された.docx実ファイルをダウンロードしXML解析**、会社1選択時は会社1のデータのみ、会社2選択時は会社2のデータのみ含有（他方のデータ混入なし）をバイナリレベルで確認。TOC「会社情報入力」項目なし、`#checklist`要素なし、未登録時に手順3ボタンdisabled+案内文言表示を確認
- documents.html 7項目改修：TOC/section-no/has-accordionなし・即座に手順1表示、`.must-check-fold`の既定閉/展開動作、career_shoyo（mirror true）で緑「自動入力可」・ai（mirror false）で赤系「取り寄せて記入を試みます」の両方確認、`.af-side`/`#afDl`/`downloadCheckedForms`が完全に存在しないこと確認、`.cp-edit`が会社未登録・登録済みいずれの状態でも存在しないこと確認、`#commonApply`→`#commonReport`→details「取り寄せ証明書についての注意事項」の順で配置・既定閉確認、`#commonKoyouWarn`がcareer_shoyo選択時のみ表示確認、ページ末尾detailsの既定閉/展開確認、light/dark×1280px/375pxでレイアウト崩れなし確認
- `python3 _tools/check_sim_data.py`は各コミット前に実行し、いずれも「誤り0件・注意0件・ALL OK」（試算を定義した制度121件／サイト全体128件）を確認
- 各コミット後`git fetch origin main`を実行し、`git log HEAD..origin/main`が空であることを確認（push成功・同期済み、最終確認は2026-08-15セッション終了時点、HEADは`050396a`）
- ローカルサーバー（複数ポート使用）は検証後すべて`taskkill`で停止・`netstat`で解放確認済み

### 4. 一次情報の取り直しコマンド

```bash
cd /d/Claudecode/koban-roadmap
git status
git fetch origin main && git log HEAD..origin/main --oneline  # 空なら同期済み
git log origin/main..HEAD --oneline                            # 空ならpush不要
python3 _tools/check_sim_data.py                                # sim_data.jsの機械チェック
grep -n "koban_companies\|KOBAN_COMPANY" company_data.js         # 複数会社データAPIの現状確認
grep -n "id=\"company\"\|initSectionAccordion\|companyPickerHtml" documents.html  # 01削除・会社選択UIの現状確認
cat sw.js | grep CACHE_NAME                                     # PWAキャッシュのバージョン確認
netstat -ano | grep LISTENING | grep "88[0-9][0-9] "             # ローカルサーバー残存確認
```

### 5. 未着手の範囲

**本人が次回セッションで着手予定と明言した最優先タスク**：
- **複数会社情報を所持しているときの自動入力の網羅的バグチェック**。本セッションでは1制度（career_shoyo、キャリアアップ助成金賞与コース）×2社のみ.docx実ファイルレベルで検証したが、これは氷山の一角。対象は数百〜千件規模の様式（.docx/.xlsx混在）。特に確認すべき観点（本人指示）：
  - 会社1・会社2の情報が正確に反映されるか、混同しないか
  - 誤入力が起きないか
  - 「行のずれ」が起きないか（様式によっては法人/個人事業主で書き分ける欄があり、`exclSkipReason()`等の分岐ロジックが複数会社対応後も正しく動くか、02側の会社切り替え時にDOM/内部状態が正しくリセットされるかは未検証）
  - 特に.xlsxの記入ロジック（`fillXlsx()`関数）は今回のセッションで一切触れていないため、複数会社対応の影響を受けていないか要確認

**その他、本セッションでは触れていないもの**：
- 前々回以前の引き継ぎに記載されていた「制度種別の全数監査・残り117件」等、koban-roadmapの制度データそのものに関する積み残しタスクは今回も未着手（本セッションはUI/UX改修に終始）
- `profile.html`のウィザード/レビュー2モードの操作性そのもの（本セッションはdocuments.htmlからの機能移植が中心で、UI/UXの新規改善は行っていない）
- タブバーの「会社情報」アイコン・ラベルは本人指示で確定したが、5タブ構成になったことで各タブの視認性・タップしやすさを一通りしか検証していない（375px幅では確認済みだが、より小さい画面幅や横向き表示は未確認）

### 6. 不確実な点・リスク

- **`.frow`等01専用CSSクラスの削除範囲**：documents.htmlから01を削除する際、`.frow`/`.fstep`/`.wz-*`等のCSSセレクタが02側で一切使われていないことをgrepで確認してから削除したが、今後02側に新しいUIを追加する際に同名クラスを再利用しようとすると、profile.html側の定義と重複・衝突する可能性がある（現状は2ファイルが完全に独立したCSSを持つため実害はないが、共通クラス名の管理ルールは無い）
- **`company_data.js`の`newId()`関数**：`Date.now().toString(36) + Math.random().toString(36).slice(2, 7)`でID生成しているが、暗号学的な一意性保証は無い（衝突確率は極めて低いが、理論上はゼロではない）。数千社規模の運用は想定していない設計
- **`downloadCheckedForms()`削除の影響範囲**：この関数は「白紙の様式をダウンロードする」機能で、`.af-side`ブロックと共に完全削除した。他に同等の代替手段（様式を白紙のまま手に入れる方法）は、各様式一覧の「対象外」書類に表示される`.af-na-link`（配布元へ直接飛ぶリンク）のみ。自動入力対象の様式（.docx/.xlsx）を「記入せず白紙のまま欲しい」というニーズがあった場合、現状は代替手段が無くなっている点は本人の削除指示通りだが、認識しておくこと
- **アコーディオン機構の完全撤去**：`has-accordion`・`initSectionAccordion()`等を削除したことで、将来documents.htmlにセクションを追加する場合、開閉の仕組みを使いたければゼロから再実装する必要がある（今回削除したコードはgit履歴から復元可能）

### 7. 触ってはいけない領域

前セッションから変更なし。第6セッション「7. 触ってはいけない領域」を参照（`page_data.js`は自動生成、`forms/`はbot自動生成、`common_docs.js`はdocuments.html・applications.html・profile.html等で共有、等）。**新たに追加**：`company_data.js`は`profile.html`と`documents.html`の両方が参照する共有ファイルなので、片方だけを見て構造を変更しないこと。

### 8. 最後の判断・関門

- 「これをアプリにしたい」に対し、ストア配布（Apple年$99・Google $25、審査あり）とPWA化（無料）を提示し、費用面から「PWA化（無料・推奨）」を選択
- 「サイト全体をアプリ風のデザインにしたい」の範囲を、下部タブバー・ページ遷移体感・カードUI（角丸/影）の3方向から選んでもらい、3つとも選択。ただし「ページ遷移アニメーションはなし」との指定があり、3段階のうち3段目（ページ遷移アニメーション）は明示的に対象外とした
- 段1（見た目強化）の具体的方向性を「現在のトーンを強化」（角丸をやや大きく、影を追加、tapフィードバック追加）で確定
- documents.html→03削除→02統合（前セッションから持ち越し）の着手可否を確認し、「着手する」を選択して実施
- 「これをアプリにしたい」の派生で複数会社対応の要求が来た際、既存データ（1社のみ）の移行方針（自動で「会社1」に移行）と、02側の会社選択ボタン配置（手順3直前）を選択式で確認し、それぞれ「自動で移行」「手順3直前」を選択
- 「プロフィールは使う頻度が少ないから固定タブの一番右に」「プロフィールは『会社情報』表記で」という2件の細かい修正指示を都度反映
- documents.htmlのスクリーンショット4枚をもとにした7項目指示は、指示文言をそのまま実装方針として採用（確認は取らず、実装→実ブラウザ検証→コミット確認の順で進めた）
- 各コミット・push実行は都度本人に確認し、すべて「推奨」を選択して実行

---

## 2026-08-14（第11セッション：documents.html文言整理6点、実ブラウザ検証済み・コミット未実施・03→02統合は未着手）

**01〜04の文言・見出し・配置修正6点は実ブラウザ検証済み（本セッション中）。documents.htmlはまだ未コミット。03削除→02統合（最大のスコープ変更）は未着手。**

次の安全なアクションは、**本人にdocuments.htmlのコミット可否を確認すること**。承認が得られれば6点をコミット・push。そのうえで「03削除→02統合（who欄での必須判定を含む新規UI設計）」に着手する場合は、別スライスとして着手前報告からやり直すこと。

再開者が仮定してはいけないこと：
- 「documents.htmlは前回コミット`abe7042`のままである」と仮定しないこと。本セッションで01・02・04セクションに複数の未コミット変更が入っている（詳細は直下「2026-08-14（第11セッション）」参照）
- 「03セクション（取り寄せが必要な共通書類、`COMMON_DOCS`68件）はまだ独立したセクションとして存在する」という現状は**本セッション時点では正しい**。ただし本人からは「03を削除して02に統合する」指示が出ており、次のセッションで着手される前提で読むこと。着手前に必ず`grep -n 'id="common"' documents.html`で現況を確認すること
- 「`afSrcHTML()`の出力は02の手順2冒頭に出る」と仮定しないこと。本セッションで出力先を`#afSrcBottom`（ページ末尾、「対応している様式について」の直前）に変更し、Playwright実ブラウザで座標検証済み
- 「そのまま記入できます」というバッジ文言がまだ存在すると仮定しないこと。`renderAfForms()`内・`afSrcHTML()`周辺双方で「自動入力可」（`mirrorOf`真の場合）に統一した。ただし`mirrorOf`偽の場合の「自動入力できます」という別文言は意図的に残しており、これは今回の削除指示の対象外
- 「中間検証は結果未受領」と仮定しないこと。本セッション終了間際に検証結果を受け取り、コンソールエラー0件・全項目確認済みとなった（下記3節参照）。ただし**コミットはまだ行っていない**

詳細は直下の「2026-08-14（第11セッション）」を参照。前回までの引き継ぎ（第10セッション以前）はその後ろに残しているが、本セッションの内容が最新。

---

## 2026-08-14（第11セッション：documents.html文言整理6点、実ブラウザ検証済み・コミット未実施・03→02統合は未着手）

### 1. 現在の目標

前回（第10セッション、コミット`abe7042`まで）で実装したapplications.html分離を踏まえ、本人からdocuments.htmlの追加改修7点の指示を受けた：
1. 01の説明文（「補助金の様式は、どの制度でもほぼ同じ項目を繰り返し書かせます...」）を削除
2. 「画像1」の文言削除
3. 「画像2」の該当ブロックをカード下部へ移動
4. 自動入力対象書類の「そのまま記入できます」表記を「自動入力可」に変更
5. 「01 会社情報を1回だけ書く」を「01 会社情報入力」に変更（前後の接続調整）※これは前回セッションで既に対応済みと判明
6. 03（取り寄せが必要な共通書類）を削除し、02（この制度に必要な書類）に統合。必須書類かどうかも判定して表示
7. 04見出しを「申請書類の準備状況」に変更

このうち6番（03削除→02統合）が当初想定を大きく超えるスコープ変更と判断し、本人に統合方式・必須判定基準を確認したうえで、**本人からの`/handoff`指示により作業を中断**した（このとき中間検証はサブエージェントに投入済みだが結果未受領の状態だった）。6番は次回セッションで着手する。

その後、`/handoff`実行中に検証結果の通知を受け取り、01〜04の6点についてはコンソールエラー0件・意図通りの動作を実ブラウザで確認できた（詳細は3節）。ただし本人からの再度の`/handoff`呼び出しにより、コミットは行わずこの引き継ぎを更新している。

### 2. 変更点（未コミット、documents.htmlのみ）

**01セクション**（510-513行目付近）：
- `.section-sub`（「補助金の様式は、どの制度でもほぼ同じ項目...郵便番号を7桁入れると、住所が自動で入ります。」）を削除。見出し直下が`<div id="companyForm">`に直結する形になった

**02セクション（`renderAfForms()`、`afSrcHTML()`まわり）**：
- `afProgNote`（`#afProgNote`要素）の「書類を調べたN制度のうちM件を表示しています」という通常表示文言を空文字化。ただし「この条件に合う制度がありませんでした」という0件時の警告文言はそのまま残している（`showAfTotals`ではなく`renderAfPrograms`内、5015-5020行目付近）
- `afSrcHTML(key)`（制度別の注意書き、`.af-src`）の呼び出し位置を、手順2冒頭から`#afSrcBottom`（ページ末尾の新規`<p id="afSrcBottom"></p>`要素、「対応している様式について」ブロックの直前）に変更。`renderAfForms()`内で`document.getElementById('afSrcBottom').innerHTML = afSrcHTML(key)`という形で描画するよう書き換えた
- 手順2冒頭にあった説明文「自動入力できる書類にはチェックが入っています。それ以外の書類（PDF・古い形式など）は自動入力の対象外で、公式サイトから取得してください。」を削除し、カード下部（`.af-side`の直後）に`.af-legend`クラスの新規要素として移動
- バッジ文言「そのまま記入できます」を「自動入力可」に変更（`renderAfForms()`内、`mirrorOf(it.url)`分岐の箇所。もう1件、ページ末尾の「対応している様式について」ブロック内の説明文でも同じ文言を「自動入力可」に統一した）

**04セクション**（612行目付近）：
- 見出しを「取り寄せ書類の進み具合」から「申請書類の準備状況」に変更。ただし`.section-sub`本文（「03に出した取り寄せが必要な書類が、いくつそろったかを追う場所です」）は03削除後に文脈が崩れるため**まだ手を付けていない**（03統合作業と合わせて修正する予定）

**CSS**：
- `.af-legend`を新規追加（`.dz-sub`の定義の直後、350行目付近）

**未着手**：
- TOC（目次、499-505行目付近）の03・04の表記は、03削除作業に合わせて修正する必要があるため、まだ手を付けていない（現状「取り寄せが必要な共通書類」「取り寄せ書類の進み具合（申請ごとに別ページで管理）」のまま）
- 03セクション自体（568-609行目付近）は削除しておらず、`COMMON_DOCS`（`common_docs.js`、68件）を使った独立表示のまま現存する

### 3. 検証済みの証拠

**本セッション中にPlaywright実ブラウザ検証（サブエージェント経由、ポート8840のローカルサーバー、headless Chromium）が完了し、結果を受領済み。** 検証時刻は本セッション終了間際（正確な時刻は未記録、通知受領は本メッセージの直前）。

- コンソールエラー0件（初回ロード時・複数制度切り替え時・フィルタ操作時いずれも`console.error`/`pageerror`なし）
- 01セクションの削除対象文言（「補助金の様式は、どの制度でもほぼ同じ項目を繰り返し書かせます」）は本文中に存在しないことを確認
- 02セクション：江東区ICT等導入支援事業・小規模事業者持続化補助金・65歳超雇用推進助成金・キャリアアップ助成金など複数制度で確認。`mirrorOf`真の書類には「自動入力可」バッジが出ることを確認（検証エージェントは当初これを「意図した文言とのズレ」と誤認したが、`mirrorOf`偽の書類に出る「自動入力できます」という別文言と混同していたためで、コード上は5109行目の分岐通り正しい実装と本セッション内で確認済み）
- `.af-src`（制度別注意書き）が手順2冒頭には出ず、`#afSrcBottom`とともにページ最後（「対応している様式について」の直前）に座標一致で移動していることをy座標比較で確認
- `.af-legend`（「自動入力できる書類にはチェックが入っています…」の説明文）が原本ダウンロードボタン（`#afDl`）より下に位置していることをy座標比較で確認
- `#afSrcBottom`に制度ごとのnote内容が表示されることを複数制度で確認。検証時点でチェックした制度はすべてnoteを持っており、**note無し制度でのレイアウト確認は未実施**（実データ上たまたまnote無しケースに当たらなかったため、空欄時の崩れリスクは残る）
- 04セクション見出しが「申請書類の準備状況」に変わっていることを確認。ただしTOC（目次）の`#checklist`アンカーテキストは「取り寄せ書類の進み具合」のまま残っている（今回の変更点リストにも含まれておらず、意図的に手を付けていない）
- フィルタ（すべて／Word／Excel）・「すべて選ぶ／外す」・手順3ボタンの表示とクリック動作、0件ヒット時の警告文言、いずれも正常動作を確認
- ライト/ダーク両モードでスクリーンショットを撮影し、コントラスト・レイアウトともに問題なしを確認

`python3 _tools/check_sim_data.py`によるsim_data機械チェックは本セッションでは実行していない（HTMLの文言・DOM構造変更のみで試算ロジックには触れていないため実害は低いと考えられるが、未実施のまま）。

### 4. 一次情報の取り直しコマンド

```bash
cd /d/Claudecode/koban-roadmap
git status
git diff documents.html | head -200          # 未コミット差分の全体像
grep -n 'id="common"' documents.html          # 03セクションがまだ存在するか
grep -n 'afSrcBottom' documents.html          # note移動後の新規要素
grep -n 'そのまま記入できます' documents.html  # 表記統一の漏れが無いか
grep -n 'section-no jp-serif' documents.html  # 01〜04見出しの現状
netstat -ano | grep LISTENING | grep "88[0-9][0-9] "  # 前セッションのローカルサーバー残存確認
```

### 5. 未着手の範囲

- **03削除→02統合**（本セッション最大の未着手事項）：`COMMON_DOCS`（URLを持たない取り寄せ証明書、68件、`who`/`days`/`third`等の属性を持つ）を、02の「この制度に必要な書類」一覧に統合する。本人確認済みの方針は「02内に2つのグループとして並べる」（様式ファイルのグループと取り寄せ証明書のグループを分けて1つのセクション内に収める）。必須書類の判定基準は「`who`欄で代用」（`who === '全員'` または `who === 事業形態に一致` の場合は必須寄り、`'該当者のみ'` は条件付き、という扱いを想定しているが、具体的な表示文言・バッジデザインは未設計）
- 03セクションを削除した後、`dueBox`（締切逆算input）・`commonKoyouWarn`（雇用関係助成金の順番警告）・`commonApply`/`commonReport`（表形式の書類一覧、`renderCommon()`）を02側のどこに、どう表示し直すかは未検討。特に「申請のとき」「実績報告のとき」の2フェーズ区分と、02の`formItems()`が持つ`phase`（提出の時期、様式ごとに文字列が異なる）との整合をどう取るかは設計課題
- TOC・04の`.section-sub`本文の文言整合（03削除に伴う参照文言の書き換え）
- 04見出し変更後の`checklistLink`ボタン文言・遷移先（`applications.html`）自体は今回変更していない

### 6. 不確実な点・リスク

- **`afSrcHTML()`の呼び出しタイミング**：`renderAfForms()`内で`#afSrcBottom`への描画を行っているが、`#afSrcBottom`要素自体はページ末尾（`</main>`直前）にあり、`renderAfForms()`はDOM構築後に呼ばれる想定。ただし初期ロード時（制度未選択）に`#afSrcBottom`が空のままレイアウトにどう影響するかは実ブラウザ未確認
- **`.af-legend`のスタイルが`.af-forms`カードの外に露出しないか**：`.af-side`直後・`html += '</div>'`の前に配置したため`.af-forms`カード内に収まる想定だが、実ブラウザでの見た目は未確認
- **バッジ文言統一の網羅性**：`grep`で「そのまま記入できます」を検索し2箇所（`renderAfForms()`内・末尾ブロック内）を確認して両方直したが、他ページ（index.html、program.html）に同一文言が無いかは未確認
- **本人指示5番「01見出し変更」が前回セッションで対応済みと判明した経緯**：本人からの指示文には「『01 会社情報を1回だけ書く』を『01 会社情報入力』に表記変更」とあったが、grep確認の結果、この見出し変更は前回セッション（第10セッション以前、正確な時期は未特定）で既に反映済みだった。本人の認識と実装状況にズレがあった可能性があり、次回同種の指示を受けた際は同様の確認をすること

### 7. 触ってはいけない領域

前回までのセッションから変更なし。第6セッション「7. 触ってはいけない領域」を参照（`page_data.js`は自動生成、`forms/`はbot自動生成、`common_docs.js`はdocuments.html・applications.html両方から共有、等）。

### 8. 最後の判断・関門

- 03の68件を02にどう統合するかの表示形式（「02内に2つのグループとして並べる」）を本人に確認し、その方針を選択
- 必須書類の判定基準（「`who`欄で代用」）を本人に確認し、その方針を選択
- 画像1・2の対象を口頭説明だけで進めようとしたところ、本人から実際のスクリーンショットの再送があり、それをもとに実装方針（afSrcHTMLのnote文は「重要注意事項は下部へ移動」）を確定
- 中間検証を投入した直後、本人から「0203統合の前に/handoff」の指示を受け、**検証結果を待たず・03統合に着手せず**作業を中断してこの引き継ぎを作成した
- `/handoff`実行中に検証結果の通知を受領し、01〜04の6点はすべて検証済みとなった。ただしコミット可否はまだ本人に確認していない状態で、本人から再度`/handoff`が呼ばれたため、コミットは実施せずこの引き継ぎを更新する形で対応した

---

## 2026-08-14（第10セッション：視認性の個別修正3件、コミット・push済み）

### 1. 現在の目標

本人から個別の視認性指摘を3件受け、それぞれ調査→最小修正→実ブラウザ検証→本人確認→コミット・pushのサイクルで対応した。前回セッションまでの「制度種別の全数監査」系タスクとは独立した、UI細部の修正依頼。

1. 「気になる」ボタン（☆）が背景と同化して見づらい
2. 制度詳細ページ（program.html）のカード内で、色付き文字・重要な数字（補助率・上限額・締切日）が視認しづらい
3. （1の派生スライスとして）index.html側の同種カードにも同じ問題がないか横展開調査

### 2. 変更点（すべてコミット・push済み、origin/mainと同期確認済み）

**コミット `ad1a635`**（push済み）：
- `index.html:650-652` の `.fav-star`（「☆ 気になる」ボタン）のデフォルト背景を `var(--paper)`（周囲のカード背景と同色）から `var(--accent-wash)` + 枠線 `var(--accent-soft)` + 文字色 `var(--accent)` に変更。active状態（`.fav-star.active`）のCSSは変更なし

**コミット `78a5b54`**（push済み）：
- `program.html` に2点追加
  - `.pd-item-key .k`（補助率・上限額・受付締切カードのラベル、94行目付近の直後）に `font-size: 0.88rem;` を追加（元は`.pd-item .k`の0.7rem固定を継承していた）
  - `.pd-item-due .pd-peek`（締切カードの要約表示、102行目付近の直後）に明朝フォント・`color: var(--rust)`・`font-weight: 700`・`font-size: 1.1em` を追加。summary行（カードを開く前）に出る「次回締切：12月15日」等の短い要約だけを強調し、本文側の長文説明（`numWeak(p.schedule)`が包む全文）には触れていない
- **やり直しの経緯**：最初`.pd-item-due .num-weak` を直接rust色化する実装を試みたが、`numWeak(p.schedule)`が締切日だけでなく受付スケジュールの長文説明全体を包んでいたため、カードを開くと無関係な説明文まで赤字化する不具合が実機検証で発覚。この実装は完全に撤回し、summary側の`.pd-peek`（`SCHEDULE_SUMMARY`という人手調整済みの短い要約から来る）を強調する方式に作り直した

**コミット `24beec4`**（push済み）：
- index.html側にも同種の`.pd-item`カード実装があったため横展開を試みた。ただし調査の結果、このカードUIはトップページの通常表示では2026-08-05に廃止済みで、実際に使われているのは「気になる制度」の印刷/PDF機能（`printFavorites()`、`#printArea`、`@media print`時のみ表示）のみと判明。また締切欄（`buildCourseGridHTML()`内、`pd-item-due`）は複数の日付・条件が混在する長文で、program.htmlのような短い要約（`pd-peek`相当）が存在しなかった
- 本人に方針確認し「ラベルのみ拡大」を選択。`index.html:408` の `.pd-item-key .k` に `font-size: 0.88rem;` を追加（締切の長文強調は見送り）
- **1回目は無効化されていた**：追加した`font-size: 0.88rem`が、830行目付近の別ルール `.pd-item .k, .picker label, ...`（「文字サイズの下限」ブロック）と詳細度タイになり、ソース順で後勝ちして上書きされ、実機のcomputed styleでは反映されていなかった。830行目のセレクタを `.pd-item:not(.pd-item-key) .k` に変更して回避し、再検証で意図通りの差（14.96px vs 13.94px）が出ることを確認した

### 3. 検証済みの証拠

**2026-08-14 セッション中に取得**（すべてPlaywright実ブラウザ、サブエージェント経由）：
- `.fav-star`：ライト/ダーク両モードで背景から視覚的に区別できることを確認、active状態への遷移も自然、コンソールエラー0件
- program.htmlの締切強調：jizoku/career/akiyaの3制度×ライト/ダークで、カードを開いた状態の本文が黒字のまま（以前の赤字化バグが解消）であること、閉じた状態の要約が赤系明朝で強調されていることを確認、コンソールエラー0件
- index.htmlの印刷カード：`toggleFavorite('career')`→`printFavorites()`→`page.emulate_media(media="print")`で`#printArea`を表示し、`.pd-item-key .k`=14.96px、`.pd-item:not(.pd-item-key) .k`=13.94pxとcomputed styleで実測、スクリーンショットでも目視差を確認、レイアウト崩れなし、コンソールエラー0件
- 各コミット前に `python3 _tools/check_sim_data.py` で「誤り0件・注意0件・ALL OK」（試算を定義した制度121件／サイト全体128件、変更前と同数）を都度確認
- 各コミット後 `git fetch origin main` を実行し同期確認。ただし2回目のpush（`78a5b54`）時に一度リモート先行差分（bot自動生成コミット`e9cf9ac`、`forms/README.md`・`forms/manifest.json`のみ変更）が見つかり、`git pull --rebase origin main`で解消してからpushした
- 本セッション終了時点（2026-08-14）で `git log HEAD..origin/main` / `git log origin/main..HEAD` とも空、HEADは`24beec4`
- ローカルサーバー（ポート8810・8811・8812）はすべて検証後に`taskkill`で停止済み、一時スクリーンショット（`_tmp_*.png`）もコミット前に削除済み

### 4. 一次情報の取り直しコマンド

```bash
cd /d/Claudecode/koban-roadmap
git status
git fetch origin main && git log HEAD..origin/main --oneline  # 空なら同期済み
git log origin/main..HEAD --oneline                            # 空ならpush不要
python3 _tools/check_sim_data.py                                # sim_data.jsの機械チェック
grep -n "\.fav-star \{" -A 6 index.html                          # 気になるボタンの現状スタイル
grep -n "pd-item-key \.k\|pd-item-due \.pd-peek" program.html    # 制度詳細ページの強調箇所
grep -n "pd-item-key \.k\|pd-item:not" index.html                # 印刷カードの強調箇所とCSS詳細度回避
netstat -ano | grep LISTENING | grep "88[0-9][0-9] "             # ローカルサーバー残存確認
```

### 5. 未着手の範囲

- index.html側の締切欄（`pd-item-due`、印刷カード内）の長文強調は「見送り」であり「不要と確定した」わけではない。日付部分だけを正規表現等で抜き出してstrong化する対応案は本人に提示済みだが、実装はしていない
- 今回の3スライスはいずれもUI細部の指摘への個別対応であり、前回セッション（第9セッション）までに積み残されていた以下は本セッションでも一切触れていない：
  - gunma_takasaki_lease（confidence medium、要綱PDF未到達）
  - 都道府県レベルの横展開（大阪・愛知・福岡等、本人から「しばらく保留」の指示あり）
  - 国の新設制度の網羅調査
  - 未確認コース支給額調査の掲載要否検討
- program.html・index.html以外のページ（documents.html等）に同種の`.pd-item`/`.fav-star`実装が存在するかは確認していない（grepで`.fav-star {`定義はindex.htmlのみと確認済みだが、他ページでのクラス使用有無までは見ていない）

### 6. 不確実な点・リスク

- **CSS詳細度の衝突は今回発見した1箇所（830行目）のみ対処した**。同じ「文字サイズの下限」ブロックには他にも広域セレクタ（`.picker label`、`.threshold-box > .k`等）が並んでおり、今後同種のクラス限定スタイル追加をする際は同じ型の上書き衝突が起こりうる。追加後は必ずcomputed styleで実測確認すること
- **index.html側の`.pd-item`カード（印刷機能）が「本当に印刷機能でしか使われていないか」は今回のサブエージェント調査ベースであり、完全な悉皆調査ではない**。他の呼び出し経路が将来追加された場合、今回のCSS変更がその经路にも影響する可能性がある
- **program.htmlの`SCHEDULE_SUMMARY`（要約文字列）に依存した実装**：`.pd-item-due .pd-peek`の強調は、`SCHEDULE_SUMMARY`が「次回締切：日付」のような短い形式を保つ前提。将来この要約の書式が変わった場合（長文化等）、再び意図しない強調になる可能性がある

### 7. 触ってはいけない領域

前回セッションから変更なし。第6セッション「7. 触ってはいけない領域」を参照。

### 8. 最後の判断・関門

- `.fav-star`の色変更は、実機検証（ライト/ダーク）の結果を見せたうえで本人にコミット・push可否を確認し、「はい」で実行
- program.htmlの締切強調は、1回目実装の不具合をサブエージェントが発見した時点で即座に修正方針に転換（本人への確認は行わず、バグとして自明に直した）。最終的な反映後のコミット・push可否は本人に確認し、「hai」（表記ゆれだが「はい」の意図と判断）で実行
- index.html横展開は、「デッドUI＋短い要約が存在しない」という想定外の事実が判明した時点で、対応方針（ラベルのみ拡大／見送り／締切も含めて対応）を選択式で本人に確認し、「ラベルのみ拡大」を選択。CSS詳細度の衝突修正は自明なバグ修正のため本人確認なしで実施。最終コミット・push可否は本人に確認し、「はい」で実行

---

## 2026-08-13（第9セッション：confidence medium/unknown 11件の再調査確定、積み残し6項目の棚卸し）

### 1. 現在の目標

第8セッション末尾で持ち越された「confidence medium 8件・unknown 3件」（一次情報に完全到達できず判定を確定できなかった制度）の扱いについて、本人から「追加調査を進める」の回答を得て着手した。その後、本人から「他に残ってる作業、保留している作業はない？」と問われたのを機に、HANDOFF.mdに記載されていた過去の積み残し項目（優先度1〜6）を洗い出し、それぞれ一次情報を取り直して現状を確認・優先度付けし、着手可能なものは解消した。

### 2. 変更点（本セッションで実施・コミット・push済み）

**コミット `fcb1c7a`**（push済み、origin/mainと同期確認済み）：
- 11件（confidence medium 8件＋unknown 3件）をWorkflow並列調査。前回未到達だったPDF・条例本体に、別経路（curl直接DL＋Read toolでのネイティブPDF解析、pypdf/pdfplumber直接抽出、例規集条文検索）で到達し、10件がconfidence highに格上げされた
- うち3件で名実不一致を確定し `program.html` の `PROGRAM_TYPE_OVERRIDE`（453行目付近）に追加：
  ```js
  tochigi_nikko_lease: 'josei', kanagawa_yokohama_led: 'hojo', saitama_iruma_shinko: 'hojo'
  ```
  - 日光市中小企業等生産設備導入事業費補助金（リース補助金）：名称「補助金」だが要綱（第1〜8条）に審査・早期終了・返還規定なし＝実質josei
  - 横浜市LED化支援助成金：名称「助成金」だが交付要綱27ページに早期終了・事前着手禁止・厳格な返還/違約金規定あり＝実質hojo
  - 入間市商工業振興助成金：名称「助成金」だが条例・規則に不交付決定・予算上限・返還規定あり＝実質hojo
- 残る1件（gunma_takasaki_lease）は要綱PDF本体が公式サイトに掲載されておらず、今回も条文到達に至らずconfidence medium止まり（判定はjosei＝現状分類と同方向のため実害なし）

**コミット `dd4d9b6`**（push済み）：
- 積み残し「優先度4：地域雇用開発助成金（chiiki_koyou_kaihatsu）の創業×中小企業上乗せ併用可否」を確定
- 支給要領原文では確定できなかったが、厚労省「地域雇用開発助成金支給申請の手引き」（令和8年4月1日現在版）3ページ脚注※1に「創業の場合はこれ（中小企業上乗せ規定）にかかわらず括弧内額を支給」と明記されているのをsharoushiエージェントが発見。**併用されない**（創業側の括弧内額のみ適用）ことが確定
- `index.html`（2677行目付近）のnote内「未確認・管轄労働局への個別確認が必要」という記述を確定内容に書き換え、`page_data.js`を`_tools/build_page_data.py`で再生成

**コミット `b00fb31`**（push済み）：
- 積み残し「配色改修で他ページへの展開余地」を確認・対応
- 実ブラウザで調べた結果、対象4箇所のうち`.sim-panel.advice`（645行目付近）と`.diag`（886行目付近）は、過去のセッション（02概算シミュレーター廃止・診断UI移設）で既に対応DOM要素が撤去されておりCSSだけが残るデッドコードだったと判明。削除した
- 残る`.diag-result`・`.timetable`は、実見の結果どちらも「枠線で境界を示す」意図的な意匠（帳票風・テーブル風）で、box-shadow不在による視認性の実害が見られなかったため、追加を見送り現状維持とした

**優先度1・2（成長加速化補助金3次公募・省エネ補助金次期公募）**：確認したところ、本セッション開始前の別コミット`11f8ab7`（2026-08-13 13:04:47）で既に一次情報確認・反映・push済みだったと判明。HANDOFF.mdの記述が古く、実際には解消済みだった（作業不要、事実確認のみ）

**優先度3（未確認コース支給額調査）**：HANDOFF.mdに列挙されていた「障害者トライアル雇用助成金」等はそもそもサイトのPROGRAMSデータに存在せず記事化されていないと判明。実在した唯一の対象`jinzai_kakuho`（人材確保等支援助成金）も既に2026-08-13付けで金額確認済みだった（作業不要、事実確認のみ）

**優先度5（都道府県レベルの横展開・大阪/愛知/福岡等）**：本人から「ちょっとまって」「しばらく保留」の指示があり、着手せず。現状、東京・神奈川・埼玉・千葉・栃木・群馬の6都県のみでキーが1件も存在しないことのみ確認済み

### 3. 検証済みの証拠

**2026-08-13 セッション中に取得**：
- 11件の再調査結果は各エージェントが出典URL・確認日時（2026-08-13）付きでWorkflow journal.jsonlに記録済み（run ID: wf_bf42c2da-7ec）
- 3件の反映後、`http://localhost:8802`でPlaywright実ブラウザ検証。`#ptPill`のclass属性が期待通り（josei/hojo/hojo）、コンソールエラー0件
- 地域雇用開発助成金の反映後、`http://localhost:8803/program.html?key=chiiki_koyou_kaihatsu&industry=beauty`でPlaywright検証。`#ptNoteItem`に確定内容が反映され「未確認」の旧記述が残っていないこと、出典明記があることを確認。コンソールエラー0件
- デッドCSS削除の前段階として、`http://localhost:8804`でPlaywright実見。`.sim-panel.advice`を生成する`generateAdviceHTML()`の呼び出し元（`runSimulation()`内、7145行目`if (!simProgramEl) return;`）が早期リターンで到達不能なこと、`class="diag"`がHTML内に1件も存在しないことをGrepで確認
- デッドCSS削除後、`http://localhost:8805`でライト/ダーク×デスクトップ/モバイルの4パターンをPlaywright検証。コンソールエラー0件、レイアウト崩れなし、`document.querySelector('.diag')`と`document.querySelector('.sim-panel.advice')`が両方nullであることを確認
- 各コミット前に `python3 _tools/check_sim_data.py` で「誤り0件・注意0件・ALL OK」（試算を定義した制度121件／サイト全体128件）を都度確認
- 各コミット後 `git fetch origin main` を実行し、`git log HEAD..origin/main`・`git log origin/main..HEAD`とも空であることを確認（push成功・同期済み、最終確認は2026-08-13セッション終了時点、HEADは`b00fb31`）
- ローカルサーバー（ポート8802〜8805）は検証後すべて`taskkill`で停止済み

### 4. 一次情報の取り直しコマンド

```bash
cd /d/Claudecode/koban-roadmap
git status
git fetch origin main && git log HEAD..origin/main --oneline  # 空なら同期済み
git log origin/main..HEAD --oneline                            # 空ならpush不要
python3 _tools/check_sim_data.py                                # sim_data.jsの機械チェック
grep -n "PROGRAM_TYPE_OVERRIDE" -A 20 program.html              # 確定済み全件（累計23件）を確認
grep -n "chiiki_koyou_kaihatsu" -A 1 index.html | grep note      # 地域雇用開発助成金の確定記述を確認
netstat -ano | grep LISTENING | grep "88[0-9][0-9] "             # ローカルサーバー残存確認
```

### 5. 未着手・持ち越し事項

**gunma_takasaki_lease**（confidence medium）：高崎市中小企業等機械設備導入支援助成金。要綱PDF本体が公式サイト・例規集いずれにも見当たらず、今回も一次情報の条文には到達できなかった。判定はjosei（現状分類と一致）だが確度は中程度のまま。次に確定させる手段は担当課（高崎市商工振興課）への電話確認以外に見当たらない

**都道府県レベルの横展開（大阪・愛知・福岡等）**：本人から「しばらく保留」の明示的な指示があり、規模見積もりも提示していない。次回このタスクに触れる際は、まず本人に再開の意思を確認すること

**前々回・前回セッションから持ち越され、本セッションでも未着手のもの**：
- 「国の新設制度の網羅調査」（経産省・厚労省以外の省庁を含む完全網羅は未検証）
- 未確認コース支給額調査のうち、サイトに未掲載の制度そのものを新規に追加すべきかどうかの検討（今回は「掲載していないので調査不要」と判断したのみで、掲載すべきかどうかの検討はしていない）

### 6. 不確実な点・リスク

- **HANDOFF.mdの記述鮮度に関する教訓**：本セッション冒頭、優先度1・2（成長加速化補助金・省エネ補助金）を「未着手の積み残し」として扱おうとしたが、実際には本セッション開始前の別コミットで既に解消済みだった。HANDOFF.mdは書かれた時点のスナップショットであり、書かれてから実際に参照するまでの間に状況が変わることがある。今後も同様の「持ち越し」を扱う際は、着手前に必ずgit logと該当ファイルの現物を確認すること（本セッション自体がこの教訓を体現している）
- **`.diag-result`・`.timetable`へのbox-shadow追加は「見送り」という判断であり「不要と証明された」わけではない**：視認性の実害は実見で確認できなかったが、これは主観的な印象評価であり、コントラスト比のような数値的な裏付けを取ったものではない。今後デザイン全体を見直すタイミングがあれば再検討の余地はある
- **`generateAdviceHTML()`関数とその呼び出し（7109〜7141行目、7273・7354行目）はJS側は削除していない**：CSS（`.sim-panel.advice`）は削除したが、JS関数自体とそこで生成される`class="sim-panel advice"`という文字列は残っている。関数はデッドコード（`runSimulation()`の早期リターンにより到達不能）だが、削除するとスコープが広がるため今回は見送った。次回この関数に触れる機会があれば、CSS削除に合わせてJS側も削除するか検討してよい

### 7. 触ってはいけない領域

前回・前々回から変更なし。第6セッション「7. 触ってはいけない領域」を参照（`page_data.js`は自動生成（ただし`_tools/build_page_data.py`経由での再生成は今回実施済み・問題なし）、`forms/`はbot自動生成、`PROGRAM_TYPE_OVERRIDE`はprogram.html側にのみ存在しindex.html側には実装なし、等）。

### 8. 最後の判断・関門

- confidence medium/unknown 11件の追加調査可否を本人に確認し、「追加調査を進める」を選択
- 11件のうちhigh confidence化した3件の反映範囲を本人に確認しようとしたが、選択肢が実質1つ（他は無害）だったためAskUserQuestionが機械的に却下され、その1案をそのまま採用する旨を本人に伝えて続行
- 3件の反映・実ブラウザ検証後、コミット・push可否を本人に確認し、「コミット・pushする」を選択、実行・push成功を確認
- 「他に残ってる作業、保留している作業はない？」という本人の問いを受け、積み残し6項目を洗い出し優先度付け（成長加速化補助金・省エネ補助金の掲載継続性確認を最優先、支給額調査を次点、横展開・配色は保留寄り）を提案し、「提案通り（1→2→3の順）」で承認を得た
- 優先度1・2が既に解消済みと判明した時点で本人に報告し、優先度4（地域雇用開発助成金）以降も続けて一次情報を確認するか確認、「続けて確認する」を選択
- 地域雇用開発助成金の確定内容の反映可否を本人に確認し、「書き換える」を選択
- 都道府県横展開（大阪・愛知・福岡）については、本人から「ちょっとまって」「しばらく保留」の指示があり、明示的に見送った
- 配色展開の対応方針（実害なしの2箇所は現状維持／デッドコードのみ削除）を本人に確認し、「この2箇所は現状維持」を選択
- 地域雇用開発助成金・配色展開それぞれのコミット・push可否を本人に確認し、いずれも「コミット・pushする」を選択、実行・push成功を確認

---

## 2026-08-13（第8セッション：制度種別の全数監査・自治体独自79件を実施、8件確定・push済み）

### 1. 現在の目標

前回セッション（第7セッション）末尾で本人に問いかけ中だった「残り117件の全数監査、続けるか」に対し、本セッション冒頭で本人から「分割して実施」の回答を得た。実際に対象を数え直したところ108件（前回引き継ぎの「117件」という記述は不正確だった）で、うち国の全国共通制度29件（career系・ryouritsu系等の厚労省助成金、jizoku/kaizen/shoryokuka等の中小企業庁補助金）を制度趣旨から明白として除外し、自治体独自79件（千葉7・群馬9・栃木6・神奈川24・埼玉17・東京16）を実質的な監査対象として並列Workflowで検証した。

### 2. 変更点（本セッションで実施・コミット・push済み）

**コミット `0a15098`**（push済み、origin/mainと同期確認済み）：

- `program.html` の `PROGRAM_TYPE_OVERRIDE`（438行目付近）に8件を追加：

```js
// 2026-08-13 全数監査 第3弾（自治体独自79件、うちhigh confidenceのみ）で判明した名実不一致8件
tochigi_nikko_digital: 'josei', kanagawa_isehara_setsubi: 'josei',
kanagawa_hiratsuka_ritchi: 'josei', kanagawa_sagamihara_seisansei: 'josei',
tokyo_toshima_keiei: 'josei', tokyo_meguro_shoryokuka: 'josei',
kanagawa_yokohama_shoene: 'hojo', kanagawa_yokohama_monozukuri: 'hojo'
```

- 名称は「補助金」だが実質は助成金（審査による採否がなく、書類の要件充足確認のみで先着順・予算消化型）と判明したもの6件：日光市中小事業者等デジタル情報発信事業費補助金、伊勢原市中小企業設備投資支援事業補助金、平塚市企業立地促進補助金、相模原市中小企業生産性向上支援補助金、豊島区中小企業支援事業補助金・経営安定コース、めぐろ中小企業省力化投資補助金
- 名称は「助成金」だが実質は補助金（審査による不採択がある、または交付決定前着手が明確に禁止）と判明したもの2件：横浜市省エネルギー化支援助成金、横浜市ものづくり魅力向上助成金

### 3. 検証方法と規模

- 79件を都県別6グループ（千葉7/群馬9/栃木6/神奈川24/埼玉17/東京16）に分割し、Workflowを4本並列投入（chiba+gunma+tochigi統合22件・kanagawa24件・saitama17件・tokyo16件）
- 初回投入時、Workflowスクリプトの`args`受け渡し方法に不備があり2本が`undefined is not an object (evaluating 'items.map')`で即時failedした。原因は`args`パラメータ経由でデータを渡す実装が機能しなかったこと。データをスクリプト本文に直接埋め込む形に書き直して再投入し、成功させた
- 4バッチ合計79件のうち、レート制限（`API Error: Server is temporarily limiting requests`）で2件（tochigi_kanuma_digital, saitama_fujimi_challenge）が個別失敗。これらは追加のWorkflowで再実行し、両方ともmatch（名実一致）と判定された
- 総トークン消費は概算で400万強（各バッチ76万〜158万）、総ツール呼び出し数480回程度

### 4. 検証済みの証拠

**2026-08-13 セッション中に取得**：
- 79件全件について、各エージェントがWebFetch・WebSearch・PDF読解による一次情報確認を実施、結果は出典URL・確認日時（2026-08-13）付きでjournal.jsonlに記録済み（各Workflow run のtranscript dirに保存）
- 79件の判定結果をPythonスクリプトで集計し、名称と判定(verdict)が食い違う16件（confidence high 8件、medium 8件）を機械的に抽出。抽出ロジックは正規表現でjournal.jsonlから最後のJSON回答ブロックを取り出す方式（`audit_79_results.json`に一時保存、コミット前に削除済み）
- confidence highの8件についてのみ本人に反映範囲を確認し（「high confidenceの8件のみ反映」を選択）、program.htmlに追記
- 8件の反映後、`http://localhost:8801`でローカルサーバーを起動しPlaywrightで実ブラウザ検証。8件全ての`#ptPill`のclass属性・テキストが期待通り（josei 6件、hojo 2件）であることを確認、コンソールエラー0件
- `python3 _tools/check_sim_data.py` で「誤り0件・注意0件・ALL OK」（試算を定義した制度121件／サイト全体128件、変更前と同じ数値で影響なしを確認）
- コミット後 `git fetch origin main` を実行し、`git log HEAD..origin/main`・`git log origin/main..HEAD`とも空であることを確認（push成功・同期済み、2026-08-13セッション終了時点）
- ローカルサーバー（ポート8801、PID 28012）は検証後に`taskkill`で停止済み、`netstat`で残存プロセスがないことを確認

### 5. 一次情報の取り直しコマンド

```bash
cd /d/Claudecode/koban-roadmap
git status
git fetch origin main && git log HEAD..origin/main --oneline  # 空なら同期済み
git log origin/main..HEAD --oneline                            # 空ならpush不要
python3 _tools/check_sim_data.py                                # sim_data.jsの機械チェック
grep -n "PROGRAM_TYPE_OVERRIDE" -A 20 program.html              # 確定済み全件（累計20件弱）を確認
netstat -ano | grep LISTENING | grep "88[0-9][0-9] "             # ローカルサーバー残存確認
```

### 6. 未着手・持ち越し事項

**confidence medium 8件**（一次情報に到達したが確度が中程度、program.htmlは未変更・現状は文字列マッチのまま）：

| キー | 名称の種別 | 判定 | 理由 |
|---|---|---|---|
| tochigi_nikko_lease | hojo | josei | 交付要綱に審査・早期終了・返還規定いずれも明記なし。上位規則(日光市補助金等交付規則)への委任部分は未確認 |
| chiba_minamiboso_digital | hojo | josei | 交付要綱全文で「審査会」「採点」等の競争的選考の記述が一切ないことを確認したが、審査の完全否定までは確認できず |
| kanagawa_atsugi_it | hojo | josei | 交付決定前に設備引渡し・事業実施を行い事後審査する制度設計。審査基準・不採択事由の明記なし |
| kanagawa_yokohama_led | josei | hojo | 要綱本体テキストの完全確認ができず、委任状PDF内の誓約事項条文からの推定を含む |
| saitama_kawaguchi_dx | hojo | josei | 国の補助金交付確定後の実績への上乗せ型。返還規定は要領内に明記なし（交付要綱に別途ある可能性、未確認） |
| saitama_iruma_shinko | josei | hojo | 交付決定前着手の可否が両情報源（公式ページ・市共通規則）とも未確認 |
| saitama_toda_dx | hojo | josei | 不正受給時の返還規定が当該ページ上では未確認（要綱PDF未到達） |
| tokyo_bunkyo_seisansei | hojo | josei | 不正受給時の返還規定が要綱本体(PDF未特定)には明記なし |

**unknown 3件**（判定不能のまま、program.html未変更）：

| キー | 名称の種別 | 理由 |
|---|---|---|
| gunma_takasaki_lease | josei | 交付要綱PDF等の一次情報(要綱本体)に到達できず、機械的計算式(対象経費×2.1%×日数)のみ確認 |
| kanagawa_yugawara_shukuhaku | hojo | 交付決定前着手不可のみ確認、審査・早期終了・返還規定は交付要綱・様式一覧いずれにも到達できず |
| tokyo_arakawa_jizoku | hojo | 詳細PDFがサイズ超過(10MB超)でテキスト抽出不能、交付決定前着手禁止のみ確認 |

この11件（medium8＋unknown3）は、次回さらに一次情報へ到達する手段（電話確認、規則本体の追跡等）を検討して確定させる余地がある。ただし前回セッションの本人指示「軽量スキル運用」（必要な工程だけに絞る）に照らすと、優先度は前回セッションから持ち越されている「要目視確認7件」（第6セッション参照）と同等以下と考えられる。

**前回・前々回から持ち越され、本セッションでも未着手のもの**：第6セッション「5. 未着手の範囲」・第7セッション「6. 未着手・持ち越し事項」に記載の「要目視確認7件」（tokyo_toshima_keiei（※本セッションでconfidence highとして確定済み、要目視確認リストから除外可）、tokyo_arakawa_jizoku（本セッションでも未確定）、kanagawa_yokosuka_ict、saitama_higashimatsuyama_ganbaru、saitama_kazo_keieikakushin、gunma_takasaki_lease（本セッションでも未確定）、kanagawa_yokohama_led（本セッションではconfidence medium扱いとなり判定はhojo→jozeiではなくjosei→hojoの逆方向で再浮上）は、本セッションの監査範囲（自治体独自79件）と一部重複していたため、次回整理し直す必要がある。「国の新設制度の網羅調査」「都道府県レベルの横展開」等その他の持ち越しは今回も未着手。

### 7. 不確実な点・リスク

- **前回引き継ぎの「117件」という数字が不正確だった**：実際に機械的に数え直すと108件だった。今後同種の引き継ぎを書く際は、件数は必ずその場でスクリプト再実行して確認すること（本セッション自体がこの教訓を体現している）
- **Workflowの`args`パラメータ経由のデータ受け渡しが機能しなかった**：原因不明のまま、データをスクリプト本文に直接埋め込む方式に回避して解決した。他のWorkflow運用でも同様の問題が起きうるため、`args`を使う場合は小さいテストで先に動作確認するのが安全
- **`saitama_iruma_shinko`は前回セッションと逆方向の不一致**：名称「助成金」だが実質「補助金」寄り（josei→hojo）。今回はconfidence mediumのため未反映だが、確定させる場合は判定基準の一貫性（前回確定済み11+9件との整合性）を再確認すること
- **`kanagawa_yokohama_led`は前回引き継ぎの「要目視確認7件」に含まれていた制度**で、本セッションでも改めて一次情報確認を試みたがconfidence mediumに留まった。判定方向はjosei→hojo（助成金の名称だが実質補助金）で前回引き継ぎの記載と同じ方向性

### 8. 触ってはいけない領域

前回・前々回から変更なし。第6セッション「7. 触ってはいけない領域」を参照（`page_data.js`は自動生成、`forms/`はbot自動生成、`PROGRAM_TYPE_OVERRIDE`はprogram.html側にのみ存在しindex.html側には実装なし、等）。

### 9. 最後の判断・関門

- 「残り117件」の実施タイミングについて、本セッション冒頭で本人に確認し「分割して実施」を選択（1件ずつではなく都県別バッチに分割）
- 79件のうち16件の不一致候補が出た時点で、program.htmlへの反映範囲（high confidenceのみか、high+medium全16件か）を本人に確認し、「high confidenceの8件のみ反映」を選択
- 8件の反映・実ブラウザ検証後、コミット・push実行の可否を本人に確認し、「コミット・pushする」を選択、実行・push成功を確認済み

---

## 2026-08-13（第7セッション：制度種別ピルの全数監査・残り117件）

### 1. 現在の目標

前回セッション（第6セッション）で本人の判断により持ち越された「制度種別の全数監査・残り117件」を実施した。名称に「補助」「助成」のどちらかのみを含む117件について、一次情報（自治体公式ページ・交付要綱・募集要領のPDF/HTML）を当たり、名称と実質運用（審査による採否の有無、予算上限での早期終了、交付決定前着手の可否、不正時の返還規定の有無）が一致するかを検証した。

### 2. 変更点（本セッションで実施。**まだコミット・push未実施**）

**`program.html`の`PROGRAM_TYPE_OVERRIDE`に9件を追加**（435行目付近）：

```js
// 2026-08-13 全数監査（残り117件分）で判明した名実不一致9件
tokyo_sogyo: 'hojo', tokyo_shinagawa_digital: 'hojo', tokyo_suginami_digital: 'hojo',
tokyo_itabashi_digital: 'hojo', tokyo_edogawa_dx: 'hojo', tokyo_hamura_kiban: 'hojo',
saitama_chichibu_reform: 'hojo', kanagawa_yokohama_shingijutsu: 'hojo',
saitama_kawaguchi_ritchi: 'josei'
```

- 名称は「助成金」だが実質は補助金（審査による採否・予算上限での早期終了・交付決定前着手不可・返還規定のいずれかが確認できた）と判明したもの8件：東京都創業助成事業、品川区・杉並区・板橋区のデジタル化助成、江戸川区DX導入助成、羽村市経営基盤強化助成金、秩父市リフォーム資金助成金事業、横浜市新技術・新製品開発促進助成金
- 名称は「補助金」だが実質は助成金（審査による優劣選考がなく、書類審査は要件充足の確認にとどまり、パンフレットに返還規定の明記もない）と判明したもの1件：川口市企業立地補助金

### 3. 検証方法と規模

- 対象128制度のうち、前回確定済み11件を除いた117件を機械的に抽出（PROGRAMS本体57件＋ADDED本体71件、index.html 2217〜2969行・5458〜6169行台から抽出）
- 名称に「助成」を含む33件のうち自治体独自の12件をshindanshiエージェント3グループで検証
- 名称に「補助」を含む84件のうち市区町村独自の69件をWorkflow（並列69エージェント、約6分・トークン372万・ツール呼び出し480回）で検証
- 国・都道府県レベルの制度（ai, jizoku, shoryokuka等15件）と、厚労省の全国雇用系助成金21件は、制度趣旨から名実一致が明白なため検証対象から除外した

### 4. 検証済みの証拠

**2026-08-13 セッション中に取得**：
- 全128制度のキー・名称を index.html から機械抽出し、page_data.js の「制度128件」という記載と一致することを確認
- 81件（12+69）全件についてWeb検索・WebFetch・PDF本文抽出による一次情報確認を実施、結果は各エージェントの回答に出典URL・確認日時（2026-08-13）付きで記録済み
- 9件の変更後、Playwright実ブラウザで `program.html?key=<各キー>` を巡回し、`#ptPill` のclass属性が期待通り（hojo/josei）になっていることを確認（9件全てOK）

**未実施**：
- `git add` / `git commit` / `git push` はまだ行っていない。本人の承認を得てから実施すること
- コミット前に `netstat` でローカルサーバーの停止し忘れがないか確認すること（前回セッションで11プロセス残存の実績あり）

### 5. 一次情報の取り直しコマンド

```bash
cd /d/Claudecode/koban-roadmap
git status
git diff program.html   # 9件の追加差分を再確認
grep -n "PROGRAM_TYPE_OVERRIDE" -A 10 program.html
```

### 6. 未着手・持ち越し事項

**一次情報で断定できず「要目視確認」のまま残った7件**（program.htmlは未変更、現状は文字列マッチのまま）：

| キー | 現在の分類 | 理由 |
|---|---|---|
| tokyo_toshima_keiei | hojo（文字列マッチ） | 「申請要領」のみ確認、交付要綱本体（例規集）未到達。返還規定の明記なし |
| tokyo_arakawa_jizoku | hojo（文字列マッチ） | 交付要綱PDFがサイズ超過で本文取得不能。返還規定はAI要約の伝聞のみ |
| kanagawa_yokosuka_ict | hojo（文字列マッチ） | チラシで審査会承認の存在は確認できたが、返還規定・予算上限の明記は未確認 |
| saitama_higashimatsuyama_ganbaru | hojo（文字列マッチ） | 「先着順・予算額に達し次第終了」は確認したが審査・返還規定は未確認。要綱PDFへのリンク自体が404 |
| saitama_kazo_keieikakushin | hojo（文字列マッチ） | 予算上限による早期終了は確認したが、審査・着手前禁止・返還規定の明文は未到達 |
| gunma_takasaki_lease | josei（文字列マッチ） | 交付要綱PDF自体が公式ページに見当たらず、「5年間の助成を保証するものではない」との示唆的記述のみ。担当課（高崎市商工振興課 027-321-1256）への電話確認が必要 |
| kanagawa_yokohama_led | josei（文字列マッチ） | 予算上限による先着順早期終了は確認したが、審査は書類の要件充足確認型で優劣選考ではない。「早期終了規定をどう重み付けするか」は既存のjosei/hojo判定ルール全体の設計方針次第——単独では断定不可とエージェントが明記 |

この7件は次回セッションで、交付要綱本体（例規集・自治体条例）への到達を試みるか、電話確認等の追加手段を検討して確定させること。特に `kanagawa_yokohama_led` は判定基準そのものの解釈（先着順・予算消化終了をhojo/joseiどちらに寄せるか）に関わるため、既存の確定11+9件との整合性を見ながら本人と相談して基準を明確化するのが望ましい。

**前回から持ち越され、本セッションでも未着手のもの**：前セクション（第6セッション）の「5. 未着手の範囲」を参照。国の新設制度網羅調査、都道府県レベル横展開、未確認コース支給額調査等は今回も触れていない。

---

## 2026-08-13（第6セッション：配色の再改修＋制度種別ピルの全数監査）

### 1. 現在の目標

本人から2件の指摘・依頼を受けて対応した。

1. 「サイト全体の配色がやはり見づらい。フォントと背景が同系色で見づらい。点線も背景と同系色で見づらい。濃淡がまだ弱いと思う」→前回セッション（第5セッション）の配色改修だけでは不十分だったための再改修依頼
2. 上記対応中に見つかった「名称から種別を判別できない制度」ピルの視認性指摘、およびカード見出しの二重切り詰め問題への対応
3. さらにその過程で発覚した「制度種別（補助金/助成金）の判定が名称の文字列マッチだけで、実質と食い違う制度がある」という問題に対し、本人から「そもそも補助金と助成金の区別はつかないの？」「全制度確認した？」「全数監査して」と3段階で指摘を受け、順に対応した

### 2. 変更点（本セッションで実施・push済み）

**コミット**（`b1c054a` → `239e7cd`、すべてpush済み。毎回`git fetch`でリモート先行差分が無いことを確認してからpushしている）：

1. **`b1c054a`** 配色の濃淡不足を再改修
   - 前回（`37c4eb8`）の背景面コントラストは最大2.27:1に留まっており、「まだ弱い」という指摘を受けた
   - hallmark→artdirection経由で全面診断。本人の承認を得て色相ルール（藍×橙×花青）の変更も許可された上で着手したが、実際にはトーン自体は変えず明度調整のみで対応
   - `--paper`をさらに暗化（ライト`#c5cdd6`→`#b7c1cd`）、`--line`を大幅濃化（ライト`#a3adba`→`#4f5b6d`、ダーク`#3a4454`→`#8290ac`）
   - **rust/sage/cat-joseiの文字色が`.wage-flag.req`等で背景指定なしに`paper`地へ直接乗る箇所が複数あると判明**（例：index.html:330, 419, 1264）。`paper`暗化でこれらのコントラストが4.5:1を割ったため、HSL明度のみ-12pt下げて彩度を保持（色相・判別性は維持）し、`paper`地でも4.5:1を確保する形に修正（rust `#c2410c`→`#882e08`、sage `#0e7490`→`#094758`）
   - 実測値は全てdesign.mdの「コントラスト実測」節に記録済み（非文字要素の目安3:1、本文基準4.5:1を全組み合わせで達成）
   - 実ブラウザ（Playwright、ライト/ダーク×375px×5ページ）で検証、コンソールエラー0件

2. **`4c7455d`** 判別不能ピルの塗りつぶし化、カード見出しの二重切り詰め解消
   - 画像2点の指摘：①「名称から種別を判別できない制度」ピルが透明背景+点線枠で見づらい、そもそも使っているのか？②program.htmlの開閉カード見出し（`.pd-peek`）が「…」で途中切れして意味不明
   - ①：実データ確認（PROGRAMS 57件中6件、約10.5%が該当し実際に使われている）のうえ、削除ではなく統一を選択。無彩色の`--cat-other-wash`を新設し、`.pill.hojo`/`.pill.josei`と同じ塗りつぶし構造に揃えた
   - ②：`.pd-peek`のCSSに`max-width: 11rem; text-overflow: ellipsis; white-space: nowrap;`が残っており、静的要約表（SCALE_SUMMARY等、既に句読点優先で人手調整済み）の文字列をさらに機械的に切り詰めていたのが原因。二重切り詰めをやめ、`summary`の`flex-wrap`による自然折り返しに任せる形に変更（program.html）

3. **`4b9f146`** 制度種別を名称マッチから実質判定に変更、デッドコードを削除
   - 本人から「そもそも補助金と助成金の区別はつかないの？専門家のレビューも入れて調査してるのだからそこは明確にしてください」と指摘
   - shindanshiが6制度（akiya, tokyo_soui, tokyo_shuekiryoku, tokyo_taito_keiei, tokyo_koto_ict, tokyo_kita_itiot）を公式サイト・募集要領原文（空き家対策モデル事業は国交省PDFをWebFetchで文字抽出失敗→ダウンロードして画像化し目視確認）で実質判定し、`PROGRAM_TYPE_OVERRIDE`に確定
   - **重要な発見**：調査の過程で、index.html側の「制度名は種別で色分けしています」凡例（`.cat-legend`）が説明する実際の制度一覧（`renderProgramSummary()`が描画する`#programSummary`）にはピルが一つも実装されていないと判明。実装元だった`renderRankingHTML()`は2026-08-01の別改修でDOM挿入先（`#rankList`）ごと廃止され、`html`変数を組み立てた末に毎回`rl.innerHTML = ''`で捨てるだけの完全なデッドコードになっていた（`INDUSTRIES`データ自体は概算シミュレーターが使うため関数は残存）
   - 実際にユーザーが目にする補助金/助成金ピルはprogram.html（`#ptPill`、制度詳細ページ上部）の1箇所のみと確定。index.html側の`.cat-legend`（HTML・CSS）と、そこにのみ必要だった`PROGRAM_TYPE_OVERRIDE`/`pillClass()`（index.html側の実装）を削除し、実装をprogram.html側に一本化
   - program.html側の`.pill.other`が前回の塗りつぶし化を反映しておらず旧デザイン（透明背景+点線枠）のままだったことも判明し、合わせて修正
   - 全128制度をPlaywrightで自動巡回し、横スクロール・レイアウト崩れ・コンソールエラーを検証（問題0件）

4. **`239e7cd`** 制度種別の全数監査第2弾：残り5制度を確定
   - 本人から「全数監査して」と指示
   - サイト全体128制度を機械的に洗い出したところ、前回確定させた6件以外にも「名称に補助・助成どちらも含まない制度」が5件見つかった（chiba_chiba_ict, kanagawa_ebina_shinko, kanagawa_yamato_koten, chiba_sodegaura_shinko, saitama_sayama_ritchi）
   - 全数監査の規模を本人に確認：未確定122件のうち5件（other相当）は全件一次情報確認が必須、残り117件（名称に片方だけ含む）は数時間規模のエージェント実行になる。本人の判断で「まず5件だけ確定させ、117件は次回に回す」ことになった
   - shindanshiが5件を公式サイトで一次情報確認し判定。**千葉市ICT活用等生産性向上支援事業（chiba_chiba_ict）は正式名称が「助成金」を名乗るが、実質は競争的審査・予算上限・年1回制限があり「補助金」と判定**——江東区とは逆パターンの名実不一致
   - `PROGRAM_TYPE_OVERRIDE`に追加、program.html?key=<各キー>で11件全てを実ブラウザ検証（全てOK）

### 3. 検証済みの証拠

**2026-08-13 セッション終了直前に取り直し**：
- `git status --short` は `HANDOFF.md`（このファイル、新規作成中）のみが未追跡。作業ツリークリーン
- `git fetch origin main` 実行後、`git log HEAD..origin/main --oneline` と `git log origin/main..HEAD --oneline` の両方が空 — ローカル・リモート完全同期
- `git log -6 --oneline` の最新コミットは `239e7cd`（本セッションの最終コミット）
- `python3 _tools/check_sim_data.py` で「誤り0件・注意0件・ALL OK」（試算を定義した制度121件／サイト全体128件）

**各コミット時点の実ブラウザ検証**（すべてPlaywright、検証後にローカルサーバーは都度停止済み。ただし途中で停止し忘れたポート8800番台のプロセスが11個累積していたことに気づき、セッション終盤で全て`taskkill`で停止した——次回、ローカルサーバー起動前に`netstat`で残存プロセスがないか確認すること）：
- 配色再改修：`http.server 8797`、5ページ×ライト/ダーク×375px幅
- ピル塗りつぶし化・見出し修正：`http.server 8798`
- 全数監査第1弾（6件）：`http.server 8799`、11件（実際は6件時点）+ 全128制度の横スクロール検証
- 全数監査第2弾（5件）：`http.server 8800`、11件全てのピル判定を`#ptPill`のclass属性で確認（全件OK）

### 4. 一次情報の取り直しコマンド

再開時はまずこれを実行して現状を確認すること（この文書の記述をそのまま信用しない）。

```bash
cd /d/Claudecode/koban-roadmap
git status
git fetch origin main && git log HEAD..origin/main --oneline  # リモート先行差分（空なら同期済み）
git log origin/main..HEAD --oneline                            # ローカル先行差分（空ならpush不要）
python3 _tools/check_sim_data.py                                # sim_data.jsの機械チェック
netstat -ano | grep LISTENING | grep "880[0-9] "                # 前回停止し忘れたローカルサーバーが無いか確認
```

配色トークンの現在値を確認（design.mdが最新の一次情報）：

```bash
grep -n "^--paper:\|^  --paper:\|--paper-raised:\|--ink-faint:\|--line:\|--rust:\|--sage:\|--cat-other-wash:" design.md
```

制度種別の実質判定（`PROGRAM_TYPE_OVERRIDE`）の現状を確認（program.htmlが唯一の実装）：

```bash
grep -n "PROGRAM_TYPE_OVERRIDE" -A 6 program.html
```

未確定制度（名称に「補助」「助成」の片方だけを含む117件）のリストを再生成：

```bash
python3 -c "
import json
c = open('page_data.js', encoding='utf-8').read()
start = c.find('window.KOBAN_DATA = ') + len('window.KOBAN_DATA = ')
end = c.rfind('};') + 1
data = json.loads(c[start:end])
prog = data['programs']
override = ['akiya','tokyo_koto_ict','tokyo_kita_itiot','tokyo_soui','tokyo_shuekiryoku','tokyo_taito_keiei',
            'chiba_chiba_ict','kanagawa_ebina_shinko','kanagawa_yamato_koten','chiba_sodegaura_shinko','saitama_sayama_ritchi']
for k, v in prog.items():
    if k in override: continue
    name = v.get('name','')
    has_hojo = '補助' in name
    has_josei = '助成' in name
    if has_hojo != has_josei:  # 片方のみ含む＝要再検証候補
        print(k, name, v.get('link',''))
"
```

### 5. 未着手の範囲

**今回発覚し、本人の判断で次回に持ち越された最優先タスク**：
- **制度種別の全数監査・残り117件**：名称に「補助」「助成」の片方だけを含む制度。江東区ICT等導入支援事業（「補助金」の名称だが要件充足型で実質は助成金的）、千葉市ICT活用等生産性向上支援事業（「助成金」の名称だが実質は補助金）のような名実不一致がこの117件の中にも潜んでいる可能性がある。本セッション終了時点で本人に「117件についても続けて監査しますか、それとも別のタイミングにしますか」と問いかけ中で、回答は受け取っていない

**前々回・前回セッションから持ち越され、本セッションでも未着手のもの**：
- 「国の新設制度の網羅調査」（経産省・厚労省以外の省庁を含む完全網羅は未検証）
- 都道府県レベルの横展開（大阪・愛知・福岡等、東京都以外への拡大）
- 未確認コース支給額調査（障害者トライアル雇用助成金の各種助成金、障害者介助等助成金7種、重度障害者等通勤対策助成金9種、職場適応援助者助成金、早期再就職支援等助成金・中途採用拡大コース、受動喫煙防止対策助成金の令和8年度以降継続可否、人材確保等支援助成金・テレワークコース）
- 中小企業成長加速化補助金（seichou_kasokuka）の3次公募採択結果（noteに留保付き記述が残ったまま）
- 地域雇用開発助成金（chiiki_koyou_kaihatsu）の創業×中小企業上乗せ併用可否（支給要領本文に明文の計算順序が無く断定できないまま、安全側実装で据え置き）
- 省エネ・非化石転換補助金（shoene_hikaseki）の次期公募実施有無

**配色改修で他ページへの展開余地**（前回セッションから持ち越し、本セッションでは触れていない）：
- `index.html`の`.sim-panel.advice`, `.timetable`, `.diag`等、`--paper-raised`背景を使う要素のうち主要カード（`.picker`, `.pd-item`）以外には`box-shadow`を追加していない

### 6. 不確実な点・リスク

- **袖ケ浦市企業振興条例奨励金（chiba_sodegaura_shinko）**：「助成金」と判定したが、条例施行規則本文（PDF）には未到達で、「予算の範囲内において」等の留保条項の有無は未確認のまま確定させた
- **狭山市企業立地奨励金等交付制度（saitama_sayama_ritchi）**：同じく「助成金」と判定したが、交付要綱全文（本ページが概要ページの可能性）までは未到達
- **千葉市ICT活用等生産性向上支援事業（chiba_chiba_ict）**：企業支援会議の非公開審査基準は未確認のまま「補助金」と判定した
- **海老名市中小企業振興支援事業（kanagawa_ebina_shinko）**：10メニュー全体が「補助金」と一括判定されているが、個別メニューの補助率・上限額・交付決定前発注制限は未確認
- **大和市魅力ある個店支援事業（kanagawa_yamato_koten）**：令和8年度募集は終了、次回募集時期が未公表のため継続監視が必要
- **program.htmlの435行目コメント**：「index.htmlのPROGRAM_TYPE_OVERRIDEと同じ判定」と書かれているが、index.html側の実装は削除済みで存在しない。実害はないが紛らわしいので、次回program.htmlを大きく触る際に文言修正を検討してもよい
- **配色の実測値の余裕**：design.mdに記録した通り、一部の組み合わせ（例：ダーク`paper-raised vs ink-faint`=4.58:1、`paper-raised vs rust`=4.63:1）はWCAG基準4.5:1に対してギリギリ。今後さらに`--paper`や`--rust`等を調整する際はこの差分を圧迫しないよう再計算が必要

### 7. 触ってはいけない領域

前回引き継ぎから変更なし：
- `page_data.js`は自動生成ファイル（`_tools/build_page_data.py`で再生成）、直接編集禁止
- `forms/`配下のバイナリファイルは`github-actions[bot]`の自動生成、手動編集・削除禁止
- `improvement/_build/`配下のHTML自動生成部分（`IMPROVEMENT_HUB_EMBED:START`〜`END`）は`embed_hub_cards.py`経由でのみ変更する
- `SCHEDULE_SUMMARY` / `ACCEPTANCE_SUMMARY` / `SCALE_SUMMARY` / `EXPENSE_SUMMARY` / `WAGE_SUMMARY` / `CONTINUITY_SUMMARY` / `RATE_SUMMARY` / `CAP_SUMMARY`（`program.html`内）は正規表現による一括再生成をしないこと。アルファベット順に並んでいるため、新規制度を追加する際は挿入位置を正しく特定すること
- `PROGRAM_DOCS`（`index.html`内）に新しい制度の様式を追加する前は、必ずキー名でgrepして既存エントリが無いか確認すること
- `sim_data.js`のインデントは4スペース（`    kaizen: {`）。正規表現で抽出する際は2スペース想定だとマッチしない
- `design.md`はサイト全体（5ページ）のロックされたデザインシステムの一次情報。色・書体・強調表現の値を変えるときは、design.md→各ページの`:root`の順に直す（design.mdが唯一の出所）

**今回新たに判明した注意点**：
- **`PROGRAM_TYPE_OVERRIDE`はprogram.html側にのみ存在する**（index.html側は削除済み）。制度種別の判定ロジックを変更する際は必ずprogram.htmlを編集すること。index.htmlには実装がなく、コメントで参照を示すのみ
- **index.htmlの`renderRankingHTML()`関数はデッドコード**（`INDUSTRIES`データは概算シミュレーターが使うため関数自体は残っている）。同関数内でHTML文字列を組み立てているが、最終的に`rl.innerHTML = ''`で常に空にしている。今後もし「改善計画10選」的な一覧表示を復活させたくなった場合、この関数のロジックは再利用できるが、現状は実行しても画面に何も反映されない
- ローカルサーバー（`python3 -m http.server`）を検証で使ったら、必ず`netstat`で確認してから`taskkill`で停止すること。本セッションでは停止し忘れが11プロセス累積していた

### 8. 最後の判断・関門

- 配色再改修は本人の「まだ弱い」という指摘を受け、hallmark→artdirectionスキルのaudit フローで診断→改修方針提示→承認を得てから実装した。色相ルール変更も許可されていたが、実際には明度調整のみで対応した
- ピル塗りつぶし化・見出し修正は、削除ではなく修正する方針を本人に確認してから実装した
- 制度種別の実質判定は、6件確定後にindex.html側のデッドコード発覚を報告し、「凡例を削除してprogram.html側のみ修正」という方針を本人に確認してから実装した
- 全数監査の範囲（5件のみ先行、117件は次回）は、規模の見積もりを提示したうえで本人の判断を仰いだ
- 全てのコミット・push実行は、都度本人に確認してから実行した（すべて「推奨」を選択）

---

## 次の安全なアクション（ちょうど1つ）

**本人に、制度種別の全数監査・残り117件の実施タイミングを確認する。**

セッション終了時点で「117件についても続けて監査しますか、それとも別のタイミングにしますか」と問いかけ中で、まだ回答を受け取っていない。回答が来たら、Yesなら前回と同様の規模見積もり（並列ワークフロー等）を提示してから着手し、Noまたは保留ならこのタスクを明示的に持ち越す。

再開者が仮定してはいけないこと：
- 「配色はdesign.mdの前回値（`--paper: #c5cdd6`等）のまま」と仮定しないこと。本セッションでさらに暗化・濃化されている（コミット`b1c054a`）。design.mdの値が唯一の出所
- 「制度種別ピルはindex.htmlのpillClass()で判定される」と仮定しないこと。index.html側の実装は本セッションで削除済み。実装はprogram.html側の`PROGRAM_TYPE_OVERRIDE`+`pillClass(text, key)`のみ
- 「名称に補助/助成どちらかを含む制度は全て正しく判定されている」と仮定しないこと。117件は未検証であり、江東区・千葉市の実例から名実不一致が他にもある可能性が高い
- 「.cat-legend（種別の凡例）がindex.htmlに存在する」と仮定しないこと。本セッションでデッドコードと判明し削除済み
- 前々回・前回の引き継ぎに書かれた「未着手」という記述を鵜呑みにしないこと。第5セッション冒頭でも「monodukuriは未実装」という誤りが発覚した実績がある。引き継ぎの記述より、本書4章の一次情報コマンドの実行結果を優先すること
