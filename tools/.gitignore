# 点検を回すたびに作られる結果ファイル。中身は毎回変わるので残さない
#
# autofill_audit.json だけは例外で、わざと git に残している。
# 「どの様式のどのセルに何が入ったか」の控えで、2回流しても中身は同じ。
# 配布元が様式を差し替えたとき（mirror-forms が毎日05:00に取り直す）、
# 監査を流し直して git diff を見れば、どの欄がずれたか・消えたかが1目で分かる。
# これが無いと、様式が変わるたびに全部を見直すことになる。
sweep_labels.json
guard_out/
jinzai_out/
shots/
soui_test/
soui_out/

render_out/
sweep_out/
